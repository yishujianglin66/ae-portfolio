# Phase C 弃用端点命中统计 · 巡检报告

- **执行时间（UTC）**：2026-09-08 11:40
- **仓库根目录**：`C:\Users\Administrator\Desktop\AE-Knowledge-Vault`
- **指定脚本**：`scripts/phasec_tally.py`

## 1. 指定命令执行结果（按任务要求原样执行）

| 命令 | 退出码 | 输出 |
|---|---|---|
| `python scripts/phasec_tally.py --date 2026-09-08` | `2` | `can't open file '.../scripts/phasec_tally.py': [Errno 2] No such file or directory` |
| `python scripts/phasec_tally.py --all` | `2` | 同上 |

**结论**：`scripts/phasec_tally.py` 在本仓库中**不存在**（与 2026-09-07 巡检状态一致），两条命令均因文件缺失而无法运行，无法由脚本产出统计。

## 2. 回退核查：Phase C 真实观测数据源

任务不可执行，故直接核查弃用命中观测链路的真实落盘点：

- 观测中间件：`web/api_server.py:554`（`deprecation_middleware`）+ `web/integrator_api.py:687` 的 Deprecation 标记中间件，命中时向 `logs/deprecated_hits.jsonl` 落盘 `DEPRECATED_HIT` 结构化记录（`web/api_server.py:511-571`）。
- `logs/deprecated_hits.jsonl`：**不存在**（该文件由中间件在首次命中时惰性创建；从未创建 = 从未有命中）。
- 全 `logs/` 目录检索 `DEPRECATED_HIT`：**0 条匹配**（含 api-server / auth / metrics / media-system 等所有日志）。
- 弃用端点清单（来自 `_DEPRECATED_PATHS`，`web/api_server.py:537-544`）：

  | service | path（弃用） | canonical（网关规范） |
  |---|---|---|
  | mgmt-api | `/metrics` | `/metrics` |
  | mgmt-api | `/api/v1/resources` | `/api/v1/system/resources` |
  | mgmt-api | `/api/v1/alerts/active` | `/api/v1/system/alerts` |
  | mgmt-api | `/api/v1/alerts/history` | `/api/v1/system/alerts` |
  | mgmt-api | `/api/v1/compose` | `/api/v1/ai/plan-and-run` |
  | mgmt-api | `/api/v1/tools` | `/api/v1/engines` |

## 3. 判定与汇报

**规则 #1（当日命中总量 = 0）→ 适用。**

> **今日无弃用端点残留调用，观测期推进中。**

- 当天（2026-09-08 UTC）弃用端点命中总量 = **0**。
- 观测期内（自 Phase C 中间件上线以来）累计命中 = **0**（`deprecated_hits.jsonl` 从未生成，全日志 0 条 `DEPRECATED_HIT`）。

**规则 #2（当日有命中 → 逐端点催迁移）→ 不适用。** 当日无任何端点命中，无调用方需催促。

**规则 #3（近 7 天某端点持续为 0 → 可进入下线流程 Phase C 回归）→ 推断适用，但受限。**

- 因 `scripts/phasec_tally.py` 缺失，无法由脚本生成正式 7 天趋势序列。
- 但从真实数据源可推断：**全部 6 个弃用端点**自观测期起命中恒为 0，已连续 ≥7 天（实际为整个观察期）无任何残留调用。
- 据此可认为这 6 个端点**均符合进入下线流程（阶段 C 回归）的条件**——但此结论基于"全期 0 命中"的数据事实推断，而非 `--all` 趋势脚本的逐日输出；建议补齐统计脚本后做正式 7 日趋势确认。

## 4. 执行约束遵守情况

- ✅ 未修改任何代码或日志文件（仅读取与统计）。
- ✅ 未创建/写入 `logs/deprecated_hits.jsonl` 等观测数据。

## 5. 后续建议

1. 创建缺失的 `scripts/phasec_tally.py`（按 `--date <UTC日期>` 与 `--all` 两个模式解析 `logs/deprecated_hits.jsonl`），以恢复本自动化的正式统计与 7 日趋势能力。
2. 脚本就绪后，本自动化即可输出真正的逐端点 `service/path/canonical/hits` 与 7 日趋势，用于规则 #2/#3 的正式判定。
