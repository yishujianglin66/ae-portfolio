---
tags: [全面扫描, 提升方案, 项目治理]
date: 2026-08-20
status: 交付
---

# AE-Knowledge-Vault 全面扫描分析与项目提升方案（2026-08-20）

> 扫描方式：文档目录全量枚举 + 代码模块清单 + Git 状态实测 + 关键证据文档交叉验证
> 证据原则：所有结论标注证据路径；无直接证据处标注「待验证」或「证据不足」
> 前序依据：`03-阶段报告/PHASE10_全项目深度审计报告_2026-07-28.md`、`09-计划文件/2026-08-17_全面深度扫描报告.md`（该报告明确建议 08-20 复扫验证 P0 修复情况，本文即该复扫）

---

## 一、项目现状摘要

| 维度 | 状态 | 证据 |
|------|------|------|
| 项目定位 | 动漫短视频自动化生产线：感知→分析→规划→执行→渲染→验证→学习七阶段管线 + 15+ 软件引擎 + 多软件知识库 | `01-项目概览/AE项目模型全景图.md`、`09-计划文件/2026-08-17_后端架构重构-目标架构与分阶段执行计划.md §一` |
| 整体成熟度 | 约 77%（07-28 审计）；「先进骨架 + 沉重最后一公里技术债」 | `PHASE10_全项目深度审计报告 §3.1/§一` |
| Git 治理 | ✅ 较 08-17 大幅改善：当前在 `master`，`git status --short` 仅 19 行（08-17 时为孤立分支 + 628 未跟踪文件）；最近提交 2026-08-18（全参微调分类器 v4），08-17 以来 21 次提交 | 本次实测（2026-08-20） |
| 三条业务主线 | ① 运镜分类器：生产接入，粗 4 类 0.7729（`commit 6251551`），正迭代全参微调 v4；② 抠像：BiRefNet 混合微调 + FATE v17 交付定案；③ 木偶动画：进行中主线（部件木偶 + 刚性关节方向） | `09-计划文件/2026-08-16_全项目任务交接与推进同步文档.md §二/§三` |
| 缺陷治理 | D-01~D-24 缺陷矩阵：11 已修复(45.8%) / 8 部分修复(33.3%) / 5 未修复或延后(20.8%) | `09-计划文件/2026-08-11_24缺陷状态矩阵.md` |
| 知识资产 | 10-风格化剪辑知识库 259 文件 + 14-Silhouette 知识库 88 文件 + 11/12 大师库 24 文件；但 112 个文件未被管线消费 | `09-计划文件/2026-08-17_全面深度扫描报告.md §3.4` |

**一句话现状**：工程治理（Git/提交纪律）近三天显著改善，业务主线（分类器/抠像/木偶）持续推进；但**架构债（4 套 API 面、2 套数据层、桥接文件轮询 SPOF）、产物一致性（文档声称的权重/MOV 路径与磁盘实况不符）、过程记录（每日记录断更 13 天）**三类问题仍未根治。

---

## 二、文档体系扫描结果

### 2.1 编号知识库目录（00–15）

| 目录 | 主题/用途 | 规模 | 状态与关系 | 证据 |
|------|----------|------|-----------|------|
| `00-每日记录/` | 跨会话进度交接（五模块强制规范） | 30 文件 | 🔴 **断更**：最新 `2026-08-07_P4全量执行-新会话交接文档.md`，至今 13 天；08-08 后的 21+ 次提交、木偶主线、分类器 v4 均无记录 | 本次实测 `Get-ChildItem` |
| `01-项目概览/` | 项目全景 + after-effects-mcp 开源基线文档 | 4 文件 | 稳定；`after-effects-mcp-README/CLAUDE.md` 是开源基线的本地镜像 | 目录枚举 |
| `02-开发文档/` | 开发手册/编码规范/Bridge 避坑/开源集成分析 | 18 文件 | 核心参考；`AE-Bridge避坑指南.md` 被架构重构计划引用为头号风险证据；`开源集成全景分析与架构方案.md` 是集成决策源头 | `2026-08-17_后端架构重构 §一` |
| `03-阶段报告/` | Phase1~9 子目录 + 18 份里程碑报告 | 9 子目录 + 18 文件 | `PHASE10_全项目深度审计报告` 是成熟度基线；`冰海战记能力退化审计与端到端管线恢复报告` 记录过能力退化事件 | 目录枚举 |
| `04-设计模式与反模式/` | 设计模式/反模式/TEMPO 自评/陷阱警醒 | 10 文件 | 经验沉淀活跃（07-29、08-01 仍有新增）；与记忆中 111 条 pitfall 互补 | 目录枚举 |
| `05-测试套件/` | AE/MCP JSX 手工测试脚本 + 报告 | 40 文件 + 5 子目录 | ⚠️ 与 `tests/`（pytest 体系）并存且未打通；多为一次性调试脚本（Test1/Step1/SIMPLE_*），缺 README 导航 | 目录枚举、`05-测试套件/README.md` 存在 |
| `06-AI辅助开发/` | Lingma prompts 与安全手册 | 3 文件 | 稳定，规模小 | 目录枚举 |
| `07-MCP参考指南/` | 表达式库/预设目录/Particular 深指 | 8 文件 | 稳定；`preset-catalog.md` 与 `config/effect_presets.json` 对应（待验证同步性） | 目录枚举 |
| `08-安装与部署/` | setup_guide + model-server README | 3 文件 | 🔴 薄弱：与根目录 `Dockerfile`/`docker-compose.yml` 的实际状态脱节（compose 被架构重构计划判定「仅 backend+nginx，Redis/Celery/Postgres 一个没起」） | `2026-08-17_后端架构重构 §一` |
| `09-计划文件/` | 计划/交接/缺陷矩阵/E2E 结果 JSON | 45 文件 | 项目治理中枢；包含 11 个 `*_e2e_results.json` 验证产物；文档间有交叉引用（缺陷矩阵←集成方案←方向机计划） | 目录枚举 |
| `10-风格化剪辑知识库/` | AE/PR/PS/Blender/FFmpeg/Topaz/剪辑理论全软件知识 | 259 文件 + 1 子目录 | 最大知识资产；仅部分被管线消费（风格卡）；含 Phase1–4 全链路技术详解 | `08-17_扫描报告 §3.4` |
| `11-大师知识库/`、`12-漫剪拉镜大师/` | 剪辑大师方法论 | 15 + 9 文件 | ❌ 未被管线消费 | `08-17_扫描报告 §3.4` |
| `13-素材获取与搜索/` | 下载器/API/语义搜索/实战报告 | 4 子目录 + 2 文件 | 结构化良好 | 目录枚举 |
| `14-Silhouette知识库/` | Silhouette Roto/Paint/跟踪全域 | 88 文件 | ❌ 未被管线消费；含《完整性检查报告》自证 | `08-17_扫描报告 §3.4` |
| `15-3D模型与骨骼动画知识库/` | FBX/骨骼/蒙皮/Blender 渲染 9 个子领域 | 9 子目录 + README | 与木偶主线强相关；`11-木偶骨骼操纵工具链` 是最新增 | 目录枚举 |
| `99-收件箱/` | 未分类笔记 | 4 文件 | 含 07-29 利威尔 AMV 执行记录，未归档 | 目录枚举 |

### 2.2 根目录与其他文档

- 根目录 26 个 .md：含 `AE2025升级检查清单.md`、`AE_崩溃根因诊断与重装指南.md`、`PRESET_PIPELINE_CHECKLIST.md` 等运维向文档——**与编号目录职责重叠，属根目录治理遗留**（证据：本次实测根目录 283 个文件）。
- `docs/`：17 文件 + 6 子目录，含 `bridge_protocol_spec.md`、`director_system_architecture.md`、两份漫剪深度研究——**与 `02-开发文档/` 存在主题重叠**（架构文档两处存放），待验证是否需要合并。
- `TASK_STATUS.md`：🔴 **被单一任务（梁祝 Blender 模型，2026-07-28）占用**，未发挥项目级状态看板作用（项目管理技能要求其登记全局任务状态）。

---

## 三、模块内容分析（结论—证据—模块映射）

### 3.1 管线与核心智能层

| 模块 | 目标与主要文件 | 当前能力（已完成） | 进行中 | 问题/风险（证据） |
|------|--------------|-------------------|--------|------------------|
| `pipeline/` | UnifiedPipeline 七阶段主体：`unified_pipeline.py`、`unified_pipeline_execute.py`、`unified_pipeline_verify.py`、`feedback_loop.py`、`midstep_critic.py`、`ffmpeg_edit_engine.py` | v2/v2.1 框架、DAG 9 引擎端到端验证（07-30 记录）、TEMPO 中途自评落地（08-15 交付报告）、诚实降级（废除虚假成功，缺陷矩阵 D-09） | flagship_runner 精调、M2 参数调优迭代 | 瓶颈：AE Bridge 单槽渲染限制（`08-17_扫描报告 §5.2`） |
| `core/` | 高阶智能：`causal_engine.py`、`llm_gateway.py`、`observability.py`、`state_machine.py`、`workflow_orchestrator.py`、`experience_harvester.py` | LLM 网关多 Provider 路由 90%、状态机 85%、P4 可观测五模块（记忆佐证） | — | 🔴 因果引擎 `output_quality` 恒为 0（`data/causal_engine/execution_log.json`，`08-17_扫描报告 §3.2`）；`core/config.py` 与 `config_schema.py` 配置重复键（架构重构 §5.3） |
| `ai/` | Agent/Director/分类器/VLM 标注/训练任务（t7~t39 系列 80+ 文件） | V4 Tool Calling、运镜分类器 v3b 接入生产、IP 分类器、场景理解、VLM 三通道标注 | 🔴 **全参微调 v4**（08-18 提交：full-ft 加载分支 + CUDA 冒烟通过）、文献审计 V2 修正 4 项选型（OmniShotCut/Peak-End-Net 等） | t 系列脚本一次性特征明显、缺统一文档（`08-17_扫描报告 §2.3`）；离线约束下不能依赖云端实时推理（项目记忆） |
| `learning/` | 反馈闭环：`learning_loop.py`、`persistent_learning_loop.py`、`parameter_optimizer.py`、`result_verifier.py` | 读侧闭合（07-29 记录）、参数优化器 | — | 与 `pipeline/feedback_loop.py` 职责部分重叠（待验证边界） |
| `knowledge_base/` | KB 加载/扫描/抽取：`kb_loader.py`、`kb_scanner.py`、`llm_extractor.py` + `adapters/` | 正文检索 3933 块索引（记忆佐证）、风格上下文注入 planning | — | 🔴 112/371 知识文件未被消费；`vector_index_faiss.py`（根目录）闲置未接入（架构重构 §3.2 明示） |

### 3.2 执行引擎与桥接层

| 模块 | 目标与主要文件 | 当前能力 | 问题/风险（证据） |
|------|--------------|---------|------------------|
| `puppet-automation/` | 18~19 引擎编排（AE/FFmpeg/Topaz/DaVinci/Blender/Silhouette/AME/PR/PS/ComfyUI/RIFE/SAM2/Whisper/MoviePy/Audition…） | AE 引擎 95%、FFmpeg 90%（PHASE10 §2.2）；Blender puppeteer 引擎 v4 定版 + render_toon v6（08-16 交接 §3.3） | 5-6 个引擎停留框架级（Flux3 50%/SAM2 55%/OpenMontage 40%，PHASE10 §2.2）；🔴 **桥接文件轮询为头号 SPOF**：主线程 `$.sleep(500)` 死循环、无锁无认证（架构重构 §一）；HMAC 活跃路径未验签，`executeScript` 具 RCE 面（架构重构 §一/P0-1 纠偏） |
| `ae/` | AE Agent Pipeline 大脑中枢（280+ 方法五层架构）+ Bridge 客户端 | 五层闭环、双协议并存 | 🔴 headless 与 Documents 面板版双协议**仍共存竞争**（架构重构 §七；`2026-08-17_P0-2_AE桥接双协议只读诊断.md` 已产出） |
| `external/` | 28 个开源项目本地副本（after-effects-mcp、birefnet、matanyone、MediaCrawler、nexrender、ComfyUI、BeatNetLite、BlenderProc…） | 集成 P0/P1 全部完成、44/44 单测通过 | 红线约束：禁改 `after-effects-mcp/src` 与 `mcp-bridge-auto.jsx`（94609 字节）；D-13/15/24 因红线相邻延后 |
| `integrations/` | 开源适配器层 | ✅ **08-21 修复**：目录此前被清空（仅剩 0 字节空文件），已从 `feat/phase1.1-gateway-consolidation` 分支（`4b8aa81`，adobe 取 `3490aa2`）恢复 52 文件；`tests/test_opensource_adapters.py + test_nexrender_integration.py` 69/72 通过，剩余 3 失败=`core/llm_gateway.py` 缺 `TASK_TIER_MAP`/开源 TaskType（分支版 3362 行 vs 工作区 1380 行，随 P1-A 网关合并处理） | ✅ 已恢复验证（本次实测） |

### 3.3 编译器、Web/API、模型产物

| 模块 | 现状 | 问题/风险（证据） |
|------|------|------------------|
| `compiler/` | TS 原子编译器 Phase3/4/5 完整，成熟度 90%（全项目最高，PHASE10 §2.3） | 与 Python 侧 Phase1/2 交互依赖 AE Agent；含 `node_modules` 入库风险（待验证 .gitignore） |
| `web/` + `fastapi-skeleton/` + `knowledge_mcp_server.py` | API 服务层；`api_server.py` 2362 行 | 🔴 **4 套 API 面并存无统一网关**（架构重构 §一）；✅ P1-6 已修复：`api_server.py:2077` 现为 `from ai.ai_chat_api import`（本次实测） |
| `database.py`（根） vs `fastapi-skeleton` | 两套数据层 | 🔴 同步 sqlite3 裸 SQL 与异步 ORM 不连通、无迁移（架构重构 §一/§3.3） |
| `models/` | 权重 + 训练脚本：LoRA v1~v3、scene_classifier_v1.pt、rhythm_reward、camera_primitive_clf、model_registry.json | 旧版本目录堆积（v1/v2/v2c/v3/v3_first_attempt/cloud_backup 共 11 个 anime_camera_lora_* 目录，本次实测）；`08-17_扫描报告 §5.1` 建议仅保留最新版 |
| `style_copy/` | 风格复刻 MVP（LocalStyleAnalyzer→规则编排→真实转码 21479 字节成片） | D-09 收口证据（缺陷矩阵 Phase3 收口） |
| `vrs/` | 视频效果逆向系统 v1/v2 + RAG + orchestrator | 已打包成模块（记忆佐证）；v2 正确率量化已建设（07-30 记录） |

### 3.4 测试与验证

| 项 | 现状 | 证据 |
|----|------|------|
| `tests/` | 385 文件，pytest 覆盖 core/pipeline/引擎/API；`pytest.ini` testpaths = tests + mcp-extension/test + compiler/test | 目录枚举、`pytest.ini` |
| 覆盖率门禁 | 🔴 **形同虚设**：`fail_under=60` 但 CI 从不跑 `--cov`；pre-commit 未进 CI | `2026-08-17_后端架构重构 §一`（`pyproject.toml:197`、`.github/`） |
| E2E 产物 | `09-计划文件/` 11 个 `*_e2e_results.json`、`reports/` 88+ 份报告（accept_* 连续 08-07~08-17 每日验收） | 目录枚举 |
| 手工 JSX 测试 | `05-测试套件/` 40 文件与 pytest 体系割裂 | 目录枚举 |

---

## 四、问题与机会点清单（含 08-17 P0 复扫结论）

### 4.1 08-17 报告 P0/P1 项复扫结果（本次实测）

| # | 原问题 | 复扫结论 | 证据 |
|---|--------|---------|------|
| P0-1 | 孤立分支 + 16 staged 悬空 | ✅ **已解决**：当前 `master`，status 仅 19 行 | `git branch/status` 实测 |
| P0-2 | v3b 精度 0.4603 不达标 | ✅ **已按方案降级收口**：粗 4 类 0.7729 接入生产；正做全参微调 v4 冲刺 >80% | `08-16_交接文档 §2.1`、08-18 git 提交 |
| P0-3 | 628 未跟踪文件 | ✅ **已解决**（同 P0-1） | `git status` 实测 |
| P0-4 | 抠像产物丢失 | ✅ **闭环（08-21）**：`external/birefnet/finetuned_anime_mixed.pth`（844MB）+ `finetuned_anime_fp16.pth`（421MB）+ 基座 `model.safetensors` 均在盘（SHA-256 已登记）；FATE alpha.mov 全仓 + `D:\AE-Work\渲染档案\one_pipeline\..._v17\` 均确认丢失，但 report 留存 QC_PASS 且输入源 mp4 在盘，可重跑复现（~31s）；交接文档 2 处过期路径已改 | 本次实测 + `data/artifact_manifest.json` |
| P0-5 | 每日记录断更 | 🔴 **未解决**：最新仍为 08-07，断更 13 天 | 本次实测 |
| P1-6 | API import 路径错 | ✅ **已修复** | `web/api_server.py:2077` 实测 |
| P1-8 | 因果引擎质量分 0 | 待验证（未在本次复扫中重查 execution_log） | — |
| P1-9 | 知识库 112 文件未消费 | 待验证（无新证据表明已接入） | — |
| P2-13 | LoRA 旧版本堆积 | 🔴 **未解决且加剧**：11 个 anime_camera_lora_* 目录 | 本次实测 `models/output/` |

### 4.2 结构性问题清单（新增 + 存续）

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| S1 | 桥接文件轮询伪 MCP 是头号 SPOF + 活跃路径不验签（RCE 面） | 架构重构 §一/§四 P0-1 纠偏 | 自动化卡死、安全风险 |
| S2 | 4 套 API 面 / 2 套数据层 / 3 套配置各自为政 | 架构重构 §一 | 维护成本、`/metrics` 恒 404 |
| S3 | 根目录 283 文件（151 py）——上轮 365→126 瘦身后反弹 | 本次实测 vs 记忆「P0-P1-P2 治理」 | 可发现性差 |
| S4 | 文档声称与磁盘实况不符（integrations/ 适配器、birefnet 权重路径、FATE MOV） | 本次实测 | ✅ 已修复（08-21）：交接文档 2 处权重路径已改；integrations/ 52 文件已从 feat 分支恢复；FATE MOV 丢失已定性 |
| S5 | 每日记录断更 13 天 + TASK_STATUS 被单任务占用 | 本次实测 | 跨会话知识断层 |
| S6 | 覆盖率门禁虚设、05-测试套件与 pytest 割裂 | 架构重构 §一、目录枚举 | 质量回归无闸门 |
| S7 | 知识资产 112 文件闲置、FAISS 向量索引闲置 | 08-17 扫描 §3.4、架构重构 §3.2 | 知识驱动剪辑能力打折 |
| S8 | compose 依赖与代码预期不符（Redis/Celery/Postgres 未起） | 架构重构 §一 | 部署不可复现 |

### 4.3 机会点

| # | 机会 | 依据 |
|---|------|------|
| O1 | 全参微调 v4 + 文献审计 V2 已修正 4 项选型，运镜分类器有望突破 0.80 目标 | 08-17/08-18 git 提交 |
| O2 | 控制平面/执行平面分离已有完整设计（Phase 1~4 路线图），可直接执行 | 架构重构 §五/§六 |
| O3 | FAISS+CLIP 接入 Knowledge Service 可一次性盘活 112 闲置知识文件 | 架构重构 §3.2、`vector_index_faiss.py` |
| O4 | 每日 accept_* 报告已连续 11 天自动生成（reports/accept_20260807~0817），具备升级成 CI 质量门禁的基础 | reports/ 目录枚举 |
| O5 | 木偶主线方向已定（部件木偶+刚性关节，Live2D 式），15-知识库已有 9 子领域支撑 | 08-16 交接 §三、15-目录枚举 |

---

## 五、文档/代码/报告证据索引

| 主题 | 关键证据文件 |
|------|-------------|
| 成熟度基线（77%）与引擎完成度 | `03-阶段报告/PHASE10_全项目深度审计报告_2026-07-28.md` |
| 最新全量扫描与 P0 清单 | `09-计划文件/2026-08-17_全面深度扫描报告.md` |
| 架构重构目标与阶段计划 | `09-计划文件/2026-08-17_后端架构重构-目标架构与分阶段执行计划.md` |
| 缺陷治理台账 | `09-计划文件/2026-08-11_24缺陷状态矩阵.md` |
| 业务主线交接（分类器/抠像/木偶） | `09-计划文件/2026-08-16_全项目任务交接与推进同步文档.md` |
| 外部开源集成评估与验证 | `09-计划文件/2026-08-11_外部开源项目集成设计方案.md`、`2026-08-11_集成验证报告.md` |
| 光流分类器缺陷修复证据链 | `docs/camera_classifier_precision_report.md` |
| 管线阶段路线图 | `09-计划文件/开发路线图-阶段A至E.md` |
| Bridge 风险经验库 | `02-开发文档/AE-Bridge避坑指南.md`、`04-设计模式与反模式/AE脚本执行通道诊断与反模式.md` |
| E2E 验收产物 | `09-计划文件/*_e2e_results.json`（11 份）、`reports/accept_*.json`（11 份） |
| 性能与闭环验证 | `performance/PERFORMANCE_BENCHMARK_REPORT.md`、`performance/CLOSURE_VERIFICATION_REPORT.md` |
| 抠像选型证据 | `09-计划文件/2026-08-14_FATE抠像定稿报告_v14.md`、`2026-08-14_RVM对比评估报告.md` |

---

## 六、外部开源项目与研究资料映射

> 主要来源：项目自有调研（`2026-08-11_外部开源项目集成设计方案.md` 基于 GitHub 987 仓库筛选）；标注「外部新增」者为本次补充建议。

| 外部项目/方案 | 对应本项目模块 | 状态/潜在价值 | 适用边界 |
|--------------|---------------|--------------|---------|
| VoidChecksum/adobe-mcp（45 工具） | `ae/`、PS/PR 引擎、缺陷 D-02~D-24 | ✅ 已集成；COM+ExtendScript 双通道绕过旧 Bridge 缺陷 | 需 Adobe 运行中；2025 兼容性待验证 |
| inlife/nexrender（4k★） | `integrations/`、AE 模板渲染 | ✅ 适配器已恢复（08-21，源于 feat/phase1.1 分支 4b8aa81）；数据驱动渲染补齐 D-07 | 需 AE 渲染节点 |
| QwenLM/Qwen-MM-Plugins | `vrs/`、`core/llm_gateway.py` | ✅ qwen Provider 三档位接入；多模态分析增强 | 依赖 DASHSCOPE_API_KEY，离线受限 |
| NanmiCoder/MediaCrawler（61k★） | 素材获取（`13-素材获取与搜索/`） | ✅ 11 操作 7 平台就绪；D-10 数据源 | playwright 依赖 |
| firecrawl（165k★） | 联网调研能力 | ✅ 已集成含缓存 | 需网络 |
| DLR-RM/BlenderProc、ComfyUI | Blender/生成引擎 | ✅ 适配器就绪 | GPU 需求 |
| BiRefNet / MatAnyone / RVM / SAM2 / samurai / rmbg25（`external/`） | 抠像链路 | ✅ 已收敛为 BiRefNet；RVM 对比评估已出报告 | 权重产物需备份治理 |
| BeatNetLite / Mugen / auto-subs | 节拍卡点/字幕 | ✅ 已集成（`02-开发文档/漫剪卡点铁律`验证链路） | — |
| DOVER-Mobile 审美评分 | 质量评估（`core/quality_gate.py` 对应） | ❌ 调研完成未集成（许可证问题） | 许可证待解决 |
| OpenBiliClaw | B 站素材发现 Agent | ❌ 有计划未启动（`2026-08-14_OpenBiliClaw集成计划.md`） | — |
| **OmniShotCut / Peak-End-Net**（外部新增） | 镜头切分 / 情绪编排 | 🔄 文献审计 V2 已确认为选型修正方向（08-17 提交 `77ce67f`） | 待验证本地可训性 |
| **Alembic + Postgres + Redis**（外部新增） | 数据层统一（Phase 1.2） | 架构重构已选定方案 | 单机部署需降级 SQLite 路径 |
| **supervisord/nssm**（外部新增） | Bridge Manager 进程守护自愈（Phase 2） | 架构重构 §3.2 指定 | Windows 适配选 nssm |

---

## 七、提升目标

| 维度 | 现状 | 目标（4 周） |
|------|------|-------------|
| 过程记录 | 每日记录断更 13 天 | 恢复当日更新，断更 ≤1 天 |
| 产物一致性 | ≥3 处文档与磁盘不符 | 关键产物（权重/MOV/适配器）100% 有路径校验脚本 |
| 架构收敛 | 4 套 API 面 / 2 套数据层 | 单一 Gateway + 单一数据层（Phase 1 退出标准） |
| 桥接可靠性 | 双协议竞争 + 无验签 | 只读诊断→单协议收敛 + 进程互斥 |
| 质量闸门 | 覆盖率门禁虚设 | CI 非阻塞采集覆盖率并出报告；accept 报告接入 CI |
| 模型目标 | 粗 4 类 0.7729 | 全参 v4 acc > 0.80（golden set 200 条） |
| 知识消费 | 112 文件闲置 | FAISS 接入，闲置率 < 20% |
| 工程卫生 | 根目录 283 文件、11 个 LoRA 目录 | 根目录 ≤80 文件；LoRA 仅保留 v3+v4 |

---

## 八、分优先级行动项

### P0（本周，阻塞性/不可逆风险）

| # | 行动项 | 对应问题 | 步骤摘要 | 验收标准 |
|---|--------|---------|---------|---------|
| P0-A | 补录每日记录 + 重建 TASK_STATUS 项目级看板 | S5 | 补 08-08~08-20 关键决策（提交历史可直接提炼）；TASK_STATUS 从梁祝单任务改为全局看板（梁祝内容归档 `03-阶段报告/`） | 当日记录存在；TASK_STATUS 含三主线状态 |
| P0-B | 产物一致性核查与修复 | S4 | ① 全仓搜索确认 FATE MOV 是否在仓库外磁盘；② 更新交接文档权重路径为 `finetuned_anime_fp16/mixed.pth`；③ 定位 integrations 适配器真实位置并更新集成方案 | 关键产物路径 100% 可 `Test-Path` 通过；文档同步 |
| P0-C | P0-2 双协议只读诊断结论落地 | S1 | 已有 `2026-08-17_P0-2_AE桥接双协议只读诊断.md`：确认加载入口后停用 Documents 面板版，保留 headless，加单例哨兵 | AE 崩溃后命令不滞留；无双写竞争日志 |
| P0-D | 权重/数据备份机制 | P0-4 复发风险 | ✅ 已完成（08-21）：关键 .pth/.mov 产物登记到 `data/artifact_manifest.json`（7 产物，6 在盘 + 1 丢失可重跑，SHA-256 全计算）；⚠️ 新发现：备份盘 `D:\AE-Data\Models` 无任何权重备份，全部 not_backed_up，需补备份 | 产物清单含 SHA-256 与备份路径；备份执行待补 |

### P1（两周内，架构止血）

| # | 行动项 | 对应问题 | 验收标准 |
|---|--------|---------|---------|
| P1-A | Phase 1.1 统一 API 网关（按架构重构 §5.1） | S2 | 单一 `/api/v1/*` 入口；Redis 令牌桶限流；`/metrics` 真实可采 |
| P1-B | CI 覆盖率非阻塞采集 + accept 报告进 CI | S6/O4 | CI 产出覆盖率报告；`test_api_auth_fail_closed` 类安全测试常绿 |
| P1-C | 根目录二次治理 + LoRA 目录清理 | S3/P2-13 | 根目录 ≤80 py；旧 LoRA 移入 `models/output/_archive/` |
| P1-D | 因果引擎质量评估接入 | P1-8 | execution_log 的 output_quality 非 0 且与 quality_gate 一致 |
| P1-E | 05-测试套件与 pytest 归并方案 | S6 | 一次性 JSX 脚本归档，保留项纳入 README 导航 |

### P2（四周内，能力增值）

| # | 行动项 | 对应机会 | 验收标准 |
|---|--------|---------|---------|
| P2-A | FAISS+CLIP Knowledge Service 上线 | O3/S7 | 11/12/14 知识库可语义检索并进入 planning prompt；闲置文件 <20% |
| P2-B | 全参 v4 分类器验收 + golden set | O1 | 200 条人工标注 golden set；acc > 0.80 或给出回退决策 |
| P2-C | 数据层统一（Alembic + JSONB，架构重构 §5.2） | S2 | 两套数据层收敛为一；迁移脚本可重放 |
| P2-D | 调研结论转化：DOVER 许可证评估、OpenBiliClaw 启动决策 | §5.3 未转化清单 | 每项有「集成/放弃」书面结论 |
| P2-E | 木偶主线推进：look_at 固化进引擎 + 挥剑动画 MP4 | O5 | 完整动画 MP4 产出并入库 `output_production/` |

---

## 九、建议实施步骤（时序）

```
Week 1:  P0-A 补记录/看板(0.5d) → P0-B 产物核查(1d) → P0-C 双协议收敛(1d, 先只读确认) → P0-D 备份(0.5d)
Week 2:  P1-B CI 采集(1d) → P1-C 治理(1d) → P1-A 网关启动(3d, 评审后动手)
Week 3:  P1-A 网关收尾 → P1-D/P1-E → P2-B golden set 标注启动
Week 4:  P2-A FAISS 接入 → P2-C 数据层 → P2-D/P2-E 收尾
```

前置依赖：P1-A/P2-C 属较大改动，按架构重构文档要求**先评审计划再动手**；P0-C 触及活跃 AE 环境，必须先出只读诊断并经确认。

---

## 十、风险与回退方案

| 风险 | 概率 | 影响 | 回退/缓解 |
|------|------|------|----------|
| 双协议收敛误停活跃加载器，桥接失效 | 中 | 高 | 只读诊断先行；收敛前备份 `Startup/` 加载器；保留恢复脚本（记忆：Bridge 恢复全流程已有先例） |
| 网关重构破坏现有能跑的链路 | 中 | 高 | 演进式灰度：旧 API 面标记 deprecated 并行保留；每步可回滚（架构重构 §二原则 3） |
| 全参 v4 仍不达 0.80 | 中 | 中 | 回退粗 4 类 0.7729 生产基线；或合并 fine-10 为 6 类重训（08-17 报告已列双方案） |
| FATE MOV 确认永久丢失 | 低 | 中 | 权重在盘即可重跑 v17 管线复现（`09-计划文件/2026-08-14_抠像链路端到端验收报告_FATE.md` 保留完整参数） |
| 根目录治理误移活跃脚本 | 低 | 中 | 先 `scripts/_archive/` 移动观察一周再删；git 已覆盖可找回 |
| 红线误触（mcp-bridge-auto.jsx / after-effects-mcp/src） | 低 | 高 | 附加式增强原则；字节数校验（94609 字节基线）已纳入缺陷矩阵检查项 |

---

## 十一、待验证清单（证据不足项）

1. FATE alpha.mov 是否存在于仓库外磁盘（如 D:\AE-Data）——本次仅搜索了项目根。
   - ✅ **已闭环（08-21）**：仓内 + `D:\AE-Work\渲染档案\one_pipeline\..._v17\` + D 盘多盘搜索均未找到 MOV 文件；仅存 `pipeline_report` JSON（QC_PASS，finished_at 2026-08-16 01:41）。输入源 mp4 在盘，可重跑 v17 管线复现（约 31s）。结论：MOV 已丢失但可重跑复现。
2. `integrations/` 适配器真实位置（`nexrender.py` 等）——文档声称存在但目录仅 `color_presets/`，疑已迁移或并入根级 `opensource_integrations.py`。
   - ✅ **已闭环（08-21）**：目录被清空（0 字节空文件），适配器实位不在根级 `opensource_integrations.py`。已从 `feat/phase1.1-gateway-consolidation` 分支（4b8aa81，adobe 取 3490aa2）恢复 52 文件至 `integrations/`，验证 69/72 测试通过。
3. 因果引擎 `output_quality` 当前是否仍为 0（P1-8 未复核）。
4. `compiler/node_modules` 是否已被 .gitignore 排除。
5. 知识库 112 闲置文件是否已有新消费路径（08-17 后无新证据）。
6. Docker 部署可行性（PHASE10 阶段 1 建议项）——compose 现状已被架构重构判定不完整，需随 Phase 1 重估。

---

**结论—证据—模块映射说明**：本文每条结论均标注证据文件路径或「本次实测」；P0~P2 行动项均回溯至 §四问题编号（S#/O#），问题编号均回溯至 §二/§三的证据栏。无证据项已集中列入 §十一，不做主观推断。

**下次扫描建议**：2026-08-27（验证 P0-A~D 与 P1-B 完成情况）。
