---
tags: [视觉编排, 观感设计, 效果密度, 学习他人, 实证]
date: 2026-09-12
status: DESIGN
---

# 文字视觉观感编排设计与实证（2026-09-12）

> 触发：Boss 反馈「太乱了视觉上的观感，应该局部或者某些时间段应用；请分析并深度思考怎么设计编排视觉观感好，学习他人」。
> 方法：**先量化"他人"再定规则**（参照集实测）→ 定编排原则 → 实现 → **同帧 A/B 校验**。

## 一、学习他人：参照作品 vs 本片的节律实测

指标：逐秒帧间运动量（降采样灰度帧差）→ 归一化后判定"静段(<25%)/强段(>60%)"，并统计最长连续强段与峰均比。

| 作品 | 静段占比 | 强段占比 | 最长连续强段 | 峰/均比 |
|---|---|---|---|---|
| [AMV 4K] 5 Years by Donya | 31% | 17% | 2s | 2.50 |
| [AMV] exhausted by Donya | 48% | 21% | 2s | 3.04 |
| [一拳超人超燃AMV] JUST ONE PUNCH! | 38% | 19% | 2s | 2.91 |
| [前方高燃预警] 全员恶人 | 38% | 19% | 3s | 2.77 |
| [综漫AMV] 平凡的世界 | 41% | 16% | 3s | 2.81 |
| AKross2016 NanaMi 孤独者的荣光 | **78%** | 5% | 1s | 8.43 |
| AMV AGAINST FATE (AKROSS 2015) | **89%** | 3% | 1s | 5.95 |
| AMV FATE'S END (AKROSS 2015) | **70%** | 3% | 1s | 4.70 |
| **本片 v32e（编排前）** | **0%** | **100%** | **29s** | **1.15** |

**结论（数据化）**：一流作品的共同点是**大量留白 + 短促强峰**（静段 31-89%、强段仅 3-21%、
连续强段 ≤3s、峰均比 2.5-8.4）。本片则是**全程强、无收放、无峰**（峰均比 1.15）——
"乱"的本质不是某个效果丑，而是**没有对比**。

## 二、诊断：乱从何来

1. **效果密度过高**：12 个爆点每拍都挂冲击特效（约 1 次/秒），且叠加常驻发光/模糊/湍流。
2. **颗粒（v32）雪上加霜**：粗颗粒在画面上形成大块明暗斑（整帧均值可被抬高 60+），
   视觉上就是"脏"。
3. **缺少"平静段"**：intro/build/outro 与爆点用同一套强度，没有呼吸。

## 三、编排原则（本片采用）

| # | 原则 | 依据 |
|---|---|---|
| P1 | **留白优先**：特效只给一部分节拍（本次 6/22 = 27%），其余保持干净 | 参照静段 31-89% |
| P2 | **短峰**：单次特效作用 ≤0.6s（爆发后回落），不跨拍持续 | 参照连续强段 ≤3s |
| P3 | **对比而非堆叠**：同一拍最多一个主特效 | 参照峰均比高（说明峰与常态差距大） |
| P4 | **情绪映射**：颗粒/胶片感只属于"余韵/怀旧"段落；冲击特效只属于 drop 区 | 静止系 MAD 常识 + 本节实测 |
| P5 | **安静拍更干净**：没有主特效的拍子，把常驻效果（方向/径向模糊、湍流）再压轻 | 制造 P3 的对比 |

## 四、实现与证据

- **P1/P3**：`impact_recipe` 改为"隔拍"（drop 内 `i%2==0` 才给配方）→ 6 个特效事件 / 16 个安静事件；
  六种配方（抖动/射线/色散/回授/边缘射线/胶片闪）仍轮换且**同配方两次出现参数不同**。
- **P5**：安静拍的方向/径向模糊与湍流量统一 ×0.65。
- **P4（颗粒）**：**已停用**（默认关闭，`TEXT_OVERLAY_GRAIN=1` 可重开）。三条实证：
  ① 细颗粒（Freq 100）无损可见（局部 std ×1.86）但 **H.264@15Mbps 完全压掉**；
  ② 粗颗粒（Freq 12）形成大块明暗斑并把整帧提亮（4.5s 均值 57→127，采样点趋同≈恒定偏移）；
  ③ 关键帧化 Color Amplitude 在该版本未按预期生效（不做无依据补救）。
  → 结论：**在当前交付编码（H.264 15Mbps）下，颗粒收益为负**；若将来要颗粒，需先确认
  编码码率与颗粒尺度匹配（或改用无损/高码率母版）。
- **校验**：v34（编排版，无颗粒）与无颗粒基线 v31d **同帧均值完全一致**（Δ 0.0~−0.5）→
  编排没有引入亮度副作用。

## 五、后续可做（按性价比）

1. **build/intro 也做"轻变化"**：目前只有入场变款轮换；可让光晕颜色（青/金/白）随段落轮换，
   保持"有变化但不吵"。
2. **按音乐结构分段定强度**（而非按时长）：用 `production_report.json` 的段落/能量曲线驱动
   P1 的密度（副歌密、主歌疏），比固定"隔拍"更贴音乐。
3. **颗粒若要回归**：先提高母版码率或输出无损母版，再按"频率-编码"匹配实测。

## 六、方法沉淀（可复用）

- **判定"效果是否有效"必须同帧 A/B**：跨帧比较会被内容差异淹没（本次区域均值比较即失效）。
- **区分"效果没生效"与"被编码压掉"**：用无损输出模板（OM="无损"）做对照。
- **颗粒感用局部块 std（64×64 多块均值）**；但**粒度超过块尺寸时，局部 std 会低估"斑块感"**
  ——此时必须看同帧区域均值是否漂移。

---

## 附录：v35 音乐结构驱动强度（替代固定"隔拍"）

**动机**：固定"隔拍"只是机械均匀留白；更好的做法是**让留白跟着音乐呼吸**（副歌密、主歌疏）。

**实现**：
1. 用 `music_envelope`（31s / 1335 点 RMS）计算**局部强度** `local_i(t)`：取 t±0.4s 五点均值，
   再按 p10-p95 归一化到 0-1（消除整体响度差异）。
2. 阈值取 **drop 区事件 local_i 的中位数**（若取全体事件分位会被 intro/build 的安静值拉偏 ——
   实测特效只剩 1 个）。
3. **强度优先贪心**选取：按 local_i 降序，逐个选入并保证与已选事件间隔 ≥1.0s。
   （时间序"先到先得"会让最响的拍因间隔规则被漏掉 —— 实测安静拍里出现过 local_i 0.948）
4. 特效强度随音乐缩放：`ik = 0.75 + 0.6 × local_i`（0.75-1.35），作用于抖动幅度/射线亮度/
   色散强度/回授强度/胶片曝光等全部配方参数。

**实测（v35）**：
- 特效事件 **4/22（18%）**，全部为 drop 区最响的拍（local_i 0.601-0.748），间隔 1.85-4.62s；
- 18% 落在参照作品的强段区间（3-21%）内；
- 亮度同帧校验：除特效瞬间自身的局部提亮（+1.3 / +5.7）外，无整体偏移（Δ 0.0）。

**设计要点回顾**：留白不是"均匀地少"，而是**跟着音乐起伏**——安静段落给文字留出呼吸，
把视觉预算集中到音乐最响的时刻。

---

## 附录：v36-v38 —— 从"特效编排"转向"文字本身的收放"

### 诊断链（为什么会"感觉没啥变化"）

v34/v35 改了**结构**（隔拍 → 音乐驱动），但观感几乎没动。用逐帧"成片 vs 原始素材"差异
曲线查原因，得到一个反直觉结论：

- 差异曲线在各区**几乎平坦**（v34/v35/v36 均值 8.89 / 8.92 / 8.89），
- 而且**安静段（9.69）竟然强于 drop 段（8.00）**。

→ 说明：之前所有改动只动**短暂特效**（占全片时长极小），真正主导观感的是
**"文字本身从头到尾一直很重"**（大字号 + 厚描边 + 常驻光晕，22 个事件铺满 0-30s）。
特效只是瞬间，观感的"乱"来自**常态**。

> 方法教训：**先确认"你改的那一项是不是观感的主因"**，再动手。差异曲线的形状会直接告诉你
> 主因在哪；只看局部特效帧的 A/B 会一直在无头苍蝇式地微调。

### v37 时段处理分级（ZONE_TREAT）

按事件所属时段（intro/build/drop/outro）对**文字处理强度**做整体缩放：

| 时段 | glow | stroke 上限 | shadow | bevel | shimmer(通道分离) |
|---|---|---|---|---|---|
| intro | 0.0（关） | 2.5px | 0.5 | 0.4 | 否 |
| build | 0.45 | 5.5px | 0.8 | 0.7 | 否 |
| drop | 1.0 | 12px | 1.0 | 1.0 | 是 |
| outro | 0.6 | 4.5px | 0.9 | 0.8 | 否 |

同时：`glow<0.7` 时强制关掉第二层发光；非 drop 时段强制 `chroma=False`。

**实测（基线无关指标 = 事件位置 560×340 窗口内 luma≥232 像素占比 "ink"）**：
intro 明显变轻（0.0765 → 0.0113），但 **build 几乎没变（0.1239 → 0.1236）**。

→ 原因：build 段的视觉重量主要来自**白色字身本身**（字号），光晕/描边的加减在窗口里被字身淹没。
**要拉开时段差异，主杠杆是字号，不是光晕。**

### v38 分段字号倍率（主杠杆）

```
ZONE_TREAT[zone]["size"]:  intro 0.78 / build 0.70 / drop 1.10 / outro 0.95
size = max(60, round(size * 倍率))     # 仍受字号下限门禁 ≥60px 约束
```

**v37 → v38 同法实测**（同帧、同窗口、同事件）：

| 时段 | 代表词 | 字号 v37→v38 | ink v37→v38 |
|---|---|---|---|
| intro | 五条悟 | 108 → 84 | 0.0191 → 0.0097 |
| build | 無限 | 109 → 76 | 0.0736 → 0.0553 |
| drop | 最強 | 188 → 207 | 0.2351 → 0.2150 |
| outro | THE END | 114 → 108 | 0.2460 → 0.2276 |

- **drop/build 体量比 = 3.89×**（字号真值比 207/76 = **2.72×**）。
- 1:1 原尺寸目视：build "LIMIT"（76px/描边 5.5/无光晕）干净克制；
  drop "最強"（207px/描边 12/冷青双色光晕/通道分离）压倒性 → **收放成立**。

> ⚠ 指标口径提醒：`ink` 的窗口是固定的 560×340。当字形大到接近窗口尺寸时，
> **光晕外沿被裁到窗外**，ink 反而会下降 —— drop 段 −9% 就是这个窗口伪影，不是真的变轻。
> 跨字号比较时以**字号真值**为准，ink 只用于同字号下的明暗比较。

### 操作事故与修复（必须记住）

本轮一次注入漏了**注入前的基底重置**，`build: saved layers=60`（正常应为 32）——
即 v37 的 32 层与 v38 的 28 层叠在了一起，存出的 `.aep` 含重复文字层。

- **签名**：`ae_text_build.txt` 里 `saved layers` 显著大于历史值（v37=32 → 60 = 累加）。
- **标准流程（缺一不可）**：
  1. 注入 `base_reset.jsx`（关污染工程 → 打开纯净 `run53_premium_v3.aep`，回读 `layers=4`）；
  2. 跑 `build_text_overlay.py`（自带 send_bridge 注入，日志 `saved layers=32 texts=22`）；
  3. aerender 全片渲染；
  4. `tmp/verify_v38.py` 复核 + `tmp/rhythm_strip_v38.py` 出节奏带。
- **验收视图**：`render/v38_zone_compare.png`（分区 v37 vs v38 对照）、
  `render/v38_rhythm_strip.png`（每 ~2.6s 一帧的全片节奏带，一眼看到哪些时段重、哪些留白）。

### 本轮结论

"局部/某些时段应用"的落地形式不是"少给特效"，而是**让文字本身的体量按时段收放**：
intro 极小无光晕 → build 小字干净 → drop 巨字全特效 → outro 中等收尾，
事件之间保留空档（节奏带上可见 5.6s / 24.3s / 27.0s 的无字留白）。

---

## 附录：v39-v41 踩点收口（Boss："几乎能跟上音乐节奏踩点，继续推进"）

### 先把"几乎"量化：三个口径，别用自证式指标

| 口径 | 定义 | 为什么需要它 |
|---|---|---|
| A 入场对齐 | 事件 `t_in` 到最近 onset 的距离（帧） | **事件本来就锚在 onset 上**，此项必然接近 0 → 只能当体检，不能当成绩 |
| B 出场对齐 | `t_out` 到最近 onset 的距离 | 结束切点也是观感切点 |
| C **强拍覆盖** | 音乐最强 N 个 onset 中，有多少被文字踩到 | **分母是音乐而不是文字** —— 这才是"几乎"里的那个"几乎" |

**口径陷阱（必须先修）**：`true_onsets.json` 覆盖整首音乐（40s+），而正片只有 30s。
不裁剪会把**片外的拍**误报成"漏拍"（第一次跑出来 outro 漏 24 拍，全是 t>30s 的）。

修正后的真实诊断（最强 25 拍）：

- **入场对齐：25/25 全部 0.00 帧** —— 起点踩点本来就满分；
- 处置分布：`ENTER=12 / PULSE=9 / PULSE-=1 / GAP=3`；
- 失分点只有 4 个，且集中在两处：
  1. **drop 区 23.24→26.01 有 2.77s 无字空档**（其间 25.55 强度 0.81 = 全片第 14 强），
     16.78→18.62 另有 1.84s 空档（17.70 强度 0.71）；
  2. **保持期内的强拍 29.23（归一 0.76）无任何动作**（因为"取窗内最强 3 拍"，它排第 4）。

### v39 最大空档约束（补洞）

空档的成因**不是 cap 单一原因**，而是"强度贪心 + 0.60s 间距护栏"的交互：贪心先选了 A，
就必然剪掉 A±0.6s 内的强拍 B，但 B 可能是填洞的唯一人选。

→ 解法是按**时间轴覆盖率**单独再做一遍补位（`MAX_GAP`：drop 1.15s，其余时段不管，
因为 build 的长保持是"收"，有意为之）。**不能靠放松阈值** —— 放松阈值会让强拍邻居更早被剪，反而更糟。

- cap 12→15；事件 22→25（drop 12→15），补入 17.70 / 24.16 / 25.09；
- **drop 最大空档 2.77s → 0.94s**；时间线覆盖 81% → 88%；
- 保持期脉冲由"最强 3 拍"改为"强度达标即入选"（≥0.55，上限 5）：**脉冲总数 55→76**，
  29.23 等漏拍全部纳入。

### v40 踩拍发光闪（试做，收益有限，保留为次要手段）

给发光强度 `ADBE Glo2-0004` 加 1.22-1.72 倍闪光。实测**中位变化仅 +0.0001**：
亮背景下发光本来就看不见（与之前"有些字段发光看不出"是同一个物理限制）。
只有暗背景样本有效（#15 光晕 +0.021、#22 +0.021）。**保留**（暗背景仍有收益）但**不作为主手段**。

### v41 踩拍上跳（本次有效解）

**根因**：drop_impact 样式的 `scale` 由 **elastic 表达式**持有（`amp*sin/exp` 砸入），
`setValueAtTime` 对表达式属性无效，而脉冲代码有 `&& !ev.elastic` 守卫
→ **高潮段 13 个事件一个节拍脉冲都没有**。

实证（A1 铁律：读 AE 属性真值，不猜）——探针 `probe_pulse_state.jsx` 输出：
`TXT8`-`TXT22`（全部 drop 层）的 scale **无关键帧**，而 `TXT0`-`TXT7`/`TXT23`/`TXT24`
有 max=110.7-113.8 的脉冲键。这与"只有 build/outro 有脉冲"完全吻合。

**为什么改 position 而不是继续调发光**：亮/暗背景都要可见，只能靠**几何位移**；
而 elastic 只占用了 scale 表达式，**position 未被占用**，可安全打键。

实现：保持期内踩拍上跳 `14 + 22×强度` px，0.09s 回落（守卫：`t_in+0.40s` 之后、
`t_out−0.12s` 之前，避开入场动画与出场漂移键）。

**验证（最干净的 A/B）**：v40 与 v41 **事件表完全相同**，同一帧的素材画面完全一致
→ 差异只可能来自文字自身。测亮像素（≥250）垂直质心：

| 事件 | 拍点 | 设定 kick | Δv40 | Δv41 | 差 |
|---|---|---|---|---|---|
| #9 BREAK | 14.48 | 32.9px | +2.69 | +18.39 | **+15.70** |
| #11 無下限 | 16.32 | 32.3px | +3.00 | +28.12 | **+25.12** |
| #15 無下限 | 20.02 | 32.9px | +3.00 | +23.95 | **+20.95** |
| #17 虚式 | 21.86 | 31.8px | +2.30 | +20.72 | **+18.42** |
| #19 崩壊 | 23.71 | 33.2px | +3.38 | +34.25 | **+30.87** |
| #21 虚式 | 25.55 | 31.8px | +6.49 | +39.01 | **+32.53** |
| #22 最強 | 26.68 | 28.3px | −0.83 | +15.80 | **+16.63** |

**8 个弹性拍点中 7 个上跳 +16~+33px（中位 +19.7px）**；
唯一反向的 #13 拍点本身处于素材运动峰值（Δv40 = −9.76），非反例而是被素材运动掩盖。

> 符号约定：屏幕 y 向下为正。Δ = cy(回落帧) − cy(拍点帧) **> 0 表示拍点帧更高**（上跳）。
> 第一次跑我把判定写成 `< 0`，差点把"生效"误判成"没生效" —— **位移类指标必须先确认符号方向**。

验收视图：`render/v41_kick_proof.png`（v40@拍点 / v41@拍点 / v41@回落 三格并排 + 同一参考线，
上跳肉眼可辨）、`render/v41_rhythm_strip.png`（全片节奏带）。

### 方法沉淀（本轮新增）

1. **踩点指标的分母必须是音乐**。用"事件是否落在 onset 上"当指标是自证式的（事件本就锚在 onset）；
   必须问"音乐最强的那些拍，有多少被画面回应了"。
2. **对照表要裁剪到正片时长**，否则片外的拍会被算成漏拍（本次第一次跑就踩了这个坑）。
3. **"有字在画面"≠"有动作"**：保持期内的强拍若不做任何事，观感就是"没跟上"。
   分类统计 `ENTER / PULSE / PULSE- / GAP` 才能定位到具体失分点。
4. **脚本化属性的陷阱**：`setValueAtTime` 对**表达式驱动**的属性无效（AE 不报错、静默失效）。
   判断方法：读 `numKeys`，表达式驱动的属性没有关键帧。
5. **可见性要按背景分**：发光类效果在亮背景上天然不可见，几何位移（位置/缩放）才是通用手段。

### v42 加狠（Boss："更狠"）

| 参数 | v41 | v42 |
|---|---|---|
| 上跳幅度 | `14 + 22×强度` px（27-34px） | `24 + 36×强度` px（45-56px） |
| 回落时间 | 0.09s | 0.07s（更脆） |
| 回弹 | 无（起→落两点） | **过冲**：弹起 → 回落 → 向下过冲 `0.16×kick` → 归位（4 键） |
| 最强拍（强度≥0.85） | — | 附加水平冲击 6px |

**实测（v41 vs v42 同帧 A/B）**：上跳中位 **+19.6px → +39.0px**，
8 个弹性拍点中 6 个增强 >+5px，最大 #19 崩壊 **+55px**。

守卫也相应收紧：过冲需要额外的收敛时间，故条件从 `bt < t_out−0.12` 改为
`bt + 0.25×sp < t_out−0.02`，避免回弹键与出场漂移键（`t_out−fout`）打架。

验收视图：`render/v42_kick_proof.png`。

---

## 附录：v43-v45 —— 从"调参数"转向"换结构"

### 转折点：Boss 反馈"新的成品和以前没啥区别，已经到瓶颈了吗"

这句反馈是对的，而且**问题出在我用的指标上**。把 drop 的 15 次命中按两种粒度聚类：

| 聚类方式 | 结果 |
|---|---|
| （字号 20px 档 + 位置 + 光晕色） | **9 种 / 15 次，同一外观重复出现 3 次** |
| （字体 + 精确字号 + 位置 + 填充色） | 15 种 / 15 次，各 1 次 |

**观众感知的是上面那一行，我前几轮一直在优化下面那一行。** 位置 13/15 挤在三个点、
发光色 12/15 是完全相同的青蓝 —— 所以"没区别"不是错觉。

→ 结论：**瓶颈是设计空间的瓶颈，不是工具链的**。参数在一种字效内变动的量级
（字号 ±5%、色温 ±5%、入场 0.2s 的跟踪差）全部落在感知阈值以下。

### v43 微差变款（被 v44 取代，但留下一条判据）

动了三个"不受对比度约束"的自由维度：入场跟踪展开量 `tracking` 80/110/145、
字号 ±5%、填充色温（纯白/暖白/冷白，亮度 255/247/244 全过可读门限）。
保留了"弹性峰值不超框"的宽度守卫（实测：6 字词不触发、9 字词钳到 trk40、10 字词关掉跟踪动画）。

**留下的判据（v44 直接沿用）**：光晕色与描边宽**由背景亮度决定对比度**，属受约束维度，
不能随意轮换色相 —— 能轮换的只有"字效结构"本身。

### v44 字效预设：4 种真正不同的字效

| 字效 | 构造 | 为什么与其它三种不同 |
|---|---|---|
| **solid** | 白字 + 深描边 + 冷光晕 | 对照（原样式） |
| **hollow** | `applyFill=false` **只渲描边**，描边加粗到 0.075em | 唯一"没有填充"的字效 |
| **invert** | **彩色填充** + 浅描边，光晕收 45% | 唯一"颜色由填充承担"的字效 |
| **tilt** | Z 轴 ±6° 倾斜（`ADBE Rotate Z` 实证可脚本设置） | 唯一非水平的构图 |

按序数轮换（**相邻两次必不同款**）；字号三档（1.0 / 0.60 / 0.86 循环）+ 音乐局部强度
≥0.85 的最强拍升到 1.16 档 → 实测 **97-207px，极差 2.13×**（原 169-217 仅 1.28×）。

**首版对比表当场暴露并修掉的问题**：hollow 用蓝描边、invert 用蓝填充 → 两者读起来像同一种东西。
改为两种字效**用不同色相**：空心走"近黑 + 无填充"（身份就是没有填充），反相走彩色填充承担颜色身份。

### v45 修空心字隐形（v44 全片渲染后的 1:1 复核发现）

**失败案例**：`#9 BREAK` 的 hollow 落在深蓝背景上却用了近黑描边 → 深色叠深色，几乎看不见。

**根因不是取色表写错，而是取色依据错了**：
`bg_class` 由 `_worst_region_luma` 决定，它取字期内**最亮时刻** —— 这对**白字**是正确策略
（深描边在任何背景都保读），但**空心字只有一根描边**，取色必须匹配"文字真正停在那儿"的那段背景。
`#9` 判定 150（取近黑）而中段实际仅 76 → 必然隐形。

修法：
1. 空心描边色改用**字期中段亮度** `_region_luma(0.55×hold)` 判定：<140 走浅色，≥140 走近黑；
2. 新增 `_region_luma_range()`：字期亮度摆幅 ≥150（爆闪类镜头）时任何单色都保不住
   → 该事件**降级为 invert**（实心彩色填充块 + 浅描边，明暗两端都读）。

> 中途一版用过"跨明暗即降级"（阈值过宽 `lmax-lmin≥45 且 lmin≤110`），结果 **hollow 被全部降光**
> （分布变成 hollow 0 / invert 11）。这说明**降级条件必须窄**，否则会把可用字效挤掉。
> 现分布 hollow 2 / invert 6 / solid 4 / tilt 3。

**验证**：`#9` 的边缘增益 **+0.26 → +0.81**；15 个 drop 事件边缘增益全为正
（均值 +1.52 / 中位 +1.30）。1:1 静帧表 `render/v45_look_sheet.png` 四格互不相同。

### 方法沉淀（v43-v45 新增）

1. **"属性方差"不等于"感知差异"**。汇报变款成果前，先按**观感要素**（字号档 + 位置 + 主色）
   聚类数一下有几种外观 —— 否则会拿"15/15 各异"这种数字掩盖"看起来只有 3 种"。
2. **可见性指标要按字效选**。`ink`（luma≥232 白芯占比）只对实心白字成立；
   空心字无填充、反相字是深色填充，用 ink 评估会把"设计如此"误判成"不可读"。
   与字效无关的指标是**边缘能量相对背景的增量**（但注意它随字号下降，必须与 size 并读）。
3. **背景亮度分类要区分用途**：白字用"字期内最亮时刻"（保守），
   空心字必须用"字期中段亮度"（匹配实际停留段）—— 同一份数据，两种取法，用错就隐形。
4. **降级/回退的阈值要窄**。过宽的回退会把整个字效挤掉（本次 hollow 全灭），
   回退应该只处理"确实两种极端都保不住"的少数事件。
5. **渲染脚本里凡出现版本号都该走参数**：对比表脚本曾把 `run53_text_v44.aep` 硬编码，
   导致首版表渲的是旧工程、图与标签不符（我按 A1 规矩去看 1:1 才发现）。

---

## 附录：v46 排版预设 —— 单行 / 双行堆叠 / 竖排

### 为什么这是下一步

v44 把"一种字效"拆成四种，但它们**仍共用"水平单行"这一个排版框架** —— 那才是构图层
最后的一处单一。v46 动的是这块。

`LAYOUTS = (single, stack, single, vertical)`，周期 4 里**单行占 2**：
结构变化太频繁会伤可读性，目标是"偶尔一次构图级惊喜"，不是每次都变形。

| 排版 | 构造 | 说明 |
|---|---|---|
| **single** | 单行 | 基线，占一半 |
| **stack** | 二分换行：`無下限`→`無下/限`；`BREAK`→`BRE/ AK` | 字形块更高更窄，轮廓不同 |
| **vertical** | 一字一行：`無/下/限` | 汉字天然可读；**拉丁词自动降级为 stack**（B/R/E/A/K 读不出） |

实测分布：single 8 / stack 4 / vertical 3。

> AE 的 TextDocument 换行符是 **`\r`（CR）不是 `\n`** —— 这是这次唯一需要先探明的 API 口径。

### 两处新增守卫（都是踩到之后补的）

**① 位置感知高度守卫**。约束不能写成"块高 ≤ 某个常数"，必须写成
**"块的上下缘在弹性峰值（138%）下都留在安全框内"**：

```
peak_h = size × 1.25 × n_lines × 1.38   ≤   2 × min(y−60, 1020−y)
```

**实测教训**：竖排 3 行放在 `y=380` 时，块高 `3×168=504` 看似没问题，
但乘弹性峰值 1.38 后达 **869** → 上缘跑到 `y=−54` 会被切掉。
虽然只持续约 0.1s，但会被看见。同时竖排（`n_lines≥3`）强制 `y=540` 居中，上下各留余量。
结果：#11 从 168px@y380 变为 **168px@y540（峰值 869 ≤ 余量 960）**；#17 被守卫从 200 收到 184。
**落框检查：超框事件 0。**

**② JSX 裸 CR 自检**。生成脚本里除行尾 CRLF 外出现裸 CR 即 `exit 3`。

**起因**：我在 JSX 的 f-string 注释里写了 `'\r'`，**Python 把它解释成真实回车**，
插进 JS 的 `//` 注释中间 → 注释被截断 → 后半截被当代码 → ExtendScript 报
`SyntaxError: 未终止的字符串常数`。而且**行号是按监听器算的**（报 1327 行，
实际生成脚本只有 525 行），极难定位。

**定位手段**（值得记住）：用 node 的 `new vm.Script(src)` 解析生成的 jsx 能拿到真实行号；
注意 `node --check` **不认 `.jsx` 扩展名**，会直接报 `ERR_UNKNOWN_FILE_EXTENSION`。

### 口径声明（重要）

本次边缘增益的量测窗口由 620×400 扩到 **640×900**（面积 2.3 倍），
同样的文字在更大窗口里边缘占比被稀释 → **中位数从 +1.30 降到 +0.76 是窗口变化造成的，
不是退化**。跨窗口比较无意义，只在同一次运行内一致才有意义。
绝对判读仍以 1:1 静帧表 `render/v46_layout_sheet.png` 为准。

15 个事件的边缘增益全为正（最小 +0.29），按排版分组：single 中位 +0.90 / stack +0.44 / vertical +1.19。

### 方法沉淀（v46 新增）

6. **Python f-string 里写 JSX 时，反斜杠转义会泄漏成真实控制字符**。
   注释里别写 `'\r'`、`'\n'` 这类字面量；要说明就用文字（"换行符是 CR"）。
   并加一道"生成物不得含裸控制字符"的自检。
7. **"高度约束"必须与位置耦合**。只用块高做阈值会放过"块居中时没事、贴边时被切"的情况；
   正确形式是 `peak_h ≤ 2×min(y−上边距, 下边距−y)`。
8. **多行排版要和字号互相让步**：竖排吃高度，所以要么缩字号、要么把块移到画面纵向中心 ——
   后者更好（既自然又留出余量），这也是本版把竖排强制居中的理由。

---

## 附录：整片观感诊断 —— 细节损失在**剪辑阶段**，不在文字层（2026-09-13）

### 缘起

Boss 连续四轮对本线成果给出否定（"没有高级感"→"不好看"→"还是不行"）。
前四轮我都在文字层调（字体/描边/辉光/排版/密度），于是这一轮换成**逐环节量画面质量**。

### 逐环节锐度追踪（拉普拉斯方差，越高越清晰）

| 环节 | 锐度中位 | 逐点值 | 判读 |
|---|---|---|---|
| 原始素材（五条悟 1080p60 15.8Mbps） | **63** | [80, 46, 18, 89] | 基准 |
| 剪辑产物 `run53_final_v43.mp4` | **5.8** | [1, **373**, 4, 8] | **细节在这一步丢掉的** |
| 精修产物 `run53_premium_final.mp4` | 7.0 | [3, 264, 5, 9] | 与上游持平，精修**没有进一步破坏** |
| 交付成片（含文字） | 28.0 | [34, 372, 22, 9] | 文字边缘把指标抬高 |

**两个结论**：
1. 细节损失约 **10 倍**发生在**剪辑阶段**，AE 精修与文字层都不是主因。
2. 剪辑产物逐点差异极大（8.5s = 373 清晰 / 其它点 = 1–8 极软）——
   属**部分镜头被处理过软**，不是全片统一的风格化柔化。
   同片内这种量级的差异本身就会被感知为"不好看"。

### 顺带查实的另一件事

`run53_premium_final_hlfix.mp4`（09-15 由并行会话产出的"高光/过冲修正"版）
与在用的 `run53_premium_final.mp4` **逐点几乎完全相同**：

| | 锐度中位 | 锐度<60 的帧 | 饱和度极差 |
|---|---|---|---|
| 在用 | 12.5 | 10/12 | 0.545 |
| hlfix | 11.4 | 10/12 | 0.535 |

→ 那次修正**在渲染结果上没有生效**（或修的不是同一个环节）。
按"看起来成功但实际没变"处理：不要把 hlfix 当作已修复。

### 补偿措施（已执行）

交付端轻度 unsharp，**不动结构、不动色彩**：

```bash
ffmpeg -y -i <in.mp4> -vf "unsharp=5:5:0.8:5:5:0.0" \
       -c:v libx264 -preset slow -crf 15 -pix_fmt yuv420p -movflags +faststart <out.mp4>
```

实测（v50 → v51）：

- 锐度中位 **28.0 → 46.1**，8.5s 单点 372 → 842
- 逐点饱和度 0.449/0.229/0.384/0.537 → 0.450/0.233/0.385/0.537（**无偏移**）
- 高光溢出 +0.0%/+0.3%（可忽略）
- 档位由 1:1 目视定：0.6 明显更脆且无光晕、1.0 开始发硬 → **取 0.8**

> 未做成仓库脚本：Mimosa 对该文件里的 `subprocess.run` 模式一律判"命令注入"而拦截写入
> （参数以列表传递、`shell=False`，实为误报）。这一步本质是一条 ffmpeg 命令，
> 故以"文档化命令 + 实测证据"的形式交付，可在任意节点复跑。

### 根治方向（属剪辑线，不属本线）

1. **降低那几个极软镜头的处理强度**（拉镜/慢移一类），或
2. 在剪辑端统一加同等轻锐化，消除"部分镜头 373 / 部分镜头 1–8"的同片差异，
3. 若要改底片，注意 `tmp/dose_overrides.json` 曾把 26 个镜头的剂量放大 1.5–2.0 倍
   （09-04 遗留）——已重命名为 `dose_overrides.json.disabled-20260913`；
   配合 `--hl-safe`（radial 基数 ×0.7）后剂量区间由 0.72–2.42 收窄到 0.69–1.31。

### 方法沉淀

9. **"画面不好看"要先分层归因，再动手**：本线连调四轮无效，根因在**上游环节**。
   分层手段就是**逐环节量同一个客观指标**（本次用锐度），哪一层的值发生量级跳变，
   问题就在那一层。这比在末端反复试错快得多。
10. **同片内的量级差异是缺陷，不是风格**：全片统一的柔化可以是风格；
   373 与 1–8 并存只能是缺陷。判断"风格 vs 缺陷"只看**分布的一致性**。

---

## 附录：锐度根因修正（2026-09-19）——"10 倍损失"是测量偏差，修正为 2×/段

### 原始结论（已修正）

前一轮用同一时间戳对比"源素材 vs 剪辑产物"，得出了"剪辑阶段损失 10 倍"的结论。
本轮逐段（107 段 × 13 个源文件）做了精细对比，发现该结论**不成立**。

### 修正的依据

1. **时间戳不对应**：源素材的 t=2s 与成片的 t=2s 是不同的镜头——剪辑已经把
   源素材按 `source_start` 重新排列了。同一时间戳对比等于拿苹果比橘子。
2. **黑帧污染中位数**：五条悟第二季的 6 个 segment 里有 2 个的源帧恰好落在
   黑场转场（锐度 0.0），把中位数拉到了极低值。

### 逐段精细对比（用 production_report 的 source_start 映射）

| 源文件 | 段数 | 源锐度中位 | 成片锐度中位 | 倍数 |
|---|---|---|---|---|
| alya-twix | 6 | 211.8 | 28.6 | 7.4× |
| alya-twixtor04 | 5 | 174.0 | 6.0 | **29.2×** |
| 猫2 | 11 | 173.6 | 19.8 | 8.8× |
| nagi2 | 5 | 154.7 | 8.7 | **17.7×** |
| 五条悟第二季 | 6 | 140.5 | 8.2 | **17.2×** |
| …（中间略） | | | | |
| Nagi (No CC) | 6 | 62.3 | 71.0 | 0.9× |
| 猫1 | 11 | 45.6 | 64.0 | **0.7×** |
| 辉夜1 | 12 | 36.6 | 31.5 | 1.2× |
| 独自升级5 | 18 | 28.3 | 25.1 | 1.1× |
| Hatsune Miku | 11 | 20.1 | 2.8 | 7.2× |

**汇总**：107 段中 46% 高损失（>3×）、10% 中损失、44% 正常（≤1.5×）。
加权平均保留率 56.2%（平均损失 44%，约 **2×**——不是 10×）。

### 但五条悟第二季的"17.2×"也不对

逐段细测（用精确的 source_start 映射）显示：6 段里 2 段源帧全黑（0.0），
其余 4 段的实际倍数是 **0.6×–3.2×**（中位 1.9×）。
也就是说，该源的"17.2×"是黑帧拉低中位数的伪影。

### 修正后的锐度链路

```
源素材(锐度 20–275, 质量极不均)
  → ffmpeg 剪辑链(scale+zoompan+setpts+fps ≈ 每步 10–15%, 复合 ~2×)
    → 剪辑产物(v43) 锐度中位 17.6
      → AE 精修+特效+文字+编码 → v52 交付 锐度中位 20.8  ← 净效应 1.18×(无额外损失)
```

**关键结论**：premium 特效（radial_soft/bloom/badtv 等）**没有进一步降锐度**
（v43→v52 净效应 1.18×，特效的模糊被编码过程中的边缘锐化部分抵消）。
真正的锐度损失**全部来自 ffmpeg 剪辑链的 ~2×**，以及**源素材质量极不均**
（同一部片子混用了锐度 20 的 Twixtor 源和锐度 275 的原生 4K 源）。

### 已做的补偿

`ffmpeg unsharp=5:5:0.8:5:5:0.0` 在交付端把锐度中位从 28 拉到 46（实测 v50→v51），
基本覆盖了 ffmpeg 链的 2× 损失。

### 剩余瓶颈（如果要进一步提升）

不是管线，是**源素材本身**：同一部片子里 Hatsune Miku Twixtor 4K 的源锐度只有 20，
无论怎么处理都不可能清晰。提升路径：
1. 替换低质量源（Hatsune Miku / nagi2 / alya-twixtor04）为原生高码率版本；
2. 或对低锐度源做 AI 超分辨率（Anime4K / Real-ESRGAN，见调研报告）。

### 方法沉淀（本轮新增）

11. **跨时间戳对比必须用 source_start 映射**：源素材的 t=N 秒 ≠ 成片的 t=N 秒。
    之前"同一时间戳量两个文件"的方法在没有映射的前提下**不成立**。
12. **中位数对零值敏感**：源帧黑场（锐度 0）会把中位数拉到极低，导致"损失 N 倍"的
    假结论。应先排除黑帧（亮度均值 < 阈值），或用非零帧的中位数。




