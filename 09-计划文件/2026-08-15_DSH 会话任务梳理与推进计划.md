# DSH 会话任务梳理与推进计划

> 生成时间：2026-08-15（**14:30 复核同步版**）
> 数据来源：14 个 DSH 会话，142 条任务记录
> 本次同步：云端训练/标注产物查验 + 人工复核队列结果核查

---

## 一、已完成任务 ✅

### 1. 项目全面审计（2026-08-14）
- **代码审计**：`docs/research/2026-08-14-audit-core-code.md`
  - 语法层全健康，TS 编译器成熟
  - 病灶：扁平导入架构导致 3 处真实失效
- **测试审计**：`docs/research/2026-08-14-audit-tests.md`
  - 4984 条用例，抽样 4 组全绿
- **知识库审计**：`docs/research/2026-08-14-audit-knowledge.md`
  - 10-风格化：259 md（非 262）
  - 13-素材库：78 个是代码，仅 4 md → 伪知识库
- **全网调研**：6 份报告（tools/datasets/knowledge/core/tests/web）
  - 新发现：ToriiGate（动漫 VLM）、kandinsky-videomae-large（运镜分类）、MatAnyone/BiRefNet（抠像）

### 2. DSH 基础设施
- ✅ DSH v0.1 安装部署（npm 全局安装）
- ✅ Web UI 启动脚本（`启动 DSH.bat`）
- ✅ 后台服务脚本（`dsh_service.ps1`）
- ✅ 开机自启（Startup 文件夹 VBS）
- ✅ 插件安装：dsh-milestone, dsh-working-activity, dsh-email, dsh-vision-proxy 等
- ✅ 插件冲突修复（dsh-cc-tui 与 dsh-base 冲突）

### 3. 运镜分类器训练（session-ed77cca3）
- ✅ LoRA v2/v2c 训练完成（v2 本地 val 0.4027；v2c 云端 val 0.2405）
- ❌ **v2c 云端产物作废**：云端 transformers 4.46.3+torch 2.1.2 加载 VideoMAE 时注意力偏置键名不匹配，基座静默损坏 → 适配器≈零权重。**实测验证**：v2c 适配器 300 条 VLM 一致性集 acc 0.2466 == 基座 0.2466（同 8/15 实验2 结论）
- ✅ **v1 本地 LoRA 为当前生产基线**：300 条 VLM 一致性集 argmax 0.7037，逐类阈值调优后 **0.7172**（阈值已存 `models/output/thresholds_tuned.json`）
- ✅ VLM 标签噪声清洗（complex 误判纠正）
- ✅ 人工复核队列生成（74 条）→ 已全部处理（`reviewed_results.jsonl`）
- 🔄 **v3 fine-10 类**：云端首训完成（val_acc 0.4593），但 `save_pretrained` 未存重建的 10 类分类头（仅 attention LoRA）→ 本地无法评估。已修复（`modules_to_save=["classifier"]`）云端重训 v3b 中，完成自动回传
- ⚠️ 目标 >80% 未达成；v3b 回传后本地评估/验收

### 4. v23 AMV 引擎
- ✅ 提交 bf6d6b7：AE 高级运镜通道接入主引擎
- ✅ zoom_back/pulse 走 AE 贝塞尔缓动 + 运动模糊
- ✅ 失败自动回退 FFmpeg
- ⚠️ 25 个文件未提交（+1753/-940 行）

### 5. 抠像链路（session-eb358b96）
- ✅ RVM 抠像尝试
- ✅ MatAnyone 安装（CVPR 2025）
- ✅ BiRefNet 下载尝试（网络问题）
- ⚠️ 精度未达 70%，2-4 秒和 8 秒后镜头有问题

---

## 二、进行中任务 🔄

### 1. 运镜分类器精度提升（P0）
**目标**：>80% 精度，尽力逼近标注质量上限

**当前进度**：
- ✅ LoRA v1 为生产基线（粗4类阈值调优 0.7172，已接入 A5）
- ❌ v2c 云端产物作废（环境 bug 致基座静默损坏，适配器零权重，实测已确认）
- ✅ VLM 标签清洗 + 全量合并：`vlm_labels_merged.jsonl` 7612 条（云端 7192 + 本地 6166 去重，三通道 x 9 路标注完成）
- ✅ 人工复核队列 74 条已处理（`reviewed_results.jsonl`）→ 三方仲裁 `queue_lora_arbitration.json`
- ⚠️ **关键发现**：VLM 与 LK 光流一致性仅 21.6%（agree 16/74），光流规则与 VLM 大规模对照 exact 仅 14.3%（`flow_vlm_eval_report.json`，n=544）→ **光流不可作为独立复核标准**，复核队列更多反映模型分歧而非真值
- 🔄 **v3 fine-10 首训完成但产物缺分类头**（云端 val_acc 0.4593/10类，随机基线 0.1）→ 已修脚本重训 v3b（`modules_to_save=["classifier"]`），后台监控自动回传中

**下一步**：
- [ ] **v3b 回传完成** → `eval_fine_lora.py`（已修复 num_labels=10 重建头）细10类盲评 vs v1 粗4类 0.7172
- [ ] v3b 达标 → `tune_fine_thresholds.py` fine 阈值两阶段调优（已修复同款加载 bug）
- [ ] 100 条分层盲评集（`scripts/build_blind_review.py`，GIF+HTML）人工验收
- [ ] 复核队列人工目检（`make_montages.py` 已生成蒙太奇）修正标签后进精修
- [ ] A5 分类器切换 v3 权重 + fine 阈值并验收

### 2. 文字动画项目资产盘点（P1）
**目标**：梳理三层体系，设计后期特效合成方案

**当前进度**：
- ✅ 历史文档盘点完成（12 份文档）
- ✅ 代码资产盘点完成（三层体系）
- ✅ 深度方案文档：`09-计划文件/2026-08-15_后期特效合成制作深度方案.md`

**下一步**：
- [ ] M1c 真机 demo（需 AE Bridge）
- [ ] 云端评估（无需 AE，可并行）

### 3. 未提交代码清理（P1）
**问题**：25 个文件未提交（+1753/-940 行）

**当前状态**：
- ✅ 大部分已提交（`17cc10c`：28 files, +31k/-12k，含 production_director/temporal_analyzer/camera_decision 等）
- ⏳ 剩 7 个新脚本未跟踪（8/15 复核/分析链新增，未提交）：
  - `analyze_review_consistency.py`、`arbitrate_queue_lora.py`、`dig_flow_stats.py`
  - `eval_flow_vs_vlm.py`、`make_montages.py`、`process_human_review_queue.py`、`vlm_label_survey.py`

**下一步**：
- [ ] 审查后提交 7 个复核分析脚本（建议归为 1 个 commit：复核队列分析链）

### 4. 抠像精度优化（P2）
**问题**：精度未达 70%，部分时间段效果差

**✅ 已完成（8/16 混合微调完整交付）**：
- ✅ BiRefNet 混合微调：动漫 1111 + cityscapes 1067 + ADE 1132 + CelebA 伪mask 3973 = **7283 对**（数据质量审计修复后）
- ✅ best_val 0.0876（污染版 0.22 → 干净版腰斩）；权重 `finetuned_anime_mixed.pth` 设为生产引用
- ✅ 双向 A/B 验收：动漫域 IoU **0.9756**（base 0.9544）、COCO person **+1.12%** → 双向最优
- ✅ **FATE 完整链路（24帧短分段定案）**：MatAnyone 分段 → BiRefNet 补抠 → MED5 → MOV（qtrle+alpha）
  - 分辨率实验结论：768 失败（21糊块）→ **24帧短分段成功（1糊块/37缺失，剧烈段跟丢降41%）**
  - **宽松上限补抠（v17 定案）**：面积上限 0.40→0.70 再补回 22 帧（含转身段 173/180 等），残留仅 15 帧
- ✅ **AE 真机合成验证通过**：新 MOV 载入 AE → BeatLock 节拍合成 → `M3_Beat_Fate` 合成树成功（9 锚点）
- ✅ 训练集 7283 对回传 `D:\AE-Data\matting_dataset_backup\`
- 交付 MOV（v17 宽松版）：`output/one_pipeline/DL_DL_FATE_r978_BV1qb411C79B_p1_v17/DL_DL_FATE_r978_BV1qb411C79B_p1_alpha.mov`（365MB，QC PASS）
- 预览（橙底）：`tmp/FATE_preview_v17.mp4`

**遗留（已确认为物理极限，结案）**：
- 残留 15 帧：189/190（头发特写，无人物主体）+ 347-363（无人物/模糊）——BiRefNet 输出 0%，Silhouette 也无轮廓可描
- 完整 alpha 增强/guided filter 环节（_alpha_boost.py 未入 git，非必需）

---

## 三、待启动任务 📋

### 1. 数据集建设
- [x] **动漫运镜自建数据集标注完成**（D:\AE-Data\AnimeCamera\ → 云端三通道 9 路标注，合并 7612 条 `vlm_labels_merged.jsonl`，含 high-conf 6764 条 ≥0.85）
- [ ] AnimeShooter 数据集下载（external/animeshooter/）
- [ ] 公开数据集利用（算力云界面，25 个已挂载，见 `docs/research/2026-08-15-cloud-datasets-integration.md`）

### 2. 模型集成
- [ ] VideoMAE-MovieShots 运镜模型集成（P0，v1 粗4类 71.7% 已接入；v3 fine-10 训练中→回传后切换）
- [ ] ToriiGate 动漫 VLM 集成（情绪识别）
- [ ] BeatNetLite+SongFormer 卡点升级

### 3. 知识库优化
- [ ] 13-素材库重构（78 个代码文件需整理）
- [ ] 机器可消费性提升（YAML frontmatter）
- [ ] 知识断层修复（207 个风格文件未被管线读取）

### 4. 工具链升级
- [ ] Adobe Firefly DGR API 集成（模板渲染上云）
- [ ] Python-UIAutomation 替代截图点击
- [ ] yt-dlp 版本锁定（规避 B 站下载回归）

---

## 四、资源与配置

### API Key 配置
- 千问 AI Key：`sk-ws-H.EEYYIHL.eYk7...`（有免费额度）
- 百炼控制台 Key：`sk-ws-H.EEYYRIX.3Irh...`
- 火山方舟：`ark-***已清除***`
- 硅基流动：`sk-sp-H.DDXMYY.hD3Z...`（有余额）

### 算力云
- SSH 1：`ssh -p 19811 root@connect.westb.seetacloud.com`
- SSH 2：`ssh -p 49339 root@connect.westb.seetacloud.com`
- 云 GPU：两台已开启

### 模型下载
- ToriiGate：`D:\AE-Data\Models\ToriiGate\`
- VideoMAE：`D:\AE-Data\Models\VideoMAE-MovieShots\`
- AnimeCamera：`D:\AE-Data\AnimeCamera\`

---

## 五、并行任务分配建议

### 主会话（当前）
- M1c 真机 demo（需 AE Bridge 单槽）
- 未提交代码清理
- 人工复核队列处理

### 对侧会话（另一 DSH 实例）
- M2 视觉评估网关（无需 AE）
- M3 BeatLock（无需 AE）
- 云端评估任务

**代码边界**：互不修改对方当日已提交文件；要改对方模块走冲突区留言板

---

## 六、关键文档索引

| 文档 | 路径 |
|------|------|
| 审计汇总 | `docs/research/2026-08-14-audit-*.md`（6 份） |
| 后期特效方案 | `09-计划文件/2026-08-15_后期特效合成制作深度方案.md` |
| AE 推进计划 | `09-计划文件/2026-08-15_AE 推进计划 - 并行同步.md` |
| 运镜数据集方案 | `docs/plans/2026-08-14-step4-anime-camera-dataset.md` |
| 集成候选 v2 | `docs/research/2026-08-14-integration-candidates-v2.md` |

---

## 七、立即行动项（Top 3）

1. **v3b 重训收尾**（已在云端运行 ~50min，后台监控 `scripts/cloud_v3b_monitor.py` 完成自动回传 `models/output/anime_camera_lora_v3`）
   ```
   # 回传后本地评估（已修复 num_labels=10 重建头）:
   py -3.12 scripts/eval_fine_lora.py --lora-dir models/output/anime_camera_lora_v3
   # 达标后 fine 阈值调优:
   py -3.12 scripts/tune_fine_thresholds.py --lora-dir models/output/anime_camera_lora_v3
   ```

2. **提交复核分析脚本 + v3 修复**（git 未跟踪项 + 本轮修改）
   ```bash
   git add scripts/analyze_review_consistency.py scripts/arbitrate_queue_lora.py \
          scripts/eval_flow_vs_vlm.py scripts/make_montages.py \
          scripts/process_human_review_queue.py scripts/vlm_label_survey.py \
          scripts/cloud_v3b_monitor.py \
          scripts/train_anime_camera_lora.py scripts/eval_fine_lora.py scripts/tune_fine_thresholds.py
   git commit -m "fix(models): v3 fine-10保存分类头(modules_to_save) + 评估脚本num_labels重建头"
   ```

3. **抠像回传权重 A/B 评估**（BiRefNet 云端微调已回传）
   - `py -3.12 scripts/eval_birefnet_coco.py` 跑原版 vs 微调后 IoU 对比
   - 达标则替换生产权重，未达标则继续分段重锚定
