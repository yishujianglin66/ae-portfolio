---
tags: [修复方案, 执行计划, 技术债, 质量治理, MasterCut]
date: 2026-09-26
status: PLAN
data_cutoff: 2026-09-26 12:00（HEAD 0a727a1；P0 关键靶点已做本轮实测复核，见各项"09-26 复验"列）
based_on:
  - 03-阶段报告/项目文档扫描与模块功能评价_2026-09-24.md（0924 审计：16 条伪造路径、矛盾 1-23、未实现声明 25 条、R1-R23、§九建议五批）
  - 03-阶段报告/项目全貌梳理与开发经验提炼_2026-09-26.md（§四 踩坑 A1-F3、§六 建议、§七 判定清单 14 条）
  - docs/handoff-2026-09-14-quality-audit-sync.md（陷阱 T1-T9、§8 未完成项、§9 各轮回写）
  - .trae/rules/evidence-gate-rules.md（证据闸门五铁律——本方案全程遵循）
  - docs/ae_bridge_lessons.md / docs/resolve_engine_lessons.md / 02-开发文档/漫剪卡点铁律*.md
---

# MasterCut 全量问题修复执行方案（2026-09-26）

> **本方案性质**：PLAN。按证据闸门⑤，任何一项在真实执行验证前不得引用为"已完成"。
> **行号纪律**（闸门④）：文中 file:line 以 **0924 审计基线 + 本轮 09-26 实测复核** 双标注；
> 两者已出现漂移的（如 `tools/unified_tool_integrator.py`），一律**按函数名重新定位后再动手**，禁止照抄行号。
> **前置纪律**：当前工作区存在 09-25/26 未提交改动（`test_section_fusion.py`、`production_director.py` 等）——
> 执行本方案任何一项前先 `git status` 确认已提交或隔离（handoff T7）。

---

## 零、一页总览

### 0.1 问题空间与合并逻辑

| 来源清单 | 原始条数 | 处置 |
|---|---|---|
| 0924 审计 §3.2b/§7.0 "伪造成功"路径 | 16 条（表列 F1-F12）+ §7.0b 六类形态 A-F（60+ 处） | 按根因合并为 **5 个"成功语义治理"动作**（FIX-01~06）+ 守护闸门包（FIX-09） |
| 0924 审计 §五 矛盾清单 | 23 条（含 1b） | 6 条与伪造路径同根因（并入 FIX-02/03/13/18/19），4 条已随 09-25/26 提交消化（§五 已消化表），其余逐条分配 |
| 0924 审计 §五之二 未实现声明 | 25 条 | 与矛盾清单高度重叠，作为对应 FIX 项的"声称出处"引用，不重复立项 |
| 0924 审计 §七 风险 | R1-R23 | 全部映射到 FIX 项（映射表见 0.3，无遗漏对账） |
| 0926 梳理 §四 踩坑（A1-F3 33 条） | 33 条 | 28 条**已有对策且已落地为代码/铁律**（档案即预防），转入"防回退守护"（FIX-07/08/17/24/47）；5 条待办型并入 FIX-10/11/26/30 |
| 0926 梳理 §七 过时/矛盾/重复 | 14 条 | 并入文档治理包（FIX-43~46）与已消化清单 |

**三大根因包（同一根因多表象的合并说明）**：
1. **"成功语义"根因包**（F1/F2/F3 + E-A/B/C + R2/R20/R21 + 矛盾 21）：根因是 07-14 `工具链集成总览.md` §7.3 `execute_with_fallback`（`if result.success: return`，real→simulate 不区分）被完整实现并扩散。所有"默认 simulate / 伪造产物 / 伪装 real / Mock 入池 / 假飞轮"都是同一蓝本的派生 → 由 FIX-01（契约）+ FIX-02~06（分域切断）+ FIX-09（扫描闸门）统一治理，**修判据而不是逐点打补丁**。
2. **"声明超前于现实"根因包**（矛盾 1/2/3/4/5/9/14/15/17/18/19/20/23 + R1/R11）：根因是**没有单一、会过期、可被 CI 校验的"当前真相"入口** → 由 FIX-15/18/19（纠偏+基线+指标）+ FIX-43（STATUS.md + 新鲜度闸门）统一收口。
3. **"强制力空心化"根因包**（矛盾 9/16 + R5/R7/R15 + 契约测试全 skip）：根因是闸门建了但都是 advisory / skip / 哑门 → 由 FIX-09/11/13/16/17/22 统一恢复强制力。

### 0.2 数字总览

| 优先级 | 条数 | 预估工作量 | 建议窗口 |
|---|---|---|---|
| **P0 切断伪造成功 + 恢复 CI 强制力** | 14 项（FIX-01~14） | **11 人天** | 第 1-2 周 |
| **P1 恢复可验证性** | 16 项（FIX-15~30） | **13 人天** | 第 3-5 周 |
| **P2 最后一公里（规则/记忆/验收真闭环）** | 8 项（FIX-31~38） | **14 人天** | 第 6-8 周（含 2 项用户决策） |
| **P3 结构债 + 文档治理** | 8 项（FIX-40~47） | **11 人天** | 第 9-12 周，部分可提前穿插 |
| **合计** | **46 项** | **≈ 49 人天** | 约 11-12 周（单人全职）/ 可并行压缩至 7-8 周 |

**并行压缩说明**：P0 内 FIX-07/08（桥协议）与 FIX-09~13（守护/强制力）文件零重叠，可双会话并行；P3 的 FIX-43~47（文档族）不碰生产代码，随时可插队。并行执行遵守 handoff T7 隔离纪律（热文件不重叠、定向提交、永远勿 `git add -A`）。

**里程碑（每条都是可客观判定的"过/不过"）**：
- **M1（第 2 周末）**：`auto_produce --dry-run` 无产物必 exit≠0；全仓 `mode="simulate"` 默认值清零；"success 必须带标记"扫描器进 CI；`full-suite-sharded` 转 required。**判据：CI 一次真实全量绿截图入档 + 扫描器命中数基线落盘。**
- **M2（第 5 周末）**：HEADLINE 指标全部可复跑复现；新基线登记 SHA；Dockerfile 可构建或已删。**判据：README/STATUS 中每个数字旁有 `reports/` 落盘出处。**
- **M3（第 8 周末）**：一条规则从 `ruleset.jsonl` 自动改变出片片语（A/B 实证）；验收 UI 产出第 1 条一手 ratings。**判据：`applied_rules[]` 日志 + `source:"ui"` 新记录。**
- **M4（第 12 周末）**：`pip install -e` 落地、shim 删除；STATUS.md 上线且 CI 校验其引用路径与新鲜度。**判据：syspath 上限测试下调且绿 + `test_status_freshness.py` 绿。**

### 0.3 R1-R23 → FIX 映射（防遗漏对账表）

| R# | → FIX | R# | → FIX | R# | → FIX |
|---|---|---|---|---|---|
| R1 指标 | FIX-19 | R9 规则读侧 | FIX-31 | R17 环境 | FIX-26 |
| R2 Mock 入池 | FIX-05 | R10 验收数据 | FIX-33 | R18 重复资产 | FIX-44 |
| R3 基线消失 | FIX-18 | R11 文档矛盾 | FIX-15/43 | R19 检索样本 | FIX-27 |
| R4 桩锁规格 | FIX-10 | R12 god 文件 | FIX-20/40 | R20 Topaz CLI | FIX-14 |
| R5 CI 零强制 | FIX-13 | R13 tmp 证据 | FIX-11 | R21 MediaPipe | FIX-03 |
| R6 服务层损坏 | FIX-21 | R14 AE 交互通道 | FIX-24 | R22 开箱即败 | FIX-14/44 |
| R7 异常吞噬 | FIX-16 | R15 源码断言 | FIX-22 | R23 桥路径分裂 | FIX-07 |
| R8 导入补丁 | FIX-40 | | | | |

矛盾 → FIX：1→18｜1b→18｜2→19｜3→19｜4→15｜5→15/43｜6→34｜7→35｜8→10｜9→13｜10→29｜11→21｜12→24｜13→已消化｜14→已消化｜15→19｜16→11｜17→28｜18→31｜19→25｜20→30｜21→02/03｜22→29｜23→30（另 .gitignore GBK→29）。
未实现声明 #1-25 全部为上述 FIX 的"声称出处"（#1→13、#2→19、#3→31、#4/5→29、#6→21、#7→21、#8→11、#9→15、#10→43、#11→31、#12→24、#13→14、#14→15、#15→26、#16→43、#17→42、#18→43、#19→40、#20→38、#21→03、#22→12/14、#23→24、#24→08、#25→40）。
0926 §七 14 条 → 15/18/19/29/34/35/42/43/45/46/47 覆盖；§四 A/B/C/D 族 28 条已落地项 → 转入守护（07/08/16/17/22/24/47），待办型 → 10/11/26/30。

---

## 一、P0 —— 切断伪造成功 + 恢复 CI 强制力（13 项，11 人天）

> 建议串行顺序即编号：FIX-01 立契约 → 02-06 分域切断 → 07-08 协议收敛 → 09-13 守护与强制力 → 14 独立裁决项。
> FIX-07/08 与 09-13 无互相依赖，可在两人/两会话并行（并行会话隔离纪律：热文件不重叠、定向提交）。

### FIX-01 成功语义契约定义（先立规矩，后动代码）｜0.5d

| 字段 | 内容 |
|---|---|
| 问题 | 六类伪造形态没有统一的"真/假执行"判别契约，逐点修必复发 |
| 来源 | 0924 审计 §7.0 处置建议；evidence-gate 闸门⑤；0926 §3.1-3 |
| 根因 | `success=True` 从不要求携带执行路径证据 |
| 影响范围 | 新建 `docs/execution_result_contract.md`；`core/exceptions.py` 增 `UntrustedSuccessError`；后续所有 FIX 的判据基准 |
| 修复动作 | ① 契约：任何返回 `success=True` 的适配器/引擎结果必须携带 `execution_path ∈ {real, simulated, fallback}`；无标记=不合法，消费端视为失败；② 契约文档引用审计 §7.0b 六类形态各配一条反例；③ simulate 产物命名规范：一律 `<output>.simulated` 后缀目录，禁止占用期望路径 |
| 验证方式 | `pytest tests/test_execution_contract_schema.py`（新增）；契约文件被 FIX-02~09 的 PR 引用 |
| 前置依赖 | 无（本包第一步） |
| 风险回退 | 纯新增，无需回退 |
| 09-26 复验 | — |

### FIX-02 顶级退出码污染切断（F1 + B 类默认入口）｜1d

| 字段 | 内容 |
|---|---|
| 问题 | 未渲染也报成功，且污染进程退出码与 HTTP 返回 |
| 来源 | 审计 F1/§3.2b①；0926 §4-E E1 |
| 根因 | 渲染步"没做也 ok:True"→AND 进顶层 success→退出码取自它；HTTP 默认 mode=auto 吃 simulate 降级 |
| 影响范围 | `ai/auto_produce.py`（09-26 实测靶点全部仍在：L217-218 random 分、L309 step6 `ok:True`、L320 AND、L360 区退出码）；`api/toolchain_api.py`（实测 7 处默认 `"auto"`：L123/144/295/397/438/467/496）；`api_server.py` L265-315（吞异常回伪造指标 `frames_processed=100...`） |
| 修复动作 | ① `step6_render.ok` 改为 `final_output.exists() and ffprobe 校验通过`；② 顶层 success 剔除未验证步；③ 退出码=所有"声明产物"真实存在；④ 随机分数改带 `score_source="random_placeholder"` 或删除演示路径；⑤ API 7 处默认 `"auto"`→`"real"`（simulate 须显式）；⑥ api_server 异常分支返回 5xx |
| 验证方式 | 新增 `tests/test_auto_produce_honest_exit.py`（mock 无产物→断言 exit≠0）；手工 `py -3.12 ai/auto_produce.py --dry-run; $LASTEXITCODE` 非 0；全量回归绿 |
| 前置依赖 | FIX-01 |
| 风险回退 | 依赖 exit 0 的旧自动化会开始报错——正是修复目的；改动集中 3 文件，单提交可 revert |
| 09-26 复验 | ✅ 全部仍在 |

### FIX-03 引擎级 simulate 默认与假产物切断（F3 + E-A + R20/R21）｜1.5d

| 字段 | 内容 |
|---|---|
| 问题 | simulate 是集成层默认值；auto 失败静默降级；降级会**在期望路径写出假产物**骗过 exists() |
| 来源 | 审计 F3/§3.3b①②③④/§7.0b A 类；矛盾 21（C4 未修）；0926 §4-E E1/E7 |
| 根因 | 同 FIX-01 契约缺失 + 默认值站在 simulate 一侧 |
| 影响范围 | 09-26 实测 7 处默认值：`integrations/topaz_integration.py:220`、`mediapipe_integration.py:152`、`blender_3d_integration.py:107`、`davinci_resolve_integration.py:155`、`adobe_suite_integration.py:81/123/162`；`silhouette/silhouette_executor.py` `_create_simulated_output()`（写 `SIMULATED_MATTE`）；`ai/ai_video_generator.py` L75 默认 simulate + L983 文本假 .mp4 50MB（F2）；`core/audio_edit_engine.py:95` 无 librosa 硬编码 `bpm=128.0`+正弦曲线（F5）；`davinci_resolve_integration.py` `or result.mode == "simulate"` 判定（行号漂移，按文本定位） |
| 修复动作 | ① 7 处默认 `"simulate"`→`"real"`，未安装显式 raise（NotImplementedError 诚实失败样板）；② simulate 产物改落 `.simulated` 目录 + 返回体带标记（FIX-01 规范）；③ 删除 auto→simulate 静默降级（只允许降到"显式声明备源"）；④ `audio_edit_engine` 无 librosa 分支返回 `available=False`；⑤ 假 .mp4 逻辑改 raise；⑥ 全库 grep `exists()` 判成功的调用方，加内容特征校验（ffprobe/magic bytes），重点 silhouette/topaz/davinci |
| 验证方式 | 新增 `tests/test_no_silent_simulate.py`（参数化 7 模块：不传 mode→断言显式失败）；`grep -rn 'mode: str = "simulate"' integrations ai` 归零；`test_ai_honest_degradation.py` 保持绿；Topaz 靶点走 FIX-14 裁决 |
| 前置依赖 | FIX-01 |
| 风险回退 | 依赖 simulate 演示的调用显式化（保留能力、去掉默认）；逐文件提交可单点 revert |
| 09-26 复验 | ✅ 7 处 + L983 + L95 全部仍在 |

### FIX-04 随机决策与假特征停用（F4/E3）｜0.5d

| 字段 | 内容 |
|---|---|
| 问题 | 随机权重参与真实融合决策；假特征返回 `available=True` |
| 来源 | 审计 F4/§3.2b③；0926 §4-E E3 |
| 根因 | 占位实现未标注，消费端无从分辨 |
| 影响范围 | `core/multimodal_fusion_hub.py`（09-26 实测：随机投影 L158/266/288/317/458 + `available=True` 7 处 L178/236/275/296/333/403/424；**新发现：L996/1004/1011 `get_fusion_hub` 重复定义三次**）；消费端 `pipeline/unified_pipeline.py:2114`（真接入，非死模块） |
| 修复动作 | ① 随机特征分支 available 全改 False + `degraded="random_projection_placeholder"`；② unified_pipeline 消费点对 available=False 走既有确定性回退；③ 删重复的 2 个 `get_fusion_hub`；④ `predict_skip_stage` 实测全仓 0 命中（审计后已消失）——档案记录防复活，不立项 |
| 验证方式 | `tests/test_multimodal_fusion_hub.py` 断言更新；该文件 grep `available = True` 归零或逐处有真实实现注释；pipeline 相关既有测试绿 |
| 前置依赖 | FIX-01 |
| 风险回退 | 暴露为显式降级而非错数据；revert 单文件 |
| 09-26 复验 | ✅ 仍在；⚠️ predict_skip_stage 已不存在 |

### FIX-05 Mock AIGC 入池封堵（F6/R2）｜0.5d

| 字段 | 内容 |
|---|---|
| 问题 | Mock 恒可用 + 蓝色占位片可被剪进成片并报告 `aigc_supplemented: True` |
| 来源 | 审计发现 3/F6；0926 §4-E |
| 根因 | 消费端只判 success 不判 source |
| 影响范围 | `ai/aigc_generator.py`（L1109/1197 区，重定位）；`ai/ai_director.py:1149-1151`（09-26 实测仍是 `if r.get("success") and r.get("path")`，L1163 以文件名猜 source） |
| 修复动作 | ① Mock 返回 `success=False, execution_path="simulated"`（或 raise）；② 入池白名单 `execution_path=="real"`；③ `aigc_supplemented` 改结构化字段统计 |
| 验证方式 | 扩展 `tests/test_aigc_generator_safety.py`：全 Mock 场景断言 `material_files` 无占位片；真实 run 报告字段核验 |
| 前置依赖 | FIX-01 |
| 风险回退 | 演示流程改为显式失败——预期行为；revert 两文件 |
| 09-26 复验 | ✅ 仍在 |

### FIX-06 伪装 real 与训练产物诚实化（E-C + F7/F8/F9/F10/F11）｜1.5d

| 字段 | 内容 |
|---|---|
| 问题 | "报告了从未发生的进展"一族五例 |
| 来源 | 审计 §7.0b C 类 + F7 假飞轮/F8 注册表脱钩/F9 进化仿真输入/F10 随机噪声/F11 名不符实 |
| 根因 | 状态字段（mode_used/status/registry entry）与真实行为脱钩 |
| 影响范围 | `tools/unified_tool_integrator.py`（⚠️ **审计写 integrations/ 系路径失真，实测在 tools/**；`_resolve_mode`/`_execute_real` ×9 组实测存在；"内部转 simulate 不改 mode_used"点位按审计 1198-1231/1400-1403/1788-1791/1820-1822 需按函数名重验；`_run_simulate` 兜底 L3116-3118 实测 mode_used 已如实为 simulate——需复核剩余伪装点）；`ai/t24_flywheel.py` L143-192；`models/model_registry.json`（09-26 实测 `clip_lora_aot.pt` 341.99MB 标 trained 而磁盘 False；`clip_lora_aot_v2/clip_lora_balanced_v1/yolov8x` 同伪）；`ai/t25_model_registry.py`；`core/evolution/run_evolution.py` `_simulate_pipeline_run`（实测 L290/337 仍在）；训练噪声 8 处（t7:85-88、t12_train:163、t21:143-145、t23:84-86、t23b:85-87、t24:98、t32:246、ip_proto_classifier:166）；`ai/t34/t21/t12` 三件名实不符；`models/training/lora_trainer.py:265-292` |
| 修复动作 | ① `_execute_real` 内转 simulate 必须同步 `mode_used="simulate"`+`fallback_reason`；PR/PS subprocess 非 0 raise+校验输出；② flywheel 无训练调用不得写 `complete`，输出改"仅推理，无参数更新"；③ registry 重生成：`status=="trained"` 必须 `path.exists()`+SHA1，缺失 4 项标 `missing_artifact`；④ run_evolution 默认仿真报告头部强制 `"input_mode":"simulated"`；⑤ 噪声兜底改 raise/skip+计数；⑥ t12 不得保存未见数据的 state_dict（删产物+脚本降 PLAN 标注）；⑦ t34/t21 文件名或 docstring 如实改名（annotate only / 蒸馏未生效声明） |
| 验证方式 | 新增 `scripts/check_model_registry.py` 进 CI（trained→exists+size 匹配）；flywheel 干跑输出含 simulated 声明；`ruff`/全量绿 |
| 前置依赖 | FIX-01 |
| 风险回退 | "已训练"清单变短是纠偏；旧 registry 存 `.legacy` 便于对照 |
| 09-26 复验 | ✅ F8 实锤；F9/F7 在；⚠️ C 类行号漂移 |

### FIX-07 桥目录单一真相源（E-9/R23——PR/PS/ME/AU"结构性不可能"）｜1d

| 字段 | 内容 |
|---|---|
| 问题 | Python 与 JSX 读写**不同目录**，全仓 10 处桥目录约定互相冲突 |
| 来源 | 审计 §7.0b E 类（四桥目录零 command 文件、日志零命中为硬证据）；0926 §4-E |
| 根因 | 桥目录路径无单一真相源，各客户端/listener 各写各的 |
| 影响范围 | `core/paths.py`；`bridges/*`（含 `adobe_universal_bridge.py:141`）；`ae/ae_mcp_client.py:77` 与 `ae/ae_command_client.py`（双胞胎且互不一致）；`premiere_mcp_client`（%TEMP%）、`engine_task_dispatcher.py:613`；根目录各 `*_mcp_listener.jsx` 硬编码 root |
| 修复动作 | ① `core/paths.py` 定义 `bridge_dir(app)` 唯一实现（约定 `<repo_root>/.<app>-mcp-bridge/`），所有 Python 客户端改 import；② JSX 侧 root 与之一致并加"改必须双端同步"注释；③ 双胞胎客户端合并为一（另一改薄转发+DeprecationWarning）；④ AU 通道官方自认 deprecated——在 §五 已消化表登记，不再投入 |
| 验证方式 | 新增闸门 `tests/test_bridge_path_consistency.py`：静态解析"所有 Python 写路径 == 所有 JSX 读路径"；`scripts/ae_channel_status.py` 前后对照；PR 通道 ping-pong 一次（依赖 Q6） |
| 前置依赖 | 与 FIX-08 同批做效率最高 |
| 风险回退 | 历史遗留命令文件失效无碍（审计证四目录零命令文件）；每应用一提交 |
| 09-26 复验 | ✅ 分裂事实成立（`.ae-mcp-bridge/*.stale_*.bak` 为旁证） |

### FIX-08 命令词汇表对齐与监听器唯一化（E-D）｜0.5d

| 字段 | 内容 |
|---|---|
| 问题 | 客户端产出的命令名不在 listener 22 条表内→必然 Unknown；7 个监听器无锁抢读同一 `ae_command.json` |
| 来源 | 审计 §7.0b D 类；0926 §4-A5/E；档案 §2 |
| 根因 | 双词汇表演化分叉；正本 listener 未唯一化 |
| 影响范围 | `ae_command_generator.py`（7 个不匹配命令：placeFootageInComp/applyEffect/setLayerKeyframe/renderComposition/setTrackMatte/setParent/e2eMusicVideo）；`ae_mcp_auto_listener.jsx`（22 条表 L1355-1422）；`ae_agent_pipeline.py:4925`（发 renderComposition） |
| 修复动作 | ① 命令词汇表提为共享 JSON 单一真相源（listener 与生成器同源）；② 7 命令：生成器改为只产表内命令（推荐，遵循"22 条已实测验证"原则）或 listener 补实现——**逐命令二选一，记入盘点报告**；③ listener 正本唯一化（09-24 双协议统一版），其余副本文档标 retired 并从启动链移除；④ `runScript` 残留载荷发出方未定位 → 信息缺口 Q4 |
| 验证方式 | CI 静态门：生成器命令集合 ⊆ listener 表；`scripts/ae_smoke.py`（FIX-24）跑通 |
| 前置依赖 | FIX-07 |
| 风险回退 | 只收词汇不改行为，revert 即可 |
| 09-26 复验 | ⚠️ 09-24 双协议统一后部分症状或已缓解，动手前重测 |

### FIX-09 "success 必须带标记" CI 扫描闸门（E10 守护网补空）｜1d

| 字段 | 内容 |
|---|---|
| 问题 | 契约测试 `TestSimulateVsRealContract` 三个全 pytest.skip——"守护网本身是空的" |
| 来源 | 审计 §6 第 2 条更正、§7.0b F 类、处置建议；0926 §4-E E10 |
| 根因 | 有契约声明、无制度化执行 |
| 影响范围 | 新建 `scripts/scan_untrusted_success.py`；`tests/test_integration_contracts.py`；`.github/workflows/quality-hardening.yml` |
| 修复动作 | ① AST 扫描器：返回 success=True 的函数必须带 `execution_path|simulated|source` 标记，白名单（FFmpeg 真执行链等）逐条带理由；② 三个 skip 契约测试改可执行对拍（无 GPU 环境用轻量替身断言"假标记必存在、真产物 ffprobe 可验"，或以 `xfail(strict=True)` 替代 skip）；③ 扫描器以**独立 job** 进 CI，初版 report-only 建基线数，FIX-02~06 完成后转阻断 |
| 验证方式 | 扫描器自身单测；`pytest tests/test_integration_contracts.py -rs` 零 skip；CI job 输出命中数（随切断推进单调下降） |
| 前置依赖 | FIX-01；与 02~06 并行启动（先建基线看板） |
| 风险回退 | 纯新增 job，revert workflow 文件即可 |
| 09-26 复验 | ✅ 契约测试 10 处 skip 实锤 |

### FIX-10 桩代码与空转断言甄别（矛盾 8/R4）｜0.5d

| 字段 | 内容 |
|---|---|
| 问题 | "忽略入参返回常量/硬编码 True 标志/函数从未被调用/无产物"四特征桩，配套测试把桩当规格锁住 |
| 来源 | 审计矛盾 8（look_at 案，项目已自纠）；建议 15；0926 §4-D5 |
| 影响范围 | 全库；已知样本（`stabilize_camera_rotation` 等）已由 `3d35c14` 固化，需复扫验证 |
| 修复动作 | 实现 `scripts/scan_stub_specs.py`（AST 四特征 + "测试断言恒等于桩返回值"检测）；输出处置清单逐条 修桩/删断言/加 PLAN 标注 |
| 验证方式 | 扫描器对 look_at 历史桩样本回归自测（当时应命中）；报告 `reports/stub_scan_*.md` |
| 前置依赖 | 无 |
| 风险回退 | 纯工具新增 |

### FIX-11 证据迁移与 gitignore 反白名单（R13/矛盾 16）｜0.25d

| 字段 | 内容 |
|---|---|
| 问题 | `reports/` 整目录被 ignore（实测 `.gitignore:198`），依赖它的两个闸门测试在 CI 静默退化为 skip；测试原始日志只在可被清的 `tmp/` |
| 影响范围 | `.gitignore`；`reports/coverage_summary.json`；`tmp/*coverage*.log`、`tmp/full_*.log`、`tmp/pytest_mainline_20260924.log`；`tests/test_ci_config_consistency.py` |
| 修复动作 | ① `.gitignore` **末尾**追加 `!reports/coverage_summary.json`、`!reports/*.md` 等反白名单（档案明文教训：git 取最后匹配，写前面会被静默作废）；② 建 `reports/test-history/` 迁入近三次全量日志与覆盖率 json；③ 磁盘清理 SOP 更新：`reports/`、`data/baseline_registry.json` 列入不可清理清单（关联 FIX-34 删除纪律） |
| 验证方式 | 全新 clone 后 `pytest tests/test_ci_config_consistency.py -rs` 无 skip；`git ls-files reports/` 非空 |
| 前置依赖 | 无；FIX-13 之前必须完成 |
| 风险回退 | KB 级体积，revert 一行 |
| 09-26 复验 | ✅ .gitignore:198 实锤 |

### FIX-12 造假蓝本原位标注｜10min

| 字段 | 内容 |
|---|---|
| 问题 | `工具链集成总览.md` §7.3 `execute_with_fallback` 是 60+ 处造假的实现蓝本，且该文档仍被知识中心当"核心工具链"入口 |
| 来源 | 审计 §7.0c、建议 10 |
| 修复动作 | §7.3 原位加警告块（引用 0924 审计 §7.0c，"新代码一律走 FIX-01 契约，勿照抄"）；§8.2"✅已完成"逐条改 实/纸 标注；**不删文档**（考古证据）；FIX-43 时同步标 `stale_since: 2026-07-14` |
| 验证方式 | 目视 diff |
| 前置依赖 | 无 |
| 风险回退 | 纯文档 |

### FIX-13 CI 强制力恢复（R5/矛盾 9）｜1d

| 字段 | 内容 |
|---|---|
| 问题 | 6,200+ 测试被强制 ~12 个（0.2%）；`--cov-fail-under=29` 在 advisory job；security 哑门；summary 硬编码 echo PASSED；"coverage 已由 CI 强制执行"注释自相矛盾（实测均在） |
| 来源 | 审计矛盾 9/R5/建议 17；0926 §1.4/§六-8 |
| 影响范围 | `.github/workflows/quality-hardening.yml`（实测 L105 continue-on-error 仍在）、`ci.yml`、`quality-gate.yml`、`test-health.yml`、`pyproject.toml` L189 区注释 |
| 修复动作 | ① `full-suite-sharded` 移除 `continue-on-error` 转 required（依据：09-20 起连续 ≥6 次全量 0 failed，满足自定"两周绿"；**保持现行 Windows 串行参数**——xdist×coverage 死锁定案见 handoff §9.5，勿回退）；② security 哑门改真实 `exit 1` 或删除；summary 去硬编码 PASSED 改 `needs:` 聚合；③ pyproject 注释与事实对齐（required 生效日 + ratchet 地板 29）；④ GitHub 端 remote/branch protection 核对 → 信息缺口 Q5 |
| 验证方式 | 真实 push 触发 Actions，红/绿结论截图归档 `reports/ci/`；故意让一个测试失败 → PR check 阻断 |
| 前置依赖 | FIX-11（coverage_summary 进仓）、FIX-09（避免转强后红海） |
| 风险回退 | 偶发环境红用 `[skip-test-gate]` 人工放行并记台账，不回退 required 本身 |
| 09-26 复验 | ✅ advisory/哑门/矛盾注释均在 |

### FIX-14 Topaz / TS 编译器存在性裁决（R20/R22）｜0.5d

| 字段 | 内容 |
|---|---|
| 问题 | `topazcli.exe` 无法证实存在却在 simulate 里"成功"；TS 编译器 `build/cli.js` 与 `node_modules` 不在盘开箱即败 |
| 来源 | 审计 R20/R22；未实现声明 #13 |
| 影响范围 | `integrations/topaz_integration.py`；`compiler/`；`tools/toolchain_manager.py` 对应条目 |
| 修复动作 | ① 探测 `where topazcli` / 安装目录（Q7：需用户确认本机 Topaz 是否可用及 CLI 名）；证实→修参数并真验一帧；不存在→显式 `NotImplementedError("Topaz CLI 未证实，见 0924 审计 R20")`；② TS 编译器开发者文档补 "npm ci && build" 前置；`compile_planning_to_jsx` 降级分支（commit `5c8ac43` 已修）保持不回退；③ 输出引擎三态盘点表（真/未证实/未装）进 `reports/tools_matrix.json`，成为 README 能力矩阵的数据源（供 FIX-15/38 消费） |
| 验证方式 | 三态表落盘；被盘点引擎的默认行为与表中状态一致 |
| 前置依赖 | FIX-03（默认值治理后本裁决才有意义） |
| 风险回退 | 显式报错打断旧演示——与 FIX-03 同义，属预期 |

> **P0 小计 11 人天。完成判定（M1）全部满足后，才进入 P1/P2 的指标与闭环工作**——理由：伪造不切断时，任何"指标改善"都可能是又一次 simulate（证据闸门②的推论）。

---

## 二、P1 —— 恢复可验证性（16 项，13 人天）

> 本批共同目标：让每个对外数字都能被一条可重跑命令复现（contributing §六"战果要能复跑"）。FIX-15/18/19 建议同批提交（README 一次改到位，避免二次失真窗口）。

### FIX-15 README/概览数字纠偏｜0.25d
| 字段 | 内容 |
|---|---|
| 问题/来源 | README 落后 10 天即 4 处事实错误；矛盾 4/5；声明 #9/#10/#16。09-26 实测 README 仍写 run53/78 次/891 篇 |
| 根因 | "当前状态"分散多文档无同步机制 |
| 影响 | `README.md`、`01-项目概览/📋-项目概览-MOC.md`、`🏠-AE知识中心.md` 统计段 |
| 修复动作 | ① run 数："14 份 unified_report 在盘（78 为过时口径无法磁盘证实）"；② 知识库："编号目录 727 篇 md；1700 万字符未经证实（实测 610-830 万）"；③ 删 run53/61 在盘表述改指新基线；④ 每数字旁标出处文件；⑤ 01-MOC "v2→v7"改 v9 口径并补 date 字段 |
| 验证 | 文中数字可一条命令复现；FIX-43 的 freshness 测试上线后纳入校验 |
| 依赖/回退 | 与 FIX-18/19 同批；纯文档 revert 无风险 |

### FIX-16 conftest 三重削弱治理（第一阶段：账目化）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 审计 §3.2b⑥；建议 18。collect_ignore 41 文件、torch/whisper MagicMock、real_* 默认 skip |
| 根因 | 收集期崩溃用整文件排除绕过，绿灯规模虚胖 |
| 影响 | `tests/conftest.py` L35-82/106-124/260-287（按函数名重定位）、被 ignore 的 41 文件 |
| 修复动作 | ① 逐文件归类产 `reports/conftest_ignore_ledger.md`：模块副作用→移入函数 / import 护栏（09-24 已有 7 例先例）/ 确认死测试→删 / 真需重环境→标 `@pytest.mark.integration`；② 首周只消化机械护栏类 1/3；③ MagicMock 范围头部注释显式"仅限 unit 标记"；④ skip 原因 `-rs` 可见并附开启方法 |
| 验证 | ignore 清单只减不增（新增防回涨测试）；`pytest --collect-only -q` 计数上升且收集时间不回退（历史 71s 教训） |
| 依赖/回退 | FIX-13 后做（避免与 required 转换互扰）；按文件粒度提交 |

### FIX-17 异常吞噬与静默 pass 收敛（R7）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | S110×214+E722×16；`_safe_urlopen` 递归被吞→"全部适配器（含 ComfyUI）从未真正发出过请求"的史实；`unified_edit.py` 5 处静默 pass；`render_gate.py:20` 裸 ffmpeg 路径（09-26 实测仍在） |
| 修复动作 | ① `render_gate.py` FF 改 `shutil.which("ffmpeg") or 候选表`（1 行，同目录 9 处先例）；② `unified_edit` 5 处 `except Exception: pass` 逐处改 `logger.warning + degraded 列表 + 写入 unified_report.gate_degraded`；③ report-only CI 门 `ruff --select S110,E722` 建计数基线（修复期不阻断）；④ LLM/rubrics 不可用降级统一附 FIX-01 标记 |
| 验证 | 注入测试：mock render_gate 抛异常→`gate_degraded` 非空且退出码非 0；ruff 计数入 `reports/` 逐周下降 |
| 依赖/回退 | 无硬依赖；逐处提交单点 revert |

### FIX-18 可回退基线重建（R3/矛盾 1+1b）｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | run53/61 已被 09-24 清理误删（09-26 实测两目录均不存在），"可回退"失效；`RUN_DIR_PATTERN` 拒绝幸存目录（矛盾 1b） |
| 修复动作 | ① 新建 `data/baseline_registry.json`：登记 `unified_integration_final`、`unified_integration_27s_v9`、`unified_run63_v9_rule_distillation` 的 SHA-256 + ffprobe 实测时长/分辨率（闸门①）；② 新建 `scripts/run_dir.py` 共享 `RUN_DIR_PATTERN = unified_(?:run\d+|r1_fixed_v\d+|integration_[a-z0-9]+(?:_v\d+)?|\w*fusion)`，三消费端改 import；③ 闸门测试防三处再漂移；④ README 基线段指向 registry |
| 验证 | `build_master_polish.py output/unified_integration_final final --dry-run` 可寻址；whitelist 测试扩样例绿；EDL 63 回归不回退 |
| 依赖/回退 | 无；纯新增+模式放宽（向后兼容） |

### FIX-19 HEADLINE 指标诚实化（R1/矛盾 2/3/15）｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | "前三分位验收线 0.8193"在落盘数据中不存在（`data/reference_top/README.md:49` 自否）；0.8455（12 参照）与 0.7712（3 参照）不同子集不可比却被并列"超越" |
| 修复动作 | ① `score_reference_gap.py` 补真分位数输出（p25/50/75/90 + n + 参照清单 sha）；② 文档统一改可复现表述："n=145 实测 p75=0.8068 / p90=0.8618，best run 超 p75、距 p90 差 1.63pp"（数字以重跑为准不手填）；③ 同片 0.8455/0.8545 双值裁决（Q8：先查参照快照/浮点稳定性）；④ contributing 补"指标引用必带口径" |
| 验证 | 重跑评分落 `reports/reference_gap_v2_*.json` 含 quantiles；grep "0.8193" 仅剩史实/更正语境 |
| 依赖/回退 | 与 FIX-15 同批；纯工具+文档 |

### FIX-20 覆盖率棘轮提升 + 特征化安全网续建（R12 前置）｜1.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 地板 29 vs 实测 34.18（余量 5.2pp）；`production_director` 单测 ~14%，render() 集成层无网；0914 T6 铁律"0% 覆盖不重构" |
| 修复动作 | ① 延续 `test_core_god_files_characterization.py` 模式，为 `production_director._plan()/_analyze()` 纯逻辑分支补 golden-master（断言口径以融合线 `a891073`/`0a727a1` 后为准，勿钉旧值）；② `ae_agent_pipeline.py` 门面方法补特征测试；③ `fail_under` 29→32（棘轮只升不降）；④ 为 FIX-40 拆分建达标线：两文件 ≥40% |
| 验证 | `coverage_summary.py` 快照对照（尊重 ±15 行噪声带）；全量绿 |
| 依赖/回退 | FIX-11、FIX-13；数值改动单行 revert |

### FIX-21 服务层部署路径修复（R6/矛盾 11/12）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | Dockerfile:34-42 六处 COPY 指向 08-26 已移位文件→构建必红；8000/8001 双 api_server 分叉；deploy.bat 指向另一项目；"api_server 含 MCP 网关"无产物支持 |
| 修复动作 | ① 修/删 6 处 COPY 行并同步 `pyproject py-modules` 清单；② 新增"端点集合一致性"闸门（两 server 路由名 diff：允许新增不允许丢失）；③ 部署链路二选一（compose 8000 为主 or Dashboard 联调的 8001 为主）——**短期以 Q9 决策为准，未决前文档明示双入口现状**；④ `deploy.bat` 改名 `deploy-puppet.bat` 并头注归属 |
| 验证 | `docker build .` 本地过；一致性测试绿 |
| 依赖/回退 | 独立；文档先行不阻断 |

### FIX-22 源码文本断言反模式清理（R15）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `test_three_bridge_fixes.py` 467 行/21 处 `read_text` 断言源码子串（从不执行被测代码）；`test_aerender_v2.py:631-667` 12 条；`test_puppet_auth_gaps.py:21` 绝对路径跨机必红；09-19 已因此产生假失败 |
| 修复动作 | ① contributing 增条款"断言行为不断言源码文本；read_text 式测试须白名单+理由"；② 三文件按 import+行为断言重写（优先 auth_gaps 的绝对路径→`tmp_path`）；③ 白名单机制复用 FIX-09 扫描器 |
| 验证 | 三文件绿且 grep 无裸 read_text 断言（除带标记白名单） |
| 依赖/回退 | 独立；逐文件提交 |

### FIX-23 AE 三轨收敛防回退守护｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 0926 §四 A/B/C 族 28 条已落地对策需制度化防复活（taskkill 禁令、alert 禁令、Startup 唯一位置、双 listener 竞争） |
| 修复动作 | ① 新增 `tests/test_ae_safety_rules.py`：静态断言仓库内无 `taskkill /F /IM AfterFX`（除护栏文件）、Bridge 下发模板无 alert(、`.mcp.json` server 数 ≤ 声明上限；② `ae_channel_status.py` 增双轮询检测；③ 断言项逐条映射档案编号（可追溯） |
| 验证 | 闸门自身绿；人为注入一处 taskkill 即红 |
| 依赖/回退 | 纯新增；与 FIX-08 共用 listener 报告 |

### FIX-24 AE 交互通道复活 + 烟测转正（R14/矛盾 12/23/24）｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 09-24 诊断：加载器静默失效 + 命令名/参数键两处不匹配 + 同一文档 §6/§7 结论相反；runScript 残留发出方未定位（Q4） |
| 修复动作 | ① `tmp/ae_smoke_0924.py` → `scripts/ae_smoke.py`（UTF-8 reconfigure 头防 T1、结果落 `reports/ae_smoke_*.json`）；② 以 `ae_channel_status.py` 实测把通道③ ❌→✅ 所需动作逐项化（依赖 FIX-07/08）；③ 09-24 诊断文档加 §6/§7 矛盾裁决批注（以 §6+1967 行日志为准）；④ runScript 发出方按 Q4 定位 |
| 验证 | AE 在场 `--e2e` 三段全绿；离线时显式 skip 并打印通道状态 |
| 依赖/回退 | FIX-07/08 后执行；烟测脚本纯新增 |

### FIX-25 成本计量打通（矛盾 19）｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `cost_log.jsonl` 166 行中 pipeline 非 null 仅 2 行、stage 全 null，"≤$0.05/条"不可计算 |
| 修复动作 | `core/cost_logger.py` `log_usage()` 透传 set_context 的 pipeline/stage（unified_edit:489-493 已 set 只差落盘）；真实出片一次产 ≥1 条完整记录；`cost_report.py` 无真实记录时输出 UNVERIFIABLE 而非空表 |
| 验证 | 新行含两字段；指标首次可计算入报告 |
| 依赖/回退 | 独立；1 函数改动 |

### FIX-26 环境脆弱性治理（R17/声明 #15）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `unified_edit.py:32-48` 硬编码 14 条 D 盘素材路径（干净环境 AssertionError）；`.mcp.json` 用系统 Python312 与"生产必用 .venv"自述矛盾；requirements 全 `>=` 无钉版、CI 从不读 uv.lock |
| 修复动作 | ① 素材路径 → `config/sources_registry.json`（缺失→显式"未配置"+模板）；② `.mcp.json` 改 `.venv` 解释器（需重启会话验证 MCP ping）；③ CI 安装步骤改 `uv sync --frozen`（推荐，锁文件已在）或关键依赖钉版 |
| 验证 | 干净 venv 收集 0 error；无 D 盘环境 `--help` 不炸；`.mcp.json` 变更后 MCP 连通 |
| 依赖/回退 | 独立；.mcp.json 先备份 |

### FIX-27 检索与评分证据补强（R19）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | R@10=1.000 仅 36 条样本且生产开关依赖云端 API（离线不可用）；评分器同源自证 |
| 修复动作 | ① held-out 扩至 ≥200（从 10 号库风格卡采样；闸门②：先打印真实首行/分布再定采样流程）；② 纯离线（`AEKV_NORM_API=0`）定 R@10 底线双口径入 `reports/retrieval_eval_v2.json`；③ 接独立人评锚点（依赖 FIX-33） |
| 验证 | 新报告含 n 与双口径；生产开关文档与实测一致 |
| 依赖/回退 | ③ 依 FIX-33；①② 独立 |

### FIX-28 render_history 诚实化（矛盾 21）｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 50 条中 47 条"待判定"；27s v2-v9 八条 behavior+gate 字节级相同；4 条 GBK 乱码 |
| 修复动作 | ① `harvest_experience.py --reclassify`（有 gate 结果自动判、无标记归 `_unknown` 账目）；② 八条相同行补跑"改动生效性核对"（Q10，无执行日志则如实标"声称行为变化无指标证据"）；③ GBK 行按 T1 重写 |
| 验证 | "待判定"占比 <20% 或逐条有原因；UTF-8 校验过 |
| 依赖/回退 | 数据文件先备份 `.bak-20260926` |

### FIX-29 DVC 名实整改 + .gitignore 编码修复（矛盾 10/22）｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 无 dvc.yaml/dvc.lock（零 stage）；实测 4 个 `.dvc` vs 文档称 8 权重；.gitignore 末行 GBK 乱码（真实文件编码污染） |
| 修复动作 | ① 口径降级：补 `dvc.yaml` 登记实际 4 个 + 实测一次每日计划任务（输出进 `reports/dvc/`），文档改"仅纳管权重，8 权重口径不实"；② `.gitignore` 按 UTF-8 重写末行（规则 ASCII 保留）；全面纳管（8 权重）登记为遗留不做 |
| 验证 | `dvc metrics show` 有输出；编码校验过 |
| 依赖/回退 | 独立 |

### FIX-30 根目录与命名卫生（矛盾 20/23）｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 根级 4 个"测试报告"名不副实（test_result.txt 仅 68B 是 AE 运行时产物占名）；根 .py 12 违反自定 11 上限；OpenSpace/portfolio 空目录 |
| 修复动作 | ① 4 报告移 `archive/root_treasury_20260926/` + 说明 README（archive 被 ignore 陷阱→配反白名单，FIX-11 同源教训）；② 根 .py 归位 + 数量上限闸门测试；③ 空目录处置；④ **清理 SOP 铁律入库**：任何按名称模式批量删除必须①物化待删清单完整打印核对（等价 -WhatIf）②保留项显式白名单排除，禁止"口头保留、正则放行"（run53 误删的直接教训，`\d+`/`.*` 贪婪模式高危） |
| 验证 | 根目录清点脚本 ≤ 阈值；SOP 写入 contributing 与 08-安装部署清理文档 |
| 依赖/回退 | FIX-11 之后（反白名单就绪） |

> **P1 小计 13 人天。M2 完成判定：README/STATUS 不再有"磁盘复现不了"的数字。**

---

## 三、P2 —— 最后一公里：规则/记忆/验收真闭环（8 项，14 人天）

> 本批是项目核心卖点（"母版生规则、规则生自动出片"）从纸面到机制的落地。审计结论："零件质量一流，连接断了"——不重复造零件，只接线。

### FIX-31 规则执行器（R9/§3.4——全项目最高差异化收益）｜5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 规则只在 `production_director.py:1345`（行号按函数名重定位）做"存在性判断"，从不解析 statement；R-0002 语义藏在默认关的 `MASTER_ANCHOR_V2` 后；13 条 R8③候选零消费者；GEPA 不存在（声明 #3/#11） |
| 根因 | 规则库只有数据层无执行层；"经验→规则→自动决策转化全靠手工改代码"（项目 09-05 方案自述） |
| 影响 | `scripts/rule_registry.py`（新增 execute）、`ai/production_director.py` 消费点、`data/rules/ruleset.jsonl`（statement 可解析化）、`09-计划文件/2026-09-23_R8③候选规则裁决清单.md` |
| 修复动作 | ① 定义最小规则 DSL：`statement` 拆为 `applies_to`（已有）+ `action: param_name=fn(args)` 白名单（首版只支持 3-5 种参数型，如网格相位/量化锦定）；② `rule_registry execute --run <dir>`：加载 active 规则→产出 director 参数包→写入 `production_report.applied_rules[]`（含 rule id/version/命中依据）；③ `MASTER_ANCHOR_V2` 从环境变量隐式开关升为决策日志显式字段（默认值=现行为不变，规则命中才翻转）；④ R8③ 13 条：跑 `induct_candidate_rules.py` 重验数据前置（审计指出其中 R-9011 的 100% 掉回 50%、R-9003 被自身实测推翻——**重验不是盲转 active**），满足才进投票；⑤ GEPA：先核 py 内 11 处命中（Q3），无实现则文档统一改口径"规则归纳=确定性脚本，GEPA 未实现" |
| 验证 | **M3 判据**：同 BGM/素材/时长跑 A/B 两 run，仅切换一条 active 规则 → `beat_anchor_check.py` 指标变化方向与 statement 一致且 >5pp，`applied_rules` 记录在案；单测：DSL 解析失败=不应用+告警（绝不禁猜参数，闸门⑤） |
| 前置依赖 | FIX-19（指标口径统一才能可信 A/B） |
| 风险回退 | 规则应用失败自动回退启发式（R-0001 退场即此机制实战样板）；`--no-rules` 总开关保基线可复现；每步独立提交 |

### FIX-32 管线记忆读侧接入｜2d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `render_history` 50 条零运行时读方（§3.4 写侧-only） |
| 修复动作 | `unified_edit` 出片前查询同风格卡历史 verdict/gate：REJECT≥2 的参数组合自动降权，日志/报告打印 `memory_hit` 字段（含命中条目 id）；消费逻辑放独立模块 `core/memory_reader.py`（可测），不塞进 god 文件 |
| 验证 | 一次真实 run 报告含 `memory_hit`；人造两条 REJECT 历史→降权生效的单测 |
| 前置依赖 | FIX-28（数据先诚实化，否则读到 47 条"待判定"无意义） |
| 风险回退 | 默认只降权不禁用；`--no-memory` 开关 |

### FIX-33 验收 UI 接进出片链末端（R10/矛盾 17）｜2d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 34 条全为回填、ratings 全 0、issues 全空——UI 从未真实使用；`discover_runs` glob 对现存目录全失效；gradio 未在 requirements 声明 |
| 修复动作 | ① glob 对齐 FIX-18 新 pattern（`scripts/run_dir.py` 同源）；② requirements 补 gradio（或 `extras[acceptance]` 显式化）；③ `unified_edit` 完成后 `--open-acceptance` 钩子（默认关，文档推荐开）；④ 用 `log_acceptance.py` 补 3-5 条真一手（含 ratings+issues，人工听感裁决）；⑤ 验收产物格式固定 schema（闸门②：字段非空断言） |
| 验证 | **M3 判据**：acceptance_log 出现 `source:"ui"` 新行且 `ratings!={a:0,b:0}`；一手率口径可动（基线 4/34=11.8% 入档） |
| 前置依赖 | FIX-18 |
| 风险回退 | 钩子默认关不影响现有流程 |

### FIX-34 EDL 桥补全到主线（矛盾 6"桥只通一半"）｜1.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 现存 `edl.json` 只含 cuts，effects/text_events/overlays 三轨全缺；`inject_edl_tracks` 唯一调用方是 agents 链，主线 `unified_edit` 从不调 |
| 修复动作 | 方案 A（推荐）：`unified_edit` 在特效/文字应用环节（mastercut 链调用后）guarded 调 inject，使主线产物三轨真落盘；方案 B：降级声明"EDL 桥仅覆盖 agents 链"并改 README。两案都需：`build_text_overlay --edl` 消费 text_events 轨做一次 replay 实证（解 K3 的完整证据链） |
| 验证 | 新 run 的 `edl.json` 三轨非空 + lint L7 PASS；round-trip replay 测试扩样例；EDL 63 回归不回退 |
| 前置依赖 | FIX-18；若走三轨含文字链则依 FIX-35 |
| 风险回退 | guarded 包裹（失败不阻断出片，与 mastercut 接线同风格）；单提交 revert |

### FIX-35 build_text_overlay 归属裁决（矛盾 7：2,118 行全仓最长孤儿）｜0.5d 裁决（接线另计 3d）
| 字段 | 内容 |
|---|---|
| 问题/来源 | 零运行期调用方；项目自身 09-11 方案已自认"⚠️ 孤儿"；但其含大量字体实证（VERIFIED_FONTS/JP-CN 分池）不可丢 |
| 修复动作 | **用户决策项（Q2）**：A. 接主线（作为 `--text` stage 吃 EDL text_events，则本项扩为 3d 并入 FIX-34）；B. 明确标注"实验线，不参与出片"并移 `scripts/experimental/`，知识库入口同步改注。无论 A/B：从能力全景中删除隐含"已集成"表述 |
| 验证 | grep 运行期调用方与声明一致（quality-verify 思路：存在≠接入） |
| 风险回退 | 移目录保留 git 历史（git mv） |

### FIX-36 River 伪漂移修复（§3.4 半闭环）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 5454 观测真实但 quality 恒 0.0、duration 5/6 恒 0.0；plan 阶段 `drift_counter=653` 而其成功率恒 1.0（std=0）→ 伪漂移 |
| 修复动作 | ① 观测写入端接真实指标（verify 阶段 gate 结果→observer）；② 漂移判定改"基于真实方差，std=0 不计数不告警"；③ 历史脏观测段标注（不删数据，加 `legacy:true` 字段） |
| 验证 | 新观测 quality 非零占比 >50%；单测：零方差→drift 不涨 |
| 前置依赖 | FIX-17（降级标记体系） |
| 风险回退 | 判定变保守只会减少误告警，低风险 |

### FIX-37 因果/反事实声明降级（§3.4 纸面项）｜0.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `counterfactuals.json` 不存在→零产出；因果图 32 节点/10 边全 `is_expert:true、sample_count:0`（100% 先验）；但全景报告写"支持反事实推理" |
| 修复动作 | 二选一：补一次真实 run 的最小反事实评估产出；或全景/MOC 改标"机制就绪、数据未积累（PLAN）"。闸门⑤直接适用，推荐先降级声明（半天），产出随 FIX-32 自然积累 |
| 验证 | 声明与 `data/causal_engine/` 目录实际内容一致（存在性检查脚本） |

### FIX-38 引擎主链战略裁决（矛盾"十余款集成 ✅"与"主链出片"并存）｜0.5d 决策（执行另计）
| 字段 | 内容 |
|---|---|
| 问题/来源 | 0924 审计头号架构事实：真正出片的 `unified_edit` 一行专业引擎都不调（只用 librosa+本地 CNN/CLIP+FFmpeg）；"集成"是能力库非产线组成（§八结论 1；声明 #20 三处口径 9/11/十余款互不一致） |
| 修复动作 | **用户决策项（Q1）**：A. 选 1-2 个已真实验证引擎接进主链 stage（候选：Resolve 调色——09-24 已真实出片 SATAVG 3.94→6.81；鼓点分离——已在盘证据最硬），其余改口径；B. 全部改口径"按需能力库"。无论 A/B：README 能力矩阵每行带"主链/按需/未证实"三态标签，数据源=FIX-14 的 `tools_matrix.json` |
| 验证 | 能力矩阵与三态盘点表逐项一致；若选 A 则新 run 报告含引擎真实调用证据（ffprobe 对照） |
| 风险回退 | 接引擎进主链属行为变更，需基线 A/B（FIX-18）护航；决策前先改文案零风险 |

> **P2 小计 14 人天。M3 完成判定：规则→决策、记忆→决策、验收→数据三条链路各有真实落盘证据。**

---

## 四、P3 —— 结构债与文档治理（8 项，11 人天）

> 本批大部分不阻断 P0-P2，可穿插；但 FIX-40 有硬前置（FIX-20 安全网），FIX-43 是文档族的枢纽项。

### FIX-40 导入债收敛 + 入口/god 层结构收敛（R8+R12 后半，最高性价比技术债）｜4.5d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `puppet-automation` 目录名带连字符非合法包名→5 份 shim 逐字复制 + 73 处 sys.path 补丁 + config 命名冲突（发现 2/声明 #25：裸导入三集成模块必 ModuleNotFoundError 被吞）；`ae_agent_pipeline.py` 5,903 行单类 ~180 方法且不在 god 清单；`production_director.py` 6,312 行单类 92%（09-26 实测又比审计长 323 行，**持续生长中，越早收越好**） |
| 修复动作 | ① `pip install -e puppet-automation`（包名 `puppet_automation`），删 5 份 `_ensure_puppet_automation_shim` 与内联版本；sys.path 补丁收敛至 conftest 一处；`test_import_paths_regression.py` 从"守绕行"改"守正规"；② 裸导入三集成模块入 `_import_redirect` 表或改绝对导入，禁 `except ImportError` 静默禁用；③ 在 FIX-20 安全网（≥40%）后拆 `production_director`：预设常量表先外置（立减 ~1,500 行）→按 分析/规划/执行/数据契约 四分；④ `ae_agent_pipeline` 按 stage 分 mixin；⑤ 降级项：`filter/transition/text_animation_engine` 改口径"参数库"，只做静态表外置 JSON，不拆逻辑（审计：真算法 <800 行，拆分收益低于观感） |
| 验证 | 干净解释器 `import puppet_automation` 过；`test_syspath_hygiene.py` 唯一数上界下调且绿；每步拆分全量 6,000+ 测试绿（行为零变化）；两文件行数下降且 `C901` 计数降 |
| 前置依赖 | FIX-20（硬阻断：0% 安全网不拆，T6 铁律） |
| 风险回退 | 每子系统一提交可二分 revert；行为变更零容忍（特征测试守） |

### FIX-41 服务层终态收敛（承 FIX-21 中期）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 双 api_server 分叉未消除（矛盾 11）；Dashboard 5 页面 mockData 兕底→后端挂掉静默显示假数据 |
| 修复动作 | ① 按 Q9 决策合一（推荐 8001 并入根 server 或反之，留一套为壳）；② Dashboard mockData 改"显式错误态 + 视觉 MOCK 标记"，不许静默假数据（与 FIX-01 同一精神的 UI 版） |
| 验证 | 端点一致性测试升级为"完全相等（除非白名单）"；Dashboard 断开后端→页面显示错误态非假数据 |
| 依赖 | FIX-21、Q9 |

### FIX-42 orchestrator 串行声明纠偏（声明 #17）｜0.25d
| 字段 | 内容 |
|---|---|
| 问题/来源 | MOC 写"依赖拓扑排序 + 并行执行"，实测 `DAGOrchestrator.execute` 纯 for 串行、`max_parallel=1` |
| 修复动作 | MOC/全景描述改"拓扑排序 + 顺序执行（并行度 1，接口预留）"；若未来真并行，须先完成 FIX-09（竞态会放大假成功）并在档案登记新坑 |
| 验证 | 描述与代码行为一致（评审项） |

### FIX-43 文档"当前真相"机制（R11——文档治理主项）｜2d
| 字段 | 内容 |
|---|---|
| 问题/来源 | 四代入口并存互斥（§3.6）；TASK_STATUS"假冻结"已误导过一次；date 字段不可信（符合率 15.4%）；`.trae/rules/` 在导航零入口；05-测试套件易混淆 |
| 修复动作 | ① 新建根 `STATUS.md`（会过期设计）：当前可回退基线（引用 FIX-18 registry）+ 各模块一行状态（口径=0924 审计裁定表 + FIX-14 三态表）+ `data_cutoff` front-matter；② 新增 `tests/test_status_freshness.py`：STATUS 引用路径 exists 校验（闸门①代码化）+ cutoff 距今 >21 天即 fail（"会过期"制度化）；③ 三份 07-14 入口加 `stale_since/superseded_by`；④ `03-阶段报告-MOC` 改脚本自动生成（扫未索引新文件）；⑤ `.trae/rules/` 接入知识中心导航；⑥ `anti-patterns.md`/`design-patterns.md` 头加 legacy 横幅（属 ae-vocal-remover，通识有效、架构条款勿引用）；⑦ `超级开发工作流-SKILL.md` 标注第三方译本来源；⑧ `TASK_STATUS.md` 正式归档或废弃（终结假冻结）；⑨ `05-测试套件/README.md` 链接进 STATUS 附录（澄清已有，入口没接） |
| 验证 | 断链/引用存在性测试绿；四代入口只剩一代主入口 + 其余带 stale 标 |
| 依赖 | FIX-18（基线数据源）、FIX-14（三态表）；不碰生产代码可提前穿插 |

### FIX-44 重复资产收敛与死代码处置（R18/R22 声明层）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `scripts/material_searcher.py` vs `integrations/material_searcher.py` 同名双份；`learning/result_verifier.py` vs `utils/`；`api/` vs `tools/toolchain_api.py` vs `web/integrator_api.py` 重名；`fastapi-skeleton/`（含第二份 uv.lock）零耦合；`ae/ae_mcp_client.py` 双胞胎（FIX-07 已管） |
| 修复动作 | 每组保留一处为真相源（grep 调用量定主从），另一处改薄转发 + DeprecationWarning 或直接删；`fastapi-skeleton/` 移 archive（配反白名单）；文档"37 端点/13 server"类计数按实测（63/5）更正 |
| 验证 | 同名双份 grep 命中归一；端点计数与实测一致 |

### FIX-45 可达性盘点与退役标注（R22 执行层，建议 12）｜1d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `ae-mcp-server/`（无 dist 不在 .mcp.json）、`mcp-extension/`（install 目标在仓库外且通道 09-19 已退役）、`phase7-media-tools/`、`bridges/{ae_bridge_base,unified_bridge_base,adobe_bridge_adapter}.py`（无生产消费方）、`integrations/rife_adapter.py`（源码自述 `_is_real=False`）、`vinland_saga_v12_davinci.lua`（孤儿） |
| 修复动作 | 按 AE 三轨收敛方法论（先可复跑诊断拿客观事实，不读自述）逐项裁定：保留（带理由+最后验证日）/ 归档 `archive/`（反白名单）/ 删除；输出 `03-阶段报告/可达性盘点_YYYY-MM-DD.md` 含逐项证据 |
| 验证 | 盘点表内每项有 真/纸/死 三态标记与处置结果；新一轮全库收集/测试不受影响 |
| 依赖 | FIX-43（盘点结果进 STATUS 消费） |

### FIX-46 知识库质量与引用合规｜0.75d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `学术研究与技术文献综述.md` 含≥三条疑似虚构参考文献且被知识中心/10-MOC 当入口引用——**违反项目自己的证据闸门 2/5，完整性红线**；深层索引腐烂 83%（15-3D 映射表 10/10 断，wikilink 名 vs 实际文件名空格差异）；10-MOC 六条"生成式目录幻觉"（摘要先于文件存在）；≥30 篇 ≤5KB 填充；`learned_aep_patterns.md` 仅 75B 却计入篇数 |
| 修复动作 | ① 疑似虚构引用文档移 `quarantine/`（在 MOC 撤链，保留原文待人工核实真伪——核实需外部文献库，本地无法证真即不得回流）；② 批量修 15-3D/13-素材 断链（脚本比对文件名归一化空格/连字符差异后重建 wikilink）；③ 六条幻觉条目删除或补文件；④ 5KB 以下填充清单化（只标注 `thin:true`，删除需 Q11 决策） |
| 验证 | 断链扫描分层：顶层 <3%、深层 83%→<20%；quarantine 入口零引用 |
| 依赖 | 扫描工具复用 FIX-43 |

### FIX-47 卡点铁律双份同步闸门｜0.25d
| 字段 | 内容 |
|---|---|
| 问题/来源 | `.trae/rules/montage-card-point-rules.md`（Trae alwaysApply）与 `02-开发文档/漫剪卡点铁律…md`（跨工具版）是**刻意冗余**（0926 §七判定：不是错误），但双份无同步必漂移 |
| 修复动作 | CI 新增一步：diff 两文件"铁律正文段"（剔除各自头部说明），差异超阈值即报；两文件互写"修改必双写"横幅 |
| 验证 | 人为改一处不同步→闸门红 |

> **P3 小计 11 人天。M4 完成判定见 §0.2。**

---

## 五、已消化 / 无需重复修复项（避免与 09-25/26 工作冲突）

| 审计/文档条目 | 现状 | 证据（均为 09-26 前实锤） |
|---|---|---|
| 矛盾 13（`vrs/` 整体不可导入） | ✅ 已修 | commit `19bbf19`；09-24 四阶段评测 VRS 真实音频 3/3 |
| 双 listener 协议分叉（0926 §四 A5） | ✅ 已统一（eval+函数体双兕底，PRO_CHECK 两风格全过） | `docs/ae_bridge_lessons.md` §10.1 |
| `analyze_beat` 旧 API 硬崩 | ✅ 已重实现+真乐回归测试 | handoff §9.2（真 stem 81 拍/6 段） |
| 收集阶段 `import faiss` 不可收集 | ✅ 已补依赖 | commit `3fd3ba6`（收集错误 1→0） |
| 子进程挂住=测试挂住 | ✅ 系统性超时加固完成 | commits `0c911fb`/`a891073`/`167f096` |
| puppet 存量 29 红 | ✅ 清零 | commit `72808f8`（632 passed/14 xfailed/0 failed） |
| 建议 5 bulk ruff autofix（"静默期再做"） | ✅ 已执行（~20K） | commit `e64ce6d`——后续 lint 基线以 fix 后为准 |
| 矛盾 14（Blender "待集成"转述失真） | ✅ 审计已自我更正 | headless bpy 5.1.0 真实可用；VSE 是 POC 后主动"不接"非遗漏 |
| 速度分层塌档（七关闸门 FAIL 披露） | ✅ 已闭环 | commit `0a727a1` 闸门"拦→修→过"全链路实证（真实 FAIL 优于全绿自夸，与长期记忆闸门策略一致） |
| 旧 7 个失败测试（config×2 + phase2×5） | ✅ 09-14 已修 | commit `5c8ac43`，验证 26 passed+209 passed |
| `--hl-safe` 高光回收等视觉线 | ✅ 已落地（默认开需 AE 复验一项仍挂账，非伪造类） | handoff §9.5 |

> 注：§四中部分 D 族坑（D1 reload、D2 xdist×cov 死锁→CI 串行）的对策已入 CI 配置，FIX-13 转 required 时**必须保持串行参数不回退**。

---

## 六、信息缺口与需用户决策清单（方案定稿前必须回收）

| # | 缺口 | 阻塞项 | 需要补充什么 |
|---|---|---|---|
| Q1 | 专业引擎是否进主链（能力叙事 vs 叙事诚实） | FIX-38 | 产品意图：要"集成名副其实"还是"轻主链+能力库" |
| Q2 | `build_text_overlay` 接主线还是归档 | FIX-35/34 | 文字线会话规划（v15/v16 后续是否继续投入） |
| Q3 | GEPA 在 py 内 11 处命中是否真实装（审计称无实现，本轮 grep 与审计口径冲突） | FIX-31⑤ | 逐处阅读 11 处命中定性（预计 2h） |
| Q4 | 仍在发 `runScript` 的发出方未定位 | FIX-24 | 运行时监听取证：在 ae_command.json 写入处挂文件监控（Process Monitor / Python watchdog）抓当前写入进程 |
| Q5 | GitHub 端 remote 指向 ae-portfolio、CI 是否真实运行过、分支保护是否存在 | FIX-13 | 需仓库网页端/CLI 登录态证据（本地无法证实） |
| Q6 | PR/PS/ME 是否仍在使用（桥目录四套修复优先级） | FIX-07 验证段 | 用户确认 Adobe 全家桶日常使用范围；不用则降为"结构修+状态标 retired" |
| Q7 | 本机 Topaz CLI（topazcli）是否存在及参数 | FIX-03/14 | 安装目录清单或卸载决定 |
| Q8 | 0.8455 vs 0.8545 双值成因 | FIX-19③ | 两次评分的参照快照 diff + 重跑稳定性（预计 1h） |
| Q9 | 部署链路主从：compose(8000) vs Dashboard 联调(8001) | FIX-21/41 | 用户选择保留哪套为准 |
| Q10 | 27s v2-v9 八条同指标行的当时执行日志是否存在（"声称改了重音门槛/包络/峰值"是否真跑过） | FIX-28② | `logs/`与 `tmp/` 归档检索（可能已随 09-24 清理丢失→如丢则只能标"无证据"） |
| Q11 | 知识库 ≤5KB 填充文档的处置授权（标注 or 删除） | FIX-46④ | 用户授权（删除不可逆，建议只标） |
| Q12 | 工作区当前未提交的融合线/测试改动（`test_section_fusion.py` 等）何时入库 | 全部前置 | 执行会话 owner 确认提交或隔离（handoff T7） |

---

## 七、执行顺序与依赖链（关键路径）

```
FIX-01 契约 ─┬→ FIX-02 → (验证)
             ├→ FIX-03 → FIX-14
             ├→ FIX-04, FIX-05, FIX-06（可并行）
             └→ FIX-09 ←─ FIX-11 ─┐
FIX-07 → FIX-08 → FIX-23/FIX-24    ├→ FIX-13 required → FIX-16 → FIX-20 ─┐
（桥协议链，与上行文件零重叠可并行）  └──────────────────────┘          ├→ FIX-40
FIX-12（10min，随时）                                                    │
FIX-18 ─┬→ FIX-15/19（同批） → FIX-31 →（M3）                          │
        ├→ FIX-33/34/35（文字链依 Q2 决策）                              │
        └→ FIX-43/45（文档族消费基线/三态表）                            │
FIX-28 → FIX-32/36；FIX-17 → FIX-36                                    │
Q1–Q12 回收 → 对应裁决项定稿（FIX-35/38/41/21）                         │
```
**硬顺序**（违反即返工）：① FIX-01 先于一切切断（否则各修各的判据）；② FIX-11 先于 FIX-13（否则 required 后覆盖率闸门静默退化）；③ FIX-20 先于 FIX-40（0% 安全网不拆，T6）；④ FIX-19 先于 FIX-31（口径不统一无 A/B）；⑤ 伪造不切断前不启动任何"指标优化"类工作（闸门②推论）。

---

## 八、修复完成后的验收检查清单（逐条打勾）

**P0 / M1**
- [ ] `py -3.12 ai/auto_produce.py --dry-run` 无产物时 exit≠0（FIX-02）
- [ ] `grep -rn 'mode: str = "simulate"' integrations ai` 归零（FIX-03）
- [ ] `grep -n 'AI_VIDEO_SIMULATED_OUTPUT\|SIMULATED_MATTE\|RESOLVE_GRADED_OUTPUT\|TOPAZ_SIMULATED_OUTPUT' -r .` 仅剩 `.simulated` 目录/测试断言语境（FIX-03）
- [ ] `ai_director` 素材入池处有 `execution_path==real` 白名单且全 Mock 场景回归测试在（FIX-05）
- [ ] `scripts/check_model_registry.py` 进 CI，trained→exists+SHA 全匹配（FIX-06）
- [ ] `tests/test_bridge_path_consistency.py` 绿：Python 写路径==JSX 读路径（FIX-07）
- [ ] 命令词汇表单一真相源 + 生成器命令集 ⊆ listener 表（FIX-08）
- [ ] `scan_untrusted_success.py` 进 CI 且命中数基线落盘、逐周下降（FIX-09）
- [ ] `pytest tests/test_integration_contracts.py -rs` 零 skip（FIX-09）
- [ ] `full-suite-sharded` 无 continue-on-error，一次真实全量绿证据入 `reports/ci/`（FIX-13）
- [ ] pyproject 注释与 CI 事实一致；security 哑门/summary 硬编码 PASSED 已清（FIX-13）
- [ ] `git ls-files reports/` 非空；全新 clone 后一致性测试不 skip（FIX-11）
- [ ] 蓝本警告块在位（FIX-12）；`scan_stub_specs.py` 首份报告产出（FIX-10）

**P1 / M2**
- [ ] README/STATUS 每个数字旁有出处文件且抽查可复现（FIX-15/19）
- [ ] `data/baseline_registry.json` 含 ≥2 条幸存基线 SHA；三处 RUN_DIR 同源（FIX-18）
- [ ] 评分报告含 quantiles 字段；"0.8193" 仅存史法语境（FIX-19）
- [ ] conftest ignore 账目落盘且只减不增（FIX-16）
- [ ] `gate_degraded` 注入测试绿；S110/E722 计数基线入 `reports/`（FIX-17）
- [ ] `docker build` 通过或 docker 声明已从文档删除（FIX-21）
- [ ] 三处源码文本断言文件重写；auth_gaps 无绝对路径（FIX-22）
- [ ] `scripts/ae_smoke.py --e2e` 绿（AE 在场）或显式 skip+状态打印（FIX-24）
- [ ] cost_log 新行含 pipeline/stage；UNVERIFIABLE 语义生效（FIX-25）
- [ ] 干净环境（无 D 盘）收集不炸；CI 读锁文件（FIX-26）
- [ ] render_history "待判定"<20% 或逐条有因（FIX-28）；DVC 口径与盘实一致（FIX-29）
- [ ] 根目录闸门绿；清理 SOP（清单核对+白名单排除）已写入 contributing（FIX-30）

**P2 / M3**
- [ ] 一条 active 规则 A/B 实验：指标变化方向与 statement 一致且 >5pp，`applied_rules[]` 在 production_report（FIX-31）
- [ ] R8③ 候选重验记录（逐条：满足进投票/不满足引用 evidence_gap 自退）（FIX-31④）
- [ ] 真实 run 报告含 `memory_hit`（FIX-32）；acceptance_log 有 `source:"ui"` 且 ratings 非零（FIX-33）
- [ ] 新 run 的 edl.json 三轨非空 + L7 lint PASS（FIX-34）；text_overlay 归属有裁决记录且声明一致（FIX-35）
- [ ] River 新观测 quality 非零 >50%（FIX-36）；反事实声明与 `data/causal_engine/` 实态一致（FIX-37）
- [ ] README 能力矩阵三态标签化，与 tools_matrix 逐项一致（FIX-38）

**P3 / M4**
- [ ] 干净解释器 `import puppet_automation` 过；syspath 上界下调且 `test_syspath_hygiene` 绿；shim 样板删除（FIX-40）
- [ ] `production_director`/`ae_agent_pipeline` 行数下降且全程 6,000+ 测试绿（FIX-40）
- [ ] 端点一致性测试升级；Dashboard 断后端显示错误态非 mock 假数据（FIX-41）
- [ ] MOC"并行"表述已纠（FIX-42）
- [ ] `STATUS.md` 在盘 + `test_status_freshness.py` 绿；三旧入口带 stale；.trae/rules 可导航；TASK_STATUS 处置完成（FIX-43）
- [ ] 同名双份归一；盘点表每项三态标记+处置留痕（FIX-44/45）
- [ ] 疑似虚构引用已隔离不可发现；深层断链 <20%；幻觉条目清零（FIX-46）
- [ ] 卡点铁律双份 diff 闸门在 CI（FIX-47）
- [ ] Q1–Q12 全部回收并在本报告尾部回填"信息缺口闭合记录"

---

## 九、方案自身的证据边界（诚实声明）

1. 本方案基于 0924 审计 + 09-26 实测复核；**审计为静态取证（当时 pwsh 不可用无法实跑）**，个别行号/路径已与磁盘现状交叉校验并在各 FIX 的"09-26 复验"列标注差异（如 FIX-04 predict_skip_stage 已消失、FIX-06 所在目录修正、FIX-40 目标文件又长 323 行）。
2. 工作量按"单人专注、含测试编写"估；不含真实 AE/Resolve GPU 渲染等待时间（每次上机验证另计 0.25-0.5d 缓冲）。
3. 所有"改成什么"级别的描述均可执行；仅剩 Q1–Q12 开放项需外部信息。按闸门⑤，本文件整体是 **PLAN**；逐项升级为 READY 的唯一路径是对应验证命令的真实输出落盘。
4. 执行过程发现的每个新坑，完工后按铁律回写 `docs/*_lessons.md` 对应章节（经验回写触发机制），本方案执行记录建议作为 §十 追加于此文件末尾，不另开文档。

---

## 十、执行记录（逐项追加，格式：编号｜日期｜状态升级｜证据）

### FIX-01 成功语义契约｜2026-09-26｜PLAN → **READY**
- 交付：`docs/execution_result_contract.md` v1.0（新建）；根级 `exceptions.py` 追加 `E504 UNTRUSTED_SUCCESS` + `UntrustedSuccessError`；`tests/test_execution_contract_schema.py`（新建）。
- 真实验证：`pytest tests/test_execution_contract_schema.py tests/test_exceptions.py -q` → **98 passed in 0.69s**；触碰文件 ruff 全绿。完整命令与输出见 `reports/fix01_verification_20260926.md`。
- 方案纠偏：原写 `core/exceptions.py` 不存在，异常体系实位于根级 `exceptions.py`（已按实际落位，契约 §8/§9 留痕）。
- 新发现（上报待决）：HEAD 全仓 ruff 基线门为红——`scripts/test_ae_real_chain.py:244` F821 缺 `import os`，系 `7040636`（09-24 归档批）预存，非本会话/非并行在途 WIP；按防污染原则未越权修改，1 行可修，待授权。
- 未提交：本轮变更未 git commit（等待用户指令；建议定向 add 三个文件路径，勿 -A）。

> 关联：`03-阶段报告/项目文档扫描与模块功能评价_2026-09-24.md`（问题清单源）· `03-阶段报告/项目全貌梳理与开发经验提炼_2026-09-26.md`（经验归纳源）· `docs/handoff-2026-09-14-quality-audit-sync.md`（前序修复基线）
