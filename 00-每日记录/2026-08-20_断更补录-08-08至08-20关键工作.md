---
tags: [每日记录, 补录, 项目治理]
date: 2026-08-20
type: 断更补录（覆盖 2026-08-08 ~ 2026-08-20）
---

# AE-Knowledge-Vault 断更补录（2026-08-08 ~ 2026-08-20）

> 性质：`00-每日记录/` 自 2026-08-07 后断更，本文依据 git 提交历史、`09-计划文件/` 与 `reports/` 产物补录该窗口关键工作。
> 证据来源标注于各条目后；非逐日流水，为决策级补录。

---

## 1. 最近完成任务（按主题归并）

### 1.1 工程治理：核心代码全量入库（08-18 集中执行）
- 根目录配置/文档/工具脚本、115 个知识库与计划文档、49 个数据文件、325 个工具脚本、227 个测试文件全部入库（commits `3b15f82`/`0489358`/`816787b`/`38aed66`/`0ccc50e`/`7059189`）
- puppet-automation 引擎基础设施 + ae/blender/ffmpeg 三核心引擎 + Phase2 剩余引擎/服务同步入库（`ef441a1`/`0dc6a86`）
- core 模块 13 个剩余文件对齐 feat 分支（`4554aa3`）；新增 AI 业务层 `ai/` + Web 服务端 `web/` + 风格卡 `data/style_cards`（`2a90f2c`）
- 修复 3 项真缺陷：知识库空格 Bug + 2 个 core 模块缺失（`8f939aa`）
- 效果：**08-17 扫描报告的 P0-1（孤立分支）/P0-3（628 未跟踪）就此解决**；08-20 实测 `git status --short` 仅 19 行

### 1.2 运镜分类器：v3b 根因修复与全参微调 v4 启动
- 新增 six 六类合并 schema，针对 v3b val_acc=0.4603 根因（push/zoom_in 重叠 + 稀缺类）（`09b42ec`）
- 训练性能修复：collate 提升模块级（spawn 可 pickle）+ decord 原生 224 缩放解码，吞吐 0.9→4.3 样本/s（`3dedd2c`）
- 云端大训练支持：`--lora-rank/--unfreeze/--amp/--early-stop`（r=64+解冻 2 层=19.7% 可训练）（`851df78`）
- V2 文献证据审计：4 项选型修正（全参微调/双标注器/Peak-End-Net/OmniShotCut）+2 项确认，附 2025-2026 论文来源（`77ce67f`）
- 全参微调模式 `--full-ft`（LoRA 不等价性修正）+ 任务书/冒烟闸门全链路更新，本地冒烟 100 样本 25s 通过（`8045103`）
- 推理侧支持 full-ft 完整模型加载分支（v4 回传兼容），冒烟全参模型 CUDA 加载 + 真实视频推理通过（`6f134fa`）
- `--data-root` 跨机路径重映射，train_pack_v1 包内冒烟 12096 样本 missing=0（`d90e60f`）

### 1.3 训练外发体系（朋友 5090/云端 GPU）
- 朋友 5090 交接任务书 + 全项目能力增强总表 + 云端 GPU 总体方案 V2（`4081a9f`）
- 全模块训练总计划 MASTER PLAN：十模块深度分析 + 分批发包 + 晚间移交清单（`758f154`）
- 回传方法补全为三方式明确步骤（微信直发/网盘/局域网）（`1efdbe5`）；阶段 5 改为智能体视角（`2e032c9`）

### 1.4 基础设施与规范
- llm_gateway 熔断器状态机增强：HALF_OPEN 半开探测与健康感知 failover（`f2758da`）
- **证据闸门铁律**：五道闸门阻断纸面结论进入执行链（2026-08-18 WorkBuddy 失实结论事故复盘）（`f4f738c`）
- tool_config.json 补全 schema_version + last_updated（修复"过期 33 天"提示，即 08-17 扫描 P1-7）（`175c8c3`）
- T1-T10 共 8 个科研级证据文件入库（Git 分支差异/知识库效果清单/导入健康/因果引擎验证等，653KB+）（`0c20d2d`）

### 1.5 规划与调研产出（08-08~08-17 窗口，以文档形式存在）
- `2026-08-14`：FATE 抠像定稿 v14、RVM 对比评估、抠像时序增强评估、M0-M4 交付报告、OpenBiliClaw 集成计划
- `2026-08-15`：TEMPO-StageCritic 落地（另有 `03-阶段报告/2026-08-15_TEMPO-StageCritic落地交付报告.md`）、开源生态调研、后期特效合成深度方案、DSH 会话计划
- `2026-08-16`：木偶骨骼操纵升级方案（部件木偶+刚性关节方向定案）
- `2026-08-17`：全面深度扫描报告 v2、后端架构重构目标架构与分阶段计划、P0-2 双协议只读诊断、开发路线图阶段 A-E 更新

---

## 2. 当前阻塞问题

| # | 问题 | 证据 | 状态 |
|---|------|------|------|
| B1 | FATE alpha.mov 全仓搜索未找到（仓库外磁盘待确认） | 08-20 实测 | 调查中 |
| B2 | `integrations/` 仅剩 `color_presets/`，与集成方案声称的 3 个适配器不符 | 08-20 实测 | 调查中 |
| B3 | AE Bridge 双协议共存竞争（收敛需用户确认时机） | `2026-08-17_P0-2_AE桥接双协议只读诊断.md` | 待确认 |
| B4 | 全参 v4 尚未产出精度验收（golden set 未建） | 08-18 提交仅冒烟 | 进行中 |

---

## 3. 下一步任务清单

按 `09-计划文件/2026-08-20_全项目扫描分析与提升方案.md` §八执行：
1. P0-A：TASK_STATUS 重建为项目级看板（本补录为其前置）✅ 本文完成补录部分
2. P0-B：产物一致性核查（FATE MOV / 权重路径 / 适配器位置）
3. P0-C：双协议诊断结论落地（先只读确认，收敛动作待用户确认）
4. P0-D：关键产物 SHA-256 清单与外盘备份登记
5. P1-B：CI 覆盖率非阻塞采集（Week 2）

---

## 4. 关键经验总结

1. **证据闸门铁律**（`f4f738c`）：任何"已完成/已修复"结论必须五道闸门验证后才可进入执行链——本次扫描发现的三处文档与磁盘不符（权重路径/MOV/适配器）正是该铁律的适用场景。
2. **断更代价可量化**：08-08~08-17 十天无记录，导致 08-17 扫描时对分支状态、v3b 进度均需重新取证；08-18 集中入库 26 个提交也说明工作积压后批量提交丢失了过程语义。
3. **根因修复优于调参**：v3b 精度 0.4603 的根因是类别语义重叠（push/zoom_in）而非超参，six 六类合并 schema 是正解方向（`09b42ec`）。

---

## 5. 新会话快速启动指南

> 请阅读 `09-计划文件/2026-08-20_全项目扫描分析与提升方案.md`，然后继续执行 §八 的 P0 行动项（当前进度见 `TASK_STATUS.md`）。

- 三主线状态：运镜分类器（全参 v4 冲刺中）/ 抠像（v17 定案，产物核查中）/ 木偶（look_at 固化待做）
- 治理基线：git 在 `master`，status 19 行；红线 = `external/after-effects-mcp/src` + `mcp-bridge-auto.jsx`（94609 字节）
