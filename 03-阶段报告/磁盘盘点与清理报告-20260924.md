# 磁盘空间盘点与清理报告（2026-09-24）

*触发：AE 磁盘缓存警告 + 网关监控 disk=93.7% ｜ 结果：两盘合计释放约 175GB*

---

## 一、清理战果总账

| 盘 | 清理前 | 清理后 | 释放 |
|---|---|---|---|
| C | 19.2 GB 剩余 | **96.6 GB** | ≈ 77 GB |
| D | 82.9 GB 剩余 | **180.7 GB** | ≈ 98 GB |

### C 盘已删明细
| 项 | 大小 | 性质 |
|---|---|---|
| `AppData\Local\Temp` | ~40 GB | 系统临时垃圾（AE 缓存警告根源） |
| `output\flagship_e2e_*` 空壳 ×30+ | ~0 | e2e 冒烟残留 |
| `output\unified_run2~62` 旧轮次 | ~11 GB | 9-08/9-10 进化中间产物 |
| `output\unified_r1_fixed_v1~8` + `_BAD` | ~9 GB | 9-10 中间版本 |
| `tmp\wheels`（旧 torch whl）+ 杂项压缩包 | ~5 GB | 可重下载安装包 |
| `.mypy/.pytest/.ruff_cache` + 回收站 | ~0.4 GB | 工具缓存 |

> ⚠️ 误删记录：`output\unified_run53`（9.63GB，9-19 进化 run 成片）因删除正则 `\d+` 贪婪匹配被误删，不可恢复。教训已沉淀记忆：**批量按名称删除必须先物化打印待删清单 + 保留项显式白名单排除**。幸存的最新产物：`unified_run63_v9_rule_distillation`、`unified_integration_27s_*` 全系列、`unified_integration_final`。

### D 盘已删明细（第一批，用户确认制）
| 项 | 大小 | 性质 |
|---|---|---|
| `D:\_Trash_2026-09-08` | 37.1 GB | 9-08 大扫除挪入的旧备份/调试目录 |
| `D:\Ae25\磁盘缓存` | 20.0 GB | AE 媒体缓存（AE 自动重建） |
| `D:\uv-cache` + `npm-cache` + `pip-cache` + `ms-playwright` | 12.2 GB | 纯工具缓存 |
| 回收站（两盘一并清空） | ~29 GB | — |

---

## 二、D 盘归属表（470GB 用量，65 个顶层目录已全量归因）

### 🚫 别动（核心资产与程序）
| 目录 | 大小 | 身份 |
|---|---|---|
| `D:\AE-Data` | 80.3 GB | **训练资产**：AnimeCamera/AutoShot/CameraBench/ClipShots/cloud_lora/cloud_weights 等模型与数据集 |
| `D:\AE-Work` | 65.0 GB | 资源库（项目根 `resources` junction 指向它） |
| `D:\Ae25\Adobe` | 39.0 GB | AE 2025 程序本体（`Ae25` 总 59GB = 程序 39 + 缓存 20，缓存已清） |
| `D:\ComfyUI` | 48.0 GB | 模型库 + 自定义节点 |
| `D:\app` | 25.7 GB | DaVinci Resolve Studio 21.1（含 ResolveMCP.exe） |
| `D:\AE-Archive` | 12.3 GB | 用户归档（2026-08-19） |
| `D:\Pr25` / `ps` / `Me` / `Au` / `Blender` / `top` | ~19 GB | Premiere / Photoshop / Media Encoder / Audition / Blender / Topaz 程序 |
| `D:\ai-pkgs` | 1.1 GB | **勿删**：peft/transformers 等训练依赖（`d_ai_pkgs.pth` 注册） |
| `D:\aot_corpus` / `multi_ip_corpus` / `models` | ~2.3+ GB | VLM 语料/断点/权重 |
| `System Volume Information` / `$RECYCLE.BIN` | — | 系统 |

### ⚠️ 第二批候选（待用户逐项确认，合计 ≈ 61GB）
| 目录 | 大小 | 删除代价 |
|---|---|---|
| `D:\hf_cache` | 31.9 GB | HuggingFace 权重（HF_HOME 指向此）；VideoMAE/LoRA 训练要用，删后重下载 |
| `D:\夸克` | 12.7 GB | 下载素材，自行过目 |
| `D:\AppData-Moved` | 9.8 GB | 抖音/虎牙/QQ音乐旧应用数据，软件弃用即可删 |
| `D:\BaiduNetdiskDownload` + `百度网盘` | 7.1 GB | 网盘下载数据 |
| 其余小项（steam 4.6 / WeGameApps / 迅雷 1.3 / huya_download / anime_downloads / 录屏 / 哔哩哔哩…） | ~10 GB | 娱乐/游戏数据，自行取舍 |

---

## 三、下次磁盘告警的处置 SOP

1. **先看两盘余量**：`Get-PSDrive C,D | fl Used,Free`
2. **对照本报告第二节**：告警盘的大头是否已被"别动"清单覆盖 → 是则只剩第二批候选可谈
3. **快速回收三板斧**（无需确认）：
   - `%LOCALAPPDATA%\Temp`（历史规律：AE/渲染后常积 20-40GB）
   - `D:\Ae25\磁盘缓存`（AE 媒体缓存，删前最好关 AE）
   - 回收站 `Clear-RecycleBin -Force`
4. **项目内**：`output/flagship_e2e_*` 空壳与 `tmp/` 媒体可周期性清（保留脚本/文档/当日产物）
5. **删除铁律**（run53 教训）：先物化打印待删清单核对，保留项白名单排除，禁止正则贪婪模式直接删

---

*扫描原始数据：`tmp/d_drive_scan.txt`（65 目录全量测量）*
