# Phase C 弃用端点命中统计 — 自动巡检报告

- 执行时间（UTC）：2026-09-07 01:53（本地 09:53 GMT+8）
- 仓库根目录：`C:\Users\Administrator\Desktop\AE-Knowledge-Vault`
- 规定巡检命令：
  - `python scripts/phasec_tally.py --date 2026-09-07`
  - `python scripts/phasec_tally.py --all`

---

## 一、执行结果：规定脚本缺失，命令无法运行

`scripts/phasec_tally.py` 在仓库中不存在（全局匹配 `scripts/phasec_tally.py` 与
`**/phasec*.py` 均无结果）。实际执行报错（证据）：

```
C:\Users\Administrator\.workbuddy\binaries\python\versions\3.13.12\python.exe: can't open file
'C:\Users\Administrator\Desktop\AE-Knowledge-Vault\scripts\phasec_tally.py':
[Errno 2] No such file or directory
---EXIT:2---
```

`--all` 变体同理无法运行。

按任务约束「不要修改任何代码或日志文件，仅统计与汇报」，本次**未创建**该脚本，
改用对 Phase C 真实观测数据源的直接核查作为替代统计。

---

## 二、Phase C 弃用机制与观测数据源核查

Phase C 弃用端点中间件确实存在于代码中：

- `web/api_server.py` 第 500–571 行：`deprecation_middleware`，每次命中弃用端点向
  `logs/deprecated_hits.jsonl` 写入 `DEPRECATED_HIT` 结构化记录，字段：
  `event / service / path / method / status / canonical / ts`；service 标记为 `mgmt-api`。
- `web/integrator_api.py`（约第 687 行起）：同款中间件，指向同一
  `logs/deprecated_hits.jsonl`。

被标记弃用、映射至网关规范路径（canonical）的端点清单（`_DEPRECATED_PATHS`）：

| service | path（弃用） | canonical（网关规范） |
|---|---|---|
| mgmt-api | /metrics | /metrics |
| mgmt-api | /api/v1/resources | /api/v1/system/resources |
| mgmt-api | /api/v1/alerts/active | /api/v1/system/alerts |
| mgmt-api | /api/v1/alerts/history | /api/v1/system/alerts |
| mgmt-api | /api/v1/compose | /api/v1/ai/plan-and-run |
| mgmt-api | /api/v1/tools | /api/v1/engines |

---

## 三、实际观测数据：命中记录为 0

- `logs/deprecated_hits.jsonl` **不存在**。该文件由中间件在**首次命中时惰性创建**；
  文件缺失即意味着自中间件部署以来从未发生过 `DEPRECATED_HIT` 事件。
- `logs/` 目录存在且有运行日志（`api-server_20260905/06/07.log`、`auth_*` 等），
  证明 API 服务一直在运行；但在全部日志中检索 `DEPRECATED_HIT / Deprecation / deprecat`
  **均无匹配**。

**结论：自 Phase C 观测窗口开启以来，弃用端点命中总量为 0**（不仅是今日 2026-09-07，
而是整个观测期）。

---

## 四、判定（对照任务四条规则）

1. **当日命中总量为 0 → 适用。** 报告：「今日无弃用端点残留调用，观测期推进中。」
   （且更强：全观测期 0 命中）
2. **当日有命中 → 不适用。** 0 命中，无需催促调用方切到 canonical。
3. **近 7 天趋势对比 → 受限。** 规定脚本缺失，无法从 `deprecated_hits.jsonl` 生成
   逐端点的 7 天趋势。基于现有直接核查：6 个弃用端点命中均为 0、无历史结构化数据。
   若要建立「持续 0 命中 → 进入下线流程（阶段 C 回归）」的正式判定，需先补齐
   `scripts/phasec_tally.py` 以形成基线趋势。当前可提示：所有 6 个弃用端点观测命中为 0，
   与「调用方已迁移至网关规范路径」的设计预期一致。
4. **未修改任何代码或日志文件，仅统计与汇报。**

---

## 五、行动建议（需用户在 Agent 模式手动处理）

- 在 `scripts/` 下创建 `phasec_tally.py`，使其：解析 `logs/deprecated_hits.jsonl`；
  `--date YYYY-MM-DD` 按 UTC `ts` 过滤并逐端点输出（service / path / canonical / hits）；
  `--all` 输出逐日 / 逐端点趋势。这是让本自动化按设计运行的前提。
- 补齐脚本后，本巡检即可给出严格的 7 天逐端点趋势，从而触发规则 #3 的下线流程建议。
