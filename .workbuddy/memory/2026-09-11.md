# 2026-09-11 记忆

## 【ZCode】P0 文字线 v6：字体分区轮换闭环（17:00-17:45）

- **Boss 反馈**: "字体不行，一直用一样的字体，审美疲劳了"。
- **根因（比预想更直接）**: `build_text_overlay.py` 事件字体恒为 `st["fonts"][0]` ——
  drop 区所有词全用 Anton；Anton 无 CJK 字形 → AE 静默回退到同一个回退字体渲染全部
  JP/CN 词。所谓"轮换"从未生效。
- **探针（probe_font_pool.jsx，预设库 §8.1 set→回读→比对 PS 名）**: 26 字体全 =YES。
  铁律补充: **YES 只代表已安装可寻址，不代表字形覆盖** —— JP 词 (強発壊無縛臨韻 新字体/
  传统字形) 只能配 JP 字体 (Yu Gothic/MS Gothic/YuMincho)；简体字库大概率缺这些字形。
- **v6 实现（018f915）**: FONT_POOLS 按样式×文字系分池
  (drop 重笔画: YuGothic-Bold/Semibold/MS-PGothic + FZCuHeiSongS/SimHei + Anton/Impact/
  Hanson-Bold/BlackOpsOne；build 现代无衬线；intro·outro 衬线书法: Georgia/STKaiti/
  MaShanZheng/YuMincho)；按 (样式,文字系) 独立游标轮换防背靠背。同词不同字体
  (最強→YuGothic-Bold→Semibold)。22 事件 16 种字体。
- **实测**: 注入零报错 (layers=26)；aerender 中文模板名
  **「最佳设置」/「H.264 - 匹配渲染设置 - 15 Mbps」**（英文 "Best Settings" 报
  No template found；`-mp 2` 该版本非法，去掉）；720 帧精确 60MB 1m36s。
- **验证**: VLM 16 帧网格 (v6_font_grid.png) 零乱码零豆腐块，14-16 种视觉可区分；
  luma 无事件帧与基底逐字节一致（无全局污染）。
- **aerender 警告「项目具有缺失字体」**: 出现在 v6 但 VLM 实拍字形全对——疑似基底工程
  其它资源或加载期字体检阅误报，不阻断；若后续版本字形异常优先查此。
- **AE 恢复流程（本日又走一遍）**: 启动对话框「崩溃修复选项」默认钮=继续 → AppActivate
  (PID)+SendKeys ENTER 一次即过（无链式对话框）；等 startup.log 新 "Polling started OK"；
  基底重置 base_reset.jsx (close DO_NOT_SAVE + open pristine) → layers=4。
- **产物**: run53_text_v6.aep / text_overlay/render/run53_text_v6_full.mp4 (60MB) /
  v6_font_grid.png + 16 单帧。tmp/extract_v6_frames.py 可复用。

## 【ZCode】v6→v7 字体真相三部曲（18:30-19:50, Boss 验收反馈驱动）

- **Boss 反馈**: "特效是不是重复添加了，视频层被影响了，字体有些亮度过剩了"。
- **审计澄清**: 26 层特效清单逐层核对——drop 文本层恰好 4 特效无重复、基底 4 层（含视频层）
  与原始一致、animator 无重复；无事件帧与基底逐字节一致。"视频被影响/重复添加"实为
  默认替换字体（密集黑体）×发光阈值115×bloom半径80 的大面积光晕洗白感知。
- **根因①（v6 完全失效的真相）**: **AE 只解析 HKLM 系统字体，LOCALAPPDATA 用户目录字体
  全部不可见**（本机 2692 个 USER 字体含全部 Google 系）→ Anton/BebasNeue/Antonio/
  BlackOps/Noto 等 **v1 起从未真正渲染**，全被替换成同一默认字体——"一直用一样的字体"
  的完整根因是双层（代码恒取 fonts[0] + USER 字体不可见）。
- **根因②（探针方法论）**: 预设库 §8.1 set→回读探针**只是回显存储字符串**，不证明可解析
  （v6 探针 26 全 YES 但渲染全替换）。真相表 = fontTools 枚举全部 face（含 TTC 多 face）
  的 nameID 6 + 文件位置 HKLM/USER。终极判定 = **渲染差分探针**（probe_render_fonts:
  每名一层渲染一帧→字形哈希分组，同组=同一替换体）——26/27 名全部真实可解析。
- **根因③（v6.1 部分失效）**: **动画范围选择器（TypeIn/Cascade 的 ADBE Text Selector
  offset 关键帧）与脚本设字体互斥**——切换逐字光栅化路径时字体静默回退默认体。
  二分实证: punch tracking/elastic/无动画层字体全部正常；重设字体救不回（层永久污染）；
  先动画器后设字体会**卡死 AE 渲染线程**（本日杀过一次进程）。
- **v7 修复（ef0cb37）**: ① FONT_POOLS 全 HKLM 真 PS 名
  （Impact/Haettenschweiler/Arial-Black/FranklinGothic-Heavy/YuGothic 四重/
  MS-PGothic/FZCHSJW--GB1-0=方正粗黑宋真名/STXihei/STKaiti/STXinwei/Rockwell/
  TrebuchetMS-Bold/TwCenMT-CondensedBold/AgencyFB-Bold/Georgia/微软雅黑/宋体）；
  ② drop 变款改整层变换（spin_z 翻入/drop_fall 下落/纯 punch）；③ 发光按文字系调制
  （CJK 阈值 115→175、强度×0.7、bloom×0.55；拉丁阈值→150、×0.85；入场脉冲 1.7→1.35/1.55）。
- **v7 验证**: 720 帧精确；无事件帧（25.2s/0.5s）与基底 0.00%；VLM 盲检（中性标签）
  同词不同字体生效（最強 YuGothic-Bold≠MS-PGothic、爆発 MS-PGothic≠YuGothic-Medium）、
  同字体对照一致（BREAK Impact 两现同款）、零乱码；轻度刺眼仅加速/余韻两格（可再调）。
- **手法库**: aerender 模板中文名「最佳设置」/「H.264 - 匹配渲染设置 - 15 Mbps」；
  `-mp` 该版本不带参数；renderQueue timeSpan 必须**在 applyTemplate 之后设**（否则被覆盖）；
  AE 启动对话框 dismissal——AppActivate(PID) 对空标题窗口不可靠，**全局 keybd_event(0x0D)**
  一次即过；字幕帧对照设计要点：对照窗必须真无事件（26.5s 有 #19 爆発，25.2s 才干净）、
  同词两现是字体隔离证明的最小实验。
- **产物**: run53_text_v7.aep / run53_text_v7_full.mp4 / v7_grid.png + 12 目视帧。
  诊断脚本: tmp/font_truth_table.py / tmp/pixdiff_v5_v6.py / tmp/verify_v61.py / tmp/verify_v7.py。
- **待办**: 手册§附D/E（cascade/typewriter 配方）与预设库§8.1（回读探针）需补勘误条目；
  Mimosa 预存风险（config.py 凭据×7 等）待专项。

## 【ZCode】v8/v9 观感修复：量化"视频被影响"（20:20-20:45, ef0cb37→3b31053）

- **Boss 复验"还是没解决问题"** → 不再靠猜，做**全片逐帧亮度扫描**（64x36 降采样
  rawvideo 逐帧均值 vs 基底）：107/720 帧整帧提亮 ΔYAVG>3，峰值 **+13.02**，
  全部落在文字事件窗 → 客观坐实"视频被影响"：白字墨量+双发光覆盖画面 20-30% 面积。
- **同帧目视定位三缺陷**（v9_stack_*.png 三代对比）：
  ① Glow+Bloom **两个发光叠加** = Boss"特效重复"的观感本质（审计"无重复特效"字面成立，
     但视觉上是双发光）；
  ② spin 旋转变款在动画中段文字倾斜 -64° + **左侧超出画框** + 压在高光爆炸上（白字白底）；
  ③ Impact 宽字 + tracking 250 在 punch 展开期 5 字母宽达 ~1280px，**左端被切**。
- **v8**: 删 Bloom；CJK 发光 115/25/1.7→185/14/×0.62；拉丁→158/16/×0.78；描边 6→5。
- **v9**: 去旋转变款（改纯平移 drop_fall/rise_up）；tracking 250→110；字号 215→196；
  候选位内收 620/1300→700/1220；亮度选位**加 0.12h 早期采样**（爆炸高光 0.13h 就爆，
  原 0.35h 起步全漏——这是 v4.1 选位逻辑的隐藏盲区）。
- **客观验收（三层指标同口径）**: 超标帧 106→71→**51**；峰值 +13.02→+9.19→**+8.26**；
  23.33s BREAK / 18.75s IMPACT 同帧目视：糊光/倾斜/超框 → 锐利/水平/完整落暗区。
- **教训（通用）**: ① "全帧均值"指标天然被白字墨量抬高，判"污染"必须同帧目视+分区域；
  ② 变款设计必须验证**动画中段帧**（起止帧好看≠中段不超框/不压高光）；
  ③ 位置避让的采样窗必须覆盖事件全程早期（爆发型高光在 <0.2h）。

## 【ZCode】v10/v11 彩光闭环：解决"有些字发光看不出"（20:45-21:00, 727be41）

- **Boss 反馈**: "还是没解决问题，有些字段发光看不出"。
- **量化诊断**: 逐事件在**读字期内**对文字落位区域采样（0.12h/0.45h/0.80h 三点取最大=最坏）:
  **22 个事件 19 个落在亮背景**（最坏亮度 150-254）——这部片子白/亮画面极多；
  亮底上白字+白辉光**物理上不可能可见**（白加白）。且同一位置亮度在保持期内大幅跳变
  （如 #13 IMPACT 82→253→74），所以时而清楚时而糊 → "有些字发光看不出"。
- **v10 对比度感知样式**（三档，按最坏亮度）:
  bright(≥150): 粗描边9 + 重投影 + 光晕 ; mid(≥90): 描边7 + 暖金光晕 ; dark(<90): 薄描边3.5 + 白辉光。
- **v11 Glo2 彩色光晕实证**（探针扩展）: **0007=发光颜色模式（默认 1=原色 → 忽略 A/B！）**,
  **0012=颜色A / 0013=颜色B**（默认 A=白/B=黑 → 这就是"光永远是白的"的原因）。
  亮底设青色 A=[0.10,0.80,1.0] B=[0,0.15,0.45]; 中间调暖金; 暗底保持白。
- **客观验收**（光晕带 RGB 均值，青色增益 B−R）: 虚式(亮底最坏) −6.1→**+16.0**;
  VIOLATION +7.3→+23.7; 無下限 +34.5→+44.9; 最強 +21.5→+31.9; 暗底爆発保持白辉光 +57.3。
  v11_grid.png 六格目视全部"可见光晕+清晰可读"。
- **知识库勘误候选（补 §七 Glo2 之外）**: Glo2 彩光三参数（0007 模式 / 0012 A / 0013 B）
  此前未知；另记 propertyValueType=TWO_D/THREE_D 可辅助识别颜色型参数。

## 【ZCode】文字高级化能力探针：描边体系与三维层（21:00-21:05）

Boss 提出"字体描边 + 三维层怎么推进，需要更高级的设计" → 实机探针摸清 AE 2025 能力边界
（方案落 `09-计划文件/2026-09-11_文字高级化_描边体系与三维层推进方案.md`）。

**探针实证（四条铁律级结论）**:
1. **图层样式无法脚本启用** — `layer.property("ADBE Layer Styles").property("frameFX/enabled").enabled = true`
   报 "canSetEnabled 为 false"；样式子属性（color/size）随之全被隐藏（setValue 被拒）。
   绕行: `layer.applyPreset()` 存在（.ffx 预设可脚本应用；仓库当前 0 个 .ffx，需产出资产）。
2. **文字挤压/倒角属性被门控** — 几何选项 [ADBE Extrsn Options Group] 的 5 子属性（斜面样式/方向/
   斜面深度/洞斜面深度/凸出深度）可**读**（val=0），但 setValue/expression 均报
   "属性或父级属性被隐藏"。替代: UI 验证渲染器设置 / .ffx / 多层 Z 偏移假挤压。
3. **文字动画器描边属性全部可用** — ADBE Text Stroke Color / Stroke Width / Stroke Opacity /
   Fill Color / Fill Opacity 全 OK → 多描边/变色字有脚本路径（但必须过字体真身验证，
   因动态 RS 选择器会静默替换字体）。第二动画器叠加同属性报"对象无效"，待解。
4. **高级 3D 渲染器已默认** — comp.renderer = "ADBE Advanced 3d"（setter 似无效=已是）；
   3D 文字层 / 摄像机 / 灯光 / 17 项材质参数（反射/高光/金属/菲涅尔/折射）可枚举。
   TextDocument 高级字段: lineJoin/miterLimit/strokeJustification = UNDEF（文档级不支持）。
5. 手册 §八（3D图层与摄像机推拉/环绕/Z视差）与 §十五（3D翻转文字）均为已实证配方，可直接迁移。

**推进波次**: W1 描边体系（双描边/动画器补描边/脉动）→ W2 三维层基础（摄像机推拉环绕/Z视差）→
W3 高级 3D（解门控/真挤压或假挤压/材质灯光/景深）→ W4 叙事化分级设计。待 Boss 决策 D-1~D-3。

## 【ZCode】W1 双描边 + W2 三维层 落地（21:05-21:25, 89ec9a4 / 6ee14dc）

**W1 双描边贴纸感（89ec9a4）**
- 探针实证图层样式描边无法脚本启用（canSetEnabled=false）→ 双文字层等效：
  底层=强调色实心外环（bright 青 16px / mid 金 13px），上层=白字+深色内描边（4/3.5px），
  上层 parent 到底层（变换动画只施加底层，opacity 与文字级 animator 两层各一份，
  效果只挂上层）；暗底保留 v11 单层白辉光（按分区切换）。
- build_jsx 重构为 helper（setDoc/addKick/addTrack/addMove）+ dbl 分派。
- 验收: layers=46 零报错；外环青色增益 B-R 提升（虚式 +16.0→+34.7）；亮度不劣化；目视六格可读。

**W2 三维层纵深（6ee14dc）——含一条重要否决结论**
- **摄像机方案被实测否决**：合成内存在摄像机 → Advanced 3D 渲染器对**整帧重新合成**，
  无文字帧出现 0.48% 全画面像素差异（max 98）= 典型"视频被影响"风险。**今后该合成禁用摄像机**。
- 附带发现：摄像机 POI 的正确 matchName 是 **ADBE Anchor Point**（"ADBE Point of Interest" 为 null）；
  摄像机默认位置 [0,0,-2666.67]（=1920×50/36，1:1 映射）；camera options 含景深全套参数（W3 可用）。
- 替代方案（v14）：drop 事件 threeDLayer=true + z 三档轮换 (0/-140/140) 做纵深层次 +
  Z 位移动画（-170→0, 0.34s）做"由远及近"；3D 层 scale 表达式必须返回三维值。
- 验收: 无文字帧与基底 0.000%(>8) 差异 = 基底零影响；3D 纵深同帧差异 11.8-19.1%；
  亮度回归不劣化（+8.44 vs v12 +8.51）；渲染 1m58s（含 3D 层，仍低于 3 分钟预算）。

**待办（W3 候选）**：真挤压/倒角解门控（需 UI 验证或 .ffx）；材质+灯光；景深（摄像机方案被封，
若要走 DOF 需重新评估"整帧重合成"代价）；描边脉动（需动画器描边 + 字体真身验证）。

## 【ZCode】v15 冲击力包：Boss 判"劣质感/冲击力拉跨"后的路线修正（21:45-22:00, db673b3）

- **Boss 反馈**: "a（选真挤压路线），效果不是很好，有种劣质感，视觉效果和视觉冲击力特别拉跨"。
- **判断**: 实心彩色外环（W1 双描边）是"劣质感"主因 → v15 关闭 dbl（保留代码路径），样式回 v11 干净单层。
- **挤压门控终局实证**: 几何选项对文字/形状/2D/3D **一律脚本锁定**（pvt=6417 AE 私有类型，
  setValue 报"属性或父级被隐藏"）→ 真挤压只有两条路：一次性 UI + .ffx 预设资产，或放弃。
- **W3 先行替代 —— v15 冲击力包**（内建效果，探针参数实证）:
  ① comp.motionBlur=true + 逐层 motionBlur=true（基底层开关未开 → 零影响，复检 0.000%）
  ② 方向模糊 ADBE Motion Blur（0001=方向/0002=长度）：入场 45-90 → 0.14s 收干；出场 30-60 拉出
  ③ 径向模糊 ADBE Radial Blur（0001=数量/0002=中心）：入场 12-32 → 0.16s 归零
  ④ 湍流置换 ADBE Turbulent Displace（0002=数量/0003=大小/0006=演化表达式）：保持期微流动
  参数随 energy 线性调制。
- **验收**: layers=26 零报错；入场帧实测拖影 + 放射模糊（BREAK 帧带彩虹边缘）；
  保持期清晰微流动；基底 0.000% 零影响；亮度 +8.94（v11 +8.51 未劣化）；渲染 1m23s。
- **经验（效果可用性速查，已实证）**: 内建效果 matchName 与参数索引 ——
  ADBE Motion Blur(方向模糊)/ADBE Radial Blur/ADBE Turbulent Displace/ADBE Ramp/ADBE Set Matte3/
  ADBE Tint/ADBE Shift Channels/ADBE Find Edges/CC Force Motion Blur 全部可 addProperty。

## 【ZCode】关闭基底发光（21:58-22:05）— Boss 直接指令"把视频的发光效果去掉"

- 执行: `.ae-mcp-bridge/kill_footage_glow.jsx` 禁用基底 3 处 ADBE Glo2 —
  素材层 run53_final_v43.mp4/发光（主因）+ PART_Burst_ShotSync/发光 + PART_Ambient/发光。
  （注: .ae-mcp-bridge 属 gitignore, 脚本不入库, 同目录其它探针一致）
- 实测: 全片平均 ΔYAVG **-7.27**（峰值 -24.74），500/720 帧暗 >3 → 证实素材层发光长期
  洗白画面（高光成片糊白）；对比帧显示去光后眼睛/脸轮廓与层次恢复。
- 仅置 enabled=false（**可逆**，随时 enabled=true 恢复）；**文字层发光未动**（保留先前要求）。
- 产物: run53_text_v16.aep / run53_text_v16_full.mp4（720 帧，1m33s）。
- 待确认: 粒子层两处发光是否也要恢复（当前一并关闭）。

## 【ZCode】v17 高级文字动画：通道分离 + 冲击分级（22:15-22:30, 6c94a68）

- Boss: "保留粒子的光晕，继续推进高级文字动画的"。
- **基底发光配置现状（重要，跨会话必须知道）**: 素材层 run53_final_v43.mp4/发光 = **关闭**（Boss 指示）;
  粒子层 PART_Burst_ShotSync/发光、PART_Ambient/发光 = **已恢复开启**。
  注入前基底准备序列: base_reset → kill_footage_glow (全关) → restore_particle_glow (只开粒子)。
- **v17 新增**:
  ① RGB 通道分离 (chromatic aberration): 红/青幽灵层 (blend Add + parent 主层, 错位 ±12-28px
     → 0.20s 归零), 仅 3 个最强爆点 (#08 最強 / #12 ZERO / #15 RED, energy≥0.70) — 防同款疲劳。
  ② 冲击分级: 冲量按 shot_scenes motion 归一化调制 (impact_mul 0.82~1.18), 静止镜头收敛。
- **验收**: layers=32 零报错; 3 爆点入场帧红青错位明显; 14.07s 的整幅色差经**同帧原片核对为素材自带**
  (原片本身有强 anamorphic 色差) 非本轮引入; 无文字帧 ΔYAVG≈-19 (素材发光移除效果);
  渲染 1m35s。产物 run53_text_v17.aep / run53_text_v17_full.mp4。
- **手法库新增（可复用）**: 通道分离 = 幽灵层(纯色填充, 无描边) + blendMode=5(Add) + parent 主层
  + 位置关键帧错位衰减; 计数验证 layers = 26 + 2×chroma事件数。

## 【ZCode】v18 字体池换代（22:30-22:45, 69aa237）

- Boss: "把所有ae系统简单普通的字体更换继续推进"。
- **关键新实证（覆盖校验）**: fontTools cmap 逐字校验 — 装饰性中文字体
  **STHupo 琥珀 / STCaiyun 彩云 / STXinwei 新魏 / STXingkai 行楷 / FZCHSJW 粗黑宋 对日文词仅 9/17 覆盖**
  （缺 強/発/壊 等新字体字形）；而 **LiSu 隶书 / DengXian-Bold / STSong / STZhongsong / STFangsong /
  STXihei / SimHei / KaiTi / STKaiti / SimSun / YouYuan / AlibabaPuHuiTi / YuGothic系 / MS-Gothic系 = 17/17 全覆盖**。
  → 池设计铁律升级: **jp 池只收 17/17 全覆字体, cn 池才能用装饰字体**（此前的"JP 必须日文字体"是过严假设）。
- **可脚本无损升级**: HKLM 里另有一大批从未用过的个性字体（隶书/琥珀/行楷/新魏/彩云/中宋/
  CooperBlack/BodoniMTBlack/GillSans-UltraBoldCondensed/ShowcardGothic/Stencil/AgencyFB/
  BernardMT/CenturyGothic/CopperplateGothic/Rockwell-ExtraBold/BerlinSansFB/GloucesterMT）→ v18 全面换池。
- **渲染差分验证升级版**: 46 字体 + 1 故意无效名对照 → **47/47 字形唯一**（对照组独立成形）→ 证明全解析。
- 新增同词去重（同一个词再次出现必换字体）。
- **未用上的更大字库**: 用户目录 743 个展示级 face（Anton-Regular/BebasNeue/BlackOpsOne/HansonBold/
  Kanit/方正魏碑 FZWBFW--GB1-0/超粗黑 FZCCHFW--GB1-0/汉仪超粗宋 HYa0gj/长城粗魏碑 GBWeiBei-Bold…）
  **需一次性 UAC 提权**安装到 HKLM（当前 1 号 shell 非提权: 写 C:\Windows\Fonts 与 HKLM 均 DENIED）。
  安装脚本已备: tmp/install_display_fonts.py（27 字体，可逆: 删文件+注册表项）。
- 产物: run53_text_v18.aep / run53_text_v18_full.mp4 / v18_grid.png（16 格字体对照）。

## 【ZCode】v19 字体资源全面接入（23:00-23:30, e009b23）

- Boss: "4到11秒的字体没改，怎么把我的字体资源利用到，给你所有权限完成任务"。
- **UAC 提权成功**: `Start-Process -Verb RunAs` 执行 tmp/install_fonts_full.py → 28 个展示级字体
  装进 HKLM（可逆: 删 C:\Windows\Fonts 文件 + HKLM 字体注册项）:
  拉丁 16 个 (Anton-Regular/BebasNeue-Bold/Antonio-Bold/BlackOpsOne-Regular/Bangers/
  AlfaSlabOne/BowlbyOneSC/Blanka/BungeeShade/HansonBold/Kanit-Black/Kanit-ExtraBold/
  Bumrush/321impact/BRUSHSTRIKE/ArtBrush);
  中文 12 个 (FZWBFW 魏碑/FZCCHFW 超粗黑/FZH4FW 粗黑/FZHPFW 琥珀/FZHTFW 黑/FZPHFW 平黑/
  FZSSFW 书宋/FZKTFW 楷/FZSTFW 舒体/GBWeiBei-Bold/HYa0gj 汉仪超粗宋/SungtiEG-Ultra-GB)
  未装成 4 个: BebasNeue-Regular.ttf、CTCuHeiSJ/CTChaoHeiSF/CTBiaoSongSF (文件名与预期不符)
- **AE 重启后渲染差分验证: 27/27 字形唯一** → 全部真实可解析。
- **字形覆盖复验**: 新中文字体对中文词 11/11、日文词 7/14 → 铁律不变: jp 池只收 17/17 全覆字体。
- **build 段 (4-11s) 未变的主因**: 字号小 (93-96px) + 所选字体彼此接近 → v19 换
  BebasNeue-Bold/Kanit-Black/平黑/楷体/等线粗 + 字号上调到 106-109px; 实测该段逐帧差异 0.9-1.06%。
- **运维坑**: JSX renderQueue 渲染结束会弹「渲染完成」模态框 → **阻塞脚本监听器**（本次连超时 3 次）。
  规避: 探针渲染后先全局 keybd_event(0x0D) 清弹窗, 再发下一条命令。
- 产物: run53_text_v19.aep / run53_text_v19_full.mp4 (720帧, 1m04s) / v19_grid.png。
- 安装脚本: tmp/install_fonts_full.py（日志 tmp/font_install.log）

## 【ZCode】文字动画特效调研（经本地代理全面采集, e8552ab）

- Boss: "本地配置了代理，更全面搜集，集思广益" + 限定范围"只研究文字动画特效制作"。
- **代理发现**: Clash Verge 混合端口 `127.0.0.1:7897`（`verge-mihomo` PID 19016）；环境变量无代理。
  用法: `curl -x http://127.0.0.1:7897` 或 urllib ProxyHandler。**arXiv / GitHub API 可达；Adobe helpx 全路径 403**。
- **安全写法（Mimosa 两次拦截后的定式）**: ① 不要 subprocess+动态 URL（判命令注入）→ 用
  `urllib.request` + ProxyHandler; ② XML 解析前拒绝 `<!DOCTYPE/<!ENTITY` 并限大小（判 XML 实体扩展）;
  ③ URL 白名单 + DNS 结果公网校验（拒绝环回/私有/保留）。
- **学术（9 篇一手摘要）**: 2407.10476 Kinetic Typography Diffusion Model（584 专业模板/600K 视频；
  效应族 flying/glitches/chromatic aberration/reflecting；三要求 审美·动效·可读）；
  2308.00224 Wakey-Wakey（GIF 刚体运动→关键点轨迹→局部仿射→文字控制点 + 拓扑保持 = **最高可迁移**）；
  2404.11614 Dynamic Typography；2406.17777 Text-Animator；2306.07560 Emordle（情绪两旋钮 entropy/speed）；
  2407.14774 艺术排版综述（**双轴: 艺术化样式 vs 语义排版**）；1905.03646 TE141K（152 专业效果）；
  1611.09026 Awesome Typography；2502.10999 ControlText。
- **工程**: iart motion-skills/kinetic-typography-skills（skill 形态 + mask/blur/clip-path 揭示 + 先静态后动态）；
  bang-motion（menus-not-defaults/同选择不重复/确定性时间线/两级文字/字号下限）；
  pv-tool（16 模板 54 效果分类；**许可已改 Non-Commercial → 仅借鉴概念**）；
  after-effects-mcp（comp 状态读取+截图自检+house-style.md+分层更新）；nautilus（按 transform 拆关键帧，
  3D 文字未支持，与我方"几何选项被门控"互证）；pycaps（CSS 渲染引擎=按文件路径取字体的逃生舱）。
- **后续可落地（本报告 W1-W5）**: 字体注册表+分级白名单 → mask 揭示 → 动作迁移（镜头/节拍驱动文字位移）
  → 情绪两旋钮 + 不重复硬规则 → 后处理族试点。
- 报告: `09-计划文件/2026-09-11_文字动画特效调研与字体资源利用报告.md`；采集脚本 tmp/collect_{arxiv,deep,docs}.py。

## 【ZCode】W1 字体体系化落地 + v20（2026-09-12 00:10-00:45, b910cbe/3ed9af6）

- Boss: "开始"（执行报告 W1：字体注册表 + 分级白名单批装）。
- **字体注册表** `scripts/build_font_registry.py` → `schemas/font_registry.json`（1MB, 已入库）:
  扫描系统+用户全部 face = **3075**（系统 415 / 用户 2660）；授权风险 **68**；
  字形全覆盖: 拉丁 2750 / 中文 941 / 日文 281。四维事实: PS名/位置/授权风险/覆盖。
- **L1 白名单** `scripts/build_font_whitelist.py` → `schemas/font_whitelist_l1.json`:
  预设库类别 PS 名 ∩ 注册表可装 ∩ 非授权风险 = **84 个**（哥特/复古装饰/手写签名/速度动感/
  街头涂鸦/马戏团童趣/Lato·Inter·Kanit·Brandon Grotesque·Merriweather·Alegreya Sans SC 全字重）。
  排除: 已在系统 85 / **授权风险 3**（InkFree、MegaFresh、CocogoosePro-Thin）/ 未安装 54。
- **批量安装** `scripts/install_fonts_l1.py`（提权）→ 84 个装入 HKLM，日志 tmp/font_install_l1.log。
- **授权风险清理** `scripts/uninstall_risk_fonts.py`（提权）→ 卸载早前误装的 **BRUSHSTRIKE trial**
  （删 C:\Windows\Fonts\Brushstrike trial.ttf + 注册表值）；已从 VERIFIED_FONTS 移除。
- **验证**: L1 抽样 20 名字形哈希全唯一（20/20）。
- **v20 池扩展**（新字体置于池首以便上片）: 冲击 BebasKai/Brat/BroadcastMatter/FasterOne/MetalMania;
  铺垫 Lato-Black/Inter-Black/BrandonGrotesque-Black/AlegreyaSansSC-Black; 收尾 DrSugiyama/GreatVibes/
  Allura/Asset/AutourOne。实测分配: THE STRONGEST=DrSugiyama(毛笔) / VIOLATION=Lato-Black /
  LIMIT=Inter-Black / BREAK=BebasKai / ZERO=Brat / IMPACT=BroadcastMatter / RED=FasterOne /
  BREAK=MetalMania / THE END=GreatVibes / 五条悟=FZ书宋 / 虚式=FZ超粗黑 / 最強=LiSu隶书。
- **工程改进**: 基底光效规范化并入注入 JSX（素材层 Glo2=OFF / 粒子层=ON）——独立 bridge 调用
  启用粒子发光会卡住超时（两次实测），现由注入统一保证；`check_glow_state.jsx` 可核验。
- **运维坑（新增）**: `python send_bridge.py <jsx> 120` 的位置参数**被忽略**（仍为默认 60s）→
  必须用 `python -c "send_script(p, timeout=N)"`；aerender 渲染时长随粒子发光开启上升（v20 ≈ 2-3 分钟）。
- **勘误**: 预设库称 AlegreyaSansSC"支持中文"，实测 cmap 纯拉丁（0/0/18）→ 以实测为准。

## 【ZCode】W2 遮罩揭示落地（2026-09-12 11:45-12:15, 37e21c1）

- Boss: "继续" → 执行报告 W2（业界最稳的 mask/clip 揭示, 替代被禁的逐字动画器）。
- **两条实证（都很有价值）**:
  ① **Linear Wipe 被否决**: 探针通过（`ADBE Linear Wipe` 0001=过渡完成 / 0002=擦除角度 / 0003=羽化,
     关键帧可用）**但作用于整帧 1920×1080 而非文字内容范围** → 文字只占一条 y 带,
     擦除线扫过时文字整块跳出（v21/v22 帧带实证）；且置于效果栈末尾时作用于被发光/模糊
     扩展后的层边界, 更不裁字。测量法: `comp.time=…` 读完成度 + `sourceRectAtTime` 读内容边界。
  ② **图层遮罩可用且字体安全**: 极简探针（mask atom + `new Shape()` 赋值 + 形状关键帧 keys=2）
     通过且 **AE 未崩溃**（此前那次崩溃与遮罩无关）。
- 实现: 按 `sourceRectAtTime` 文字边界建矩形遮罩; 形状关键帧 零高度→完整（wipe_up 上升 /
  wipe_right 横向）, 羽化 6px, 出场反向; 入场变款循环 `enter_cycle` 生效（5/22 事件命中:
  THE STRONGEST / 束縛 / LIMIT / VIOLATION / 余韻）。
- 验收: layers=32 零报错; 帧带实证 束縛@4.94s 上缘被裁切带羽化边 → 逐步完成（真揭示）;
  字体仍是 DengXian-Bold 无回退; 亮度与 v20 完全一致（峰值 ΔYAVG +6.75 / 超标帧 23）。
- **运维/方法补充**:
  · "无事件帧 vs 原始基底"指标已失效——交付基底按 Boss 指示移除了素材层发光, 与原始基底整体不同;
    现在正确的回归对照是"同参数版本之间"（如 v23 vs v20）。
  · AE 崩溃过一次（探针致?）→ 重启后拆小探针定位, 结论: 崩溃源非遮罩。
  · bash 直接写 .jsx 被 Mimosa 拦截 → 探针一律用 Write 工具落盘再送桥。

## 【ZCode】v24 观感回归（2026-09-12 12:35-12:50）

- Boss: "将视频层的发光效果去除（指令重申），v2(v23) 没啥实际效果，之前的描边和字体艺术化都没了，
  变成一开始的版本了"。
- **诊断（探针实测, 避免误判）**: 机制全对——文字层 Glo2 en=true；描边 9px；字体正确；
  基底素材层 Glo2=OFF（符合 Boss 设定）、粒子层 ON。→ 根因是**审美选择而非功能回归**:
  v20 起池首改为细/装饰字体（仿宋/花体/毛笔/BebasKai/Brat），笔画细 + 亮底光晕阈值 205 偏高
  → 整体显轻、显平，看起来像"回到早期版本"。
- **v24 修正**: ① 厚重冲击体回归池首（jp YuGothic-Bold/MS-PGothic/DengXian-Bold/LiSu；
  cn 超粗黑/汉仪超粗宋/琥珀/粗黑宋；latin Anton/BlackOpsOne/AlfaSlabOne/GillSans-UltraBoldCondensed/
  CooperBlack/Bangers/MetalMania），细花体后置；② 描边 亮底 9→11、中间调 7→9；
  ③ 彩色光晕 亮底阈值 205→185、强度 ×1.35；暖金 190→175、×1.20。
- **验收**: 三代对比图 `v17_v23_v24_cmp.png`（最強/BREAK/虚式 同帧）—— v24 描边与青色边缘光
  为三代最强，厚重感回归。素材层发光确认 OFF。
- **教训（写进方法）**: 换字体池时必须看"字重/笔画密度"这一维——同是"展示级字体"，
  细花体与超粗黑的观感差异极大；池首顺序直接决定成片观感（每池只用到前几个）。

## 【ZCode】v25 词库外置 + 4-8s 字体升级 + 音乐联动（2026-09-12 12:55-13:05）

- Boss 三问: 字体现在能用了? / 4-8 秒字体不达标 / 文字内容可否后续变更 + 如何随音乐共同演绎。
- ① **词库外置**: `data/text_overlay_words.json`（脚本缺文件时自动生成）。
  优先级: `--words-json` > 该文件 > 内置 DEFAULT_WORDS。改文字后**重跑脚本即生效**（确定性: 同词同乐同结果）。
  文字层仍是 AE 真实文本层（可手改），但可复现路径是重跑管线。
  注意: 词与事件是"按列表循环"映射；若要"指定词配指定时间"，需要加 per-event 覆盖（待办）。
- ② **4-8s 段字体升级**（原等线/平黑/Lato 被判不达标）: jp → LiSu/DengXian-Bold/YuGothic-Bold/STZhongsong;
  cn → 方正琥珀/方正舒体/STHupo/方正楷; latin → HansonBold/BrandonGrotesque-Black/Kanit-Black/321impact。
- ③ **音乐联动（确定性烘焙，无随机）**:
  · 发光强度随 `music_envelope` 逐帧呼吸 → 每事件 **8 个**强度关键帧（探针实测数值随音乐变化）
  · **节拍脉冲缩放**: 事件窗内最多 3 个最强 onset，幅度 4-14%（按强度），0.12s 回落；
    弹性 drop 层跳过（其律动由发光呼吸 + 弹性表达式承担）
  · 既有: onset 锚点对齐 ≤2 帧、能量→字号、冲量按镜头运动分级
- 验收: 720 帧；探针: 发光 KF=8 / 非弹性层缩放 KF=6 / 弹性层表达式；4-8s 段目视提升。
- 方法备注: 同事件多帧的"区域亮度极差"不能作为呼吸证据（背景运动主导）——
  呼吸的权威证据是关键帧数值本身（探针读取）。

## 【ZCode】W3 运动驱动漂移（2026-09-12 13:10-13:25, 245e1b9）

- Boss: "继续" → 执行 W3（运动迁移）。
- **关键负结果（省后人重复投入）**: 三种镜头运动矢量估计在本素材全部不可信 —
  cv2 相位相关（位移 -153~-455px，置信 0.004~0.015）/ 中值光流 Farneback（前后景反向运动抵消，
  中值 |v|≈0.00，而帧间差 4~80 证明帧确有运动）/ cv2 ECC 欧氏配准（cc=0.03~0.55，位移 +340/-382px 离谱）。
  根因: 平均每 0.8s 一切点 + 粒子/爆炸 + 前后景混合运动。→ **不做伪矢量**。
- **实际实现**: 用我方管线自产的镜头运动标量 `shot_scenes.motion`（0.03~10.51）驱动保持期**视差漂移**:
  幅度 12~30px（drop ×1.2），方向取 8 向确定性循环（防重复），安全边距门禁（估文字半宽/半高）。
- **验收（权威证据=关键帧数值，非像素质心）**: 余韻 -21 / THE END +26 / 爆発（与 drop_fall 的 Z 下落
  叠加）(-19,+19) / BREAK (+18,+18) 且保住 3D 的 Z=140 / 最強 (-18,-18) —— 逐项与事件表一致。
- **方法教训**: 像素质心法测位移在本素材不可用（背景亮区把中值拉偏，出现 -194/-230 之类离谱值）；
  位移类改动的权威验证是**直接读属性关键帧**。
- 报告已补"W3 负结果"附录（含三法实测数据与替代方案）；若要真矢量迁移需特征点+RANSAC（标注待验证）。

## 【ZCode】W4 硬门禁 + 情绪旋钮（2026-09-12 13:35-14:00, 提交见上）

- Boss: "继续" → 执行 W4（报告最后一项: 设计规则化 + 情绪调参）。
- **四条硬门禁**（每次 dry-run 打印, 是断言不是声明）:
  · 同款不连打-字体（相邻事件字体不得相同）
  · 同款不连打-变款（drop 层按 drop_fall/rise_up/punch 三变款判定, 因 drop 的 enter 恒为 punch_tracking）
  · 两级文字（实测并发事件数 = 1, 上限 2）
  · 字号下限（≥60px @1920 宽; 实测最小 106）
- **情绪档案 + 三旋钮**（CLI 化）: `--mood {auto,impact,elegant,kinetic}` / `--entropy 0~1` /
  `--speed` / `--seed`。elegant = 关通道分离 + 柔光 + 快入场 + 小漂移; kinetic = 强脉冲 + 大漂移 + 关揭示。
  实测 elegant 档 layers=26（auto 32）证明档案真正生效。
- **诚实口径**: entropy<0.35 时"变款不连打"标 N/A（低熵=显式要求固定变款, 不是缺陷）。
- **Mimosa 交互记录**: `random.Random` 被判"弱随机"。此处**必须**确定性可复现（同 seed 同输出）,
  换 secrets 会破坏可复现性 → 保留并加注释说明用途（非安全场景）, 这是正确的取舍。
- 产物: run53_text_v27e.aep/_full.mp4（elegant 演示）+ v26_vs_v27e.png（同一音乐两种演绎对比）。

## 【ZCode】v28 时间-文字对应表（2026-09-12 13:45-14:00, 46d00f9）

- Boss 确认需要"指定某句话出现在指定时间"。
- 实现: `data/text_overlay_timeline.json` = { "timeline": [{t, word, font?, note?}] }。
  · **匹配**: 与事件锚点 t_in 距离 ≤ 0.6s 即命中（容差常量 TIMELINE_TOL）
  · **优先级**: 时间表 > 分区词库列表循环（未命中处仍走列表）
  · **字体指定**: 新增 `JP_SAFE_FONTS` / `CN_SAFE_FONTS` 白名单（依据实测 cmap 结果）与
    `font_allowed_for_script()`；须在 VERIFIED_FONTS 且覆盖该词文字系，不合规→告警回退（不静默）
  · **显式对账**: 构建时打印 载入/命中 数，未命中条目点名告警；事件表新增 from_timeline/font_override
- 验收: 7/7 命中 + 字体指定全部生效（最強=LiSu / BREAK=Anton-Regular / 無下限=DengXian-Bold /
  IMPACT=BlackOpsOne-Regular / 虚式=FZCCHFW / 余韻=STFangsong）；layers=32；720 帧；W4 门禁仍全过。
- 产物: run53_text_v28.aep/_full.mp4 + v28_timeline_grid.png（时间表句逐条抽帧）。
- 已知限制（诚实记录）: 时间表只影响"用哪个词/字体"，不影响事件锚点本身（锚点仍由节拍决定）；
  若想让某句必须落在任意时间（无节拍处），需扩展为"可插入人工锚点"，未做。

## 【ZCode】AE 已装插件/脚本盘点与可用性判定（2026-09-12 14:30-15:00）

- Boss 问: 描点判断能否借助已装 AE 脚本? 有可配合开发的吗? 你能调用吗?
- **盘点**: ScriptUI 面板(TypeMonkey/AIT/Animation Kit/Motion 2/AutoSway/Projection 3D/Duik…) +
  商业套件(Boris FX Continuum / Sapphire / Trapcode / Red Giant + Digieffects/Digital Anarchy/
  Flicker Free/Lockdown/Image Lounge/Effects) + aescripts 20+ 带 .lic + CEP(MCPBridgeCEP 等)。
- **可脚本驱动（已实证）**:
  · **Sapphire 16 效果** addProperty 全部成功无弹窗（S_Glow/S_BlurMotion/S_BlurDirectional/
    S_WarpChroma/S_FilmEffect/S_Grain/S_LensFlare/S_Rays/S_Shake/S_LightLeak/S_Feedback/
    S_EdgeRays/S_Kaleido/S_Distort/S_DissolveLuma/S_WipeClock）；HKLM\SOFTWARE\GenArts 存在
  · **Aescripts DeepGlow** 加载成功(90 参数) + 单帧渲染正常光晕**无水印**；aescripts 授权目录含
    DeepGlow/RealGlow/LongShadow/AEPixelSorter2/CartoonMoblur/FastCameraLensBlur/Bokeh/Diopter/…
  · Sapphire 参数关键名: S_Shake-0050 Amplitude / 0051 Frequency / 0110 Stillness / 0111 Twitch /
    0113 Drift / 0054 Motion Blur / 0059 X Rand Amp; S_WarpChroma-0054/0055 位移 / 0100 Warp Amount
  · DeepGlow 参数(PEDG-): 0001 半径 / 0002 曝光 / 0004 阈值 / 0005 阈值平滑度
- **不可用（已实证负结果, 重要）**:
  · **Boris FX Continuum**: addProperty → "Boris FX - Continuum Error" 模态框**阻塞 AE**（2 次卡死重启）；
    HKCU\SOFTWARE\Boris FX 有记录但 Continuum 报错 → 需修复/激活
  · **Red Giant**: 弹授权对话框阻塞（窗口枚举实证）→ 注册表无授权键 = **未激活**
  · 教训: **不要盲探商业插件**（会弹模态框卡死自动化）；探测前去查授权文件/注册表
- **对描点判断的结论**: 第三方节拍工具当前都调不动；我方 Python 节拍链已实证（onset ≤2 帧/节拍脉冲/
  发光呼吸）且确定性可复现 → 不支持改依赖第三方。第三方独有价值两处: BCC Extruded Text（真 3D 挤压）+
  Red Giant Cool Text/TypeMonkey（逐字引擎，绕开 RS 动画器×字体问题）。
- 报告: `03-阶段报告/AE已装插件脚本盘点与可用性判定_2026-09-12.md`（含待验证项）

## 【ZCode】v29 插件包接入 + 渲染阻塞负结果（2026-09-12 15:00-15:50, b76ecdd）

- Boss: "开始" → 接入零授权风险插件（按盘点报告 §三-1）。
- **已实现**: S_Shake（节拍驱动抖动, drop 层, Amplitude 按 beat 打 KF）+ LongShadow（intro/outro 长阴影）;
  新增开关 `TEXT_OVERLAY_PLUGINS=0/1`（插件异常时快速隔离）。
- **参数实证**: S_Shake-0050/0051/0054/0056; ADBE LongShadow-0005/0006/0014/0017/0018;
  S_Grain-0050/0051/0060（可用未接）; **AEPixelSorter2 的 matchName 无效**（"AEPixelSorter2" 报错, 待另找）。
- **负结果（关键）**: 插件**可注入但无法渲染** —— aerender 无头实例卡在 2.1MB 不动；
  交互实例 renderQueue 420s 超时无输出；两次都需强杀重启 AE。
  推断为**渲染期授权**（Red Giant 未激活 / Sapphire 渲染期许可未验证），归因标"待确认"。
- **同时修掉两个真 bug**:
  ① 爆点变款循环原用 `bank_pos % 3`，时间表命中时不推进 → 相邻爆点同变款；改用事件序号 `i % 3`。
  ② `plan_events` 里 `_tl = timeline or []` 直接 mutate 调用方列表 → 主流程时间表对账字段全丢；
     改为 `list(timeline or [])` 拷贝。**教训: 传参进函数后要拷贝再改**。
  ③ 时间表指定字体导致的相邻重复 → 归为软提示（font_repeat_from_override），不误判门禁失败。
- 交付: `run53_text_v29np_full.mp4`（插件关闭版, 720 帧 60MB, W4 四门禁全 True, 时间表 7/7 命中）。
- 待 Boss: 激活 Red Giant / 修复 Boris 后重评插件渲染路径（每次尝试前先确认授权状态, 避免反复重启 AE）。

## 【ZCode】v29b 插件路线打通：S_Shake 可用 + LongShadow 排除（2026-09-12 16:00-16:35, f9e9d55）

- Boss: "继续推进，没账号，不怕麻烦" → 把插件路线走到底。
- **方法：单一变量 A/B 隔离（这是本轮关键）**
  | 测试 | 结果 |
  |---|---|
  | LongShadow 单独（2D, 1帧） | ✅ 0 秒 |
  | S_Shake 单独（2D / 3D / +运动模糊 / +幅度KF） | ✅ 1-2 秒（**3D 也无问题**） |
  | S_Shake 在完整 22 事件工程 | ✅ **720 帧 60MB 全片成功** |
  | LongShadow 在完整工程 | ❌ 卡在 ~2.6MB（约 26-30 帧）需强杀 |
  | LongShadow + 遮罩/多效果组合（**构建期**） | ❌ JSX 执行即卡死、aep 未生成 |
  | 关 LongShadow 后同工程 | ✅ 100s 输出 27.8MB → 全片完成（**元凶确认**） |
- **结论修正**（附录 A 的"插件全不可渲染"是粗判）：S_Shake 可用并已交付；LongShadow 单独可用但与
  遮罩/效果组合会在**构建期**卡死 → 改为显式开关（`TEXT_OVERLAY_LONGSHADOW=1`，默认关）。
- **实现**: px_shake 默认启用（Amplitude 按 beat 起峰 0.14s 归零, Freq=8, MotionBlur on, Seed=7 固定）；
  `TEXT_OVERLAY_PLUGINS=0` 整体关插件。
- **验收**: v29b 720 帧 60MB；探针实证每爆点层 **7 个幅度KF 按节拍**（IMPACT 1.18@23.24→0@23.38→1.13@23.71…）；
  逐帧对比：爆点窗差异 1.4-4.0%、开场对照帧 0.01%。
- **方法论教训（写进规矩）**: ① 判"插件不可用"必须**单一变量 A/B**（否则会误杀可用项）；
  ② **构建期卡死**与**渲染期卡死**要分开测（本次两种都遇到）；③ 商业插件探测前先查授权状态。
- 遗留临时文件（无法删除, 被残留句柄占用）: render/run53_text_v29_full.*.m4v/.aac；下次重启后可清。

## 【ZCode】v30 冲击配方轮换（2026-09-12 16:36-16:50）

- Boss: "效果不错, 就是太单一了, 都用同一个效果了" → 与字体/变款单调同源问题(第三次出现)。
- **解法**: 4 配方轮换 (`impact_recipe` = ["shake","rays","chroma","feedback"][i%4]) + 配方内参数按 i 取模变化。
  · shake: S_Shake (Style 1-3 轮换, Freq 5/8/11, Amplitude 按 beat 起峰)
  · rays: S_Rays (长度/方向/亮度峰值 3.2→0, 中心=文字位)
  · chroma: S_WarpChroma (位移方向 ±, Warp Amount 0.02→0.55→0.02)
  · feedback: S_Feedback (Max Steps 10/14/18, Prev Brightness 0.55-0.79, Blur 2.5→0)
- **参数实证**(探针): S_Rays-0050 中心/0051 长度/0052 亮度/0100 反向; S_EdgeRays 同构;
  S_Feedback-0100 Max Steps/0050 Prev Brightness/0056 Blur Amount; S_Shake-0001 Style。
- 验收: 分布各 3; 720 帧 60MB 渲染成功无卡顿; 四配方抽帧差异显著(v30_recipes_grid.png)。
- **规律**(值得记): Boss 对"同一元素重复使用"高度敏感(字体→变款→效果都因此被打回)。
  凡是跨事件复用的效果/字体/动画, 默认就要做轮换或参数抖动。
- 可继续: feedback 的拖影会在保持期持续(可改为爆发后衰减); S_Grain/S_FilmEffect 待接入。

## 【ZCode】v31 六配方轮换 + 回授语义修正（2026-09-12 17:05-17:55）

- 承接"效果单一"反馈, 把冲击配方从 4 扩到 **6**: shake / rays / chroma / feedback /
  **edgerays(S_EdgeRays)** / **filmflash(S_FilmEffect)** → 12 爆点各 2 次且不相邻。
- **探针新增参数**: S_EdgeRays-0050 中心/0051 长度/0052 亮度/0100 反向;
  S_FilmEffect-0057 Print Exposure / 0063 Glow Brightness / 0056 Neg Exposure / 0050 Neg Film / 0051 Print Film。
- **feedback 语义修正（重要教训）**: Prev Brightness 固定 0.85 会让首帧以 85% 权重混合"上一帧(空)",
  导致爆发帧文字半透明/看不见; 改为关键帧 0.15 → 0.55 → 0.08（文字先可见→回授堆叠→衰减）。
  教训: **反馈类效果的 Prev Brightness 在入场时必须从低值起步**, 否则等于延迟显示自身。
- **迭代效率方法（新）**: 全片渲染 ~5 分钟 → 改用 `aerender -s <帧> -e <帧>` **只渲受影响帧段**
  （本次 25 帧=22 秒）做快速验证, 确认后再全片渲染。以后调参都先走帧段。
- **度量教训**: 判定"文字可见"的阈值要匹配实际（235 太严→误判不可见; 220 合理）;
  半透明白字（220-235 灰阶）在 JPEG 网格里不易察觉, 需用数值而非肉眼网格判断。
- 交付: run53_text_v31c_full.mp4（720 帧 60MB）+ v31_recipes_grid.png（6 配方×爆发/保持 12 格）。

## 【ZCode】v31d 同配方参数区分（自查发现, 2026-09-12 18:25-18:45）

- **自查发现真问题**: 配方内参数用 `i % k` 推导 → 同配方的两次出现 (i 与 i+6) 奇偶与模3 相同
  → 每种配方实际是"同款出现两次", 用户很可能再次感到重复。
- **修正**: 改用 `recipe_occ = i // 6`（按出现次数）驱动配方内参数:
  shake Style/Freq、rays 长度/方向/亮度(第二次转暖金)、chroma 位移方向/Warp峰值、
  feedback 步数/回授峰值、edgerays 长度/方向/亮度、filmflash 曝光/辉光/负曝光符号。
- 验收: 六配方 12 次出现的参数键全部互异; 720 帧 60MB; v31d_occ_pairs.png 逐对并排可见差异。
- **方法论（又一次同源教训）**: "轮换"必须验证**每个实例的参数确实不同**——只轮换"种类"不够,
  还要检查同种类内部是否因索引同余而退化。**索引同余陷阱**: i 与 i+周期 的高低位组合可能完全相同。
- 交付: run53_text_v31d_full.mp4 + v31d_occ_pairs.png。

## 【ZCode】v32 全局颗粒（Boss 批准, 2026-09-12 18:40-19:05）

- Boss 选"1"= 同意做全局颗粒（明知会动基底画面）。
- **实现**: S_Grain 加在**素材层**（Color Amplitude 0.38 / Frequency 12 / Bw 0.22 / Seed 0.37 固定）。
- **三条实证（关键）**:
  ① **调整层路线无效** — `addSolid + adjustmentLayer=true` 上挂 S_Grain **两次实测均不改画面**
     （Sapphire 效果在调整层上读不到下层合成）→ 改直接加素材层。
  ② **颗粒必须粗** — Frequency=100（极细）时**无损渲染有颗粒**（局部块 std 10.4→19.3, ×1.86）但
     **H.264@15Mbps 完全压掉**（mp4 对比 ×0.93-1.09, 看不出）；改 Freq 12-14 + Amp 0.55 →
     H.264 下仍可见（×1.46-2.15）。**结论: 交付走 H.264 时, 细颗粒等于白做**。
  ③ **振幅标定** — Amp 0.55 时全片均值偏移 ±3；降到 0.38 后 ±2（Sapphire 颗粒非零均值，属固有；
     若 Boss 觉得偏亮/偏暗，可用亮度补偿抵消）。
- **校验方法（新增, 值得沿用）**:
  · 局部块 std（64×64 多块均值）比"拉普拉斯/高频方差"更能反映颗粒感；
  · **必须 1:1 原尺寸裁切对比**——缩图会把颗粒抹平, 网格图看不出；
  · 无损(OM="无损")渲染用于区分"效果没生效"与"被编码压掉"。
- 交付: run53_text_v32e_full.mp4（720 帧 61.8MB, 比无颗粒版 +1.8MB）+ v32_grain_cmp.png（1:1 对比）。

## 【ZCode】v34 视觉编排（留白与对比）+ 颗粒停用（2026-09-12 19:10-19:35）

- Boss: "太乱了视觉上的观感，应该局部或者某些时间段应用…学习他人"。
- **学习他人（数据化, 关键）**: 对参照集抽样 8 部量化逐秒运动量 → 静段 31-89% / 强段 3-21% /
  最长连续强段 ≤3s / 峰均比 2.5-8.4；本片 v32e = 静段 0% / 强段 100% / 连续强段 29s / 峰均比 1.15。
  → **"乱"的本质是没有收放对比**，不是某个效果丑。
- **编排五原则**（P1 留白优先 / P2 短峰 / P3 同拍单主特效 / P4 情绪映射 / P5 安静拍更干净）:
  · 冲击配方改"隔拍"（drop 内 i%2==0）→ 22 事件仅 6 个特效(27%)、16 个留白
  · 安静拍的方向/径向模糊+湍流 ×0.65
- **颗粒停用（重要负结果, 三条实证）**:
  ① 细颗粒(Freq100) 无损可见(局部std ×1.86) 但 **H.264@15Mbps 完全压掉**;
  ② 粗颗粒(Freq12) 形成**大块明暗斑并整帧提亮**（4.5s 均值 57→127, 采样点趋同≈恒定偏移）→ 这才是"脏/乱";
  ③ 颗粒的 Color Amplitude **关键帧化未生效**（该版本行为与预期不符）。
  → 保留开关 `TEXT_OVERLAY_GRAIN=1`，待码率与颗粒尺度匹配后再评。
- **方法教训（新增两条）**:
  · **判定效果必须同帧 A/B**：跨帧/跨版本的"区域均值"比较会被内容差异淹没（本次一度误判）；
  · 局部块 std 只在"颗粒粒度 < 块尺寸"时有效；粒度大于块时需看**同帧区域均值漂移**才能发现斑块。
- 验收: v34 亮度与无颗粒基线 v31d 同帧完全一致（Δ 0.0~-0.5）→ 编排无副作用。
- 文档: `09-计划文件/2026-09-12_文字视觉观感编排设计与实证.md`（含参照集数据表与原则）。

## 【ZCode】v35 音乐结构驱动强度（2026-09-12 19:40-20:00）

- Boss: "继续"（承接"编排"方向）→ 把固定"隔拍"留白升级为**跟着音乐呼吸**。
- **实现**: local_i(t) = music_envelope 的 ±0.4s 五点均值 → p10-p95 归一化；
  阈值取 **drop 区** local_i 中位数；**强度优先贪心**选取（降序 + 间隔 ≥1.0s，防扎堆且不漏最响拍）；
  特效强度 `ik = 0.75+0.6*local_i` 作用于全部配方参数。
- **两个坑（实测）**: ① 阈值若用全体事件分位 → 被 intro/build 安静值拉偏，特效只剩 1 个；
  ② 选取若按时间序先到先得 → 最响的拍会被间隔规则漏掉（安静拍里出现过 local_i 0.948）。
- 实测: 特效 4/22 (18%)，全为 drop 最响拍 (0.60-0.75)，间隔 1.85-4.62s；18% 落在参照强段区间 3-21%；
  亮度同帧校验 Δ 0.0（除特效瞬间自身 +1.3/+5.7）。
- 交付: run53_text_v35_full.mp4；设计文档已追加 v35 附录。
- 可继续: build 区若出现强响段（如 riser）是否也给特效（当前设计为"留给 drop"，可讨论）。
