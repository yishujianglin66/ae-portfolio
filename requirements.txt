# AE-Knowledge-Vault — 核心运行依赖
# ============================================================================
# 安装（务必使用 Python 3.11/3.12，本机默认 python 3.13 超出 requires-python）：
#   py -3.12 -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# 说明：项目采用 bootstrap.py 路径注入（非 pip 打包）解决跨目录 import，
#       因此只需安装第三方依赖即可运行，无需 pip install -e .
# 重型 ML 依赖（语义搜索 / 风格分析）见 requirements-ml.txt，体积大、建议按需安装。
# ============================================================================

# --- 核心 ---
numpy>=1.24,<2.0
Pillow>=10.0
pydantic>=2.0
pydantic-settings>=2.0
loguru>=0.7

# --- Web / MCP 服务（Phase 0：AEToolsMCP + Web API 必需）---
fastapi>=0.104
uvicorn[standard]>=0.24
websockets>=12.0
fastmcp>=2.0

# --- AI / 工具链 ---
openai>=1.0
scikit-learn>=1.3

# --- 视频处理 / 素材下载 ---
yt-dlp>=2024.0
requests>=2.31
ffmpeg-python>=0.2
moviepy>=1.0
opencv-python>=4.8

# --- 音频分析 ---
librosa>=0.10
soundfile>=0.12

# --- 开发 / 测试（可选）---
pytest>=7.4
pytest-asyncio>=0.23
httpx>=0.25

# --- MG动画 / 手书动画 (2026-08-21 新增) ---
# SCAIL-2 / MuseTalk / MatAnyone2 的依赖在各自 external/ 子目录的 requirements.txt 中
# 以下为 MG 模板引擎 + Lottie 导出 + 手绘风格化的直接依赖
# (Pillow/numpy/opencv 已在上方核心依赖中)
