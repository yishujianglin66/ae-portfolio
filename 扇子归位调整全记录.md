# 扇子归位调整全记录 - 上官婉儿卡通渲染

> 本文档记录了从 v1 到 v15 所有版本的扇子位置调整迭代过程、报错信息、问题分析和经验教训。
> 目的：在新会话中快速获取完整上下文，避免重复踩坑。

---

## 📋 项目背景

### 目标
- 在 Blender 卡通渲染中，将扇子精确放置在角色右手上
- 确保帽子（凤凰头饰）正确戴在头上
- 人物在画面中居中显示

### 核心挑战
1. **模型无骨骼**：FBX 模型没有骨骼系统，无法通过骨骼名称定位手的位置
2. **扇子双部件**：扇子由两个分离部件组成（扇柄 + 扇面），需要正确组装
3. **武器与头饰混淆**：帽子名字包含 "Weapon"，容易被武器归位逻辑误判
4. **测试与实际差异**：测试脚本效果好，但完整渲染流程效果不同

### 模型信息
- 模型文件：`data/models/shangguan_er/shangguan_er.fbx`
- 角色：上官婉儿（书生造型）
- 总部件数：8 个
- 武器部件：2 个（扇柄 Weapon_High、扇面 Weapon_High002）
- 头饰部件：1 个（凤凰造型发冠，名字含 Weapon）

---

## 🔧 核心文件

| 文件 | 作用 |
|------|------|
| `puppet-automation/src/engines/blender/cel_shading.py` | 卡通渲染主脚本（武器归位逻辑在此） |
| `puppet-automation/src/engines/blender/engine.py` | Blender 引擎调用逻辑 |
| `debug_fan_v8_test.py` | v8 版本测试脚本（目前最稳定） |
| `fan_preview_final.html` | 预览页面 |

---

## 📜 版本迭代历史

### v1-v3：初始探索阶段
**问题**：扇子掉在地上，位置完全错误
**方案**：使用模型宽度百分比估算手位置
**结果**：失败。因为外袍/腿部比躯干宽，导致手位置估算偏差极大。

---

### v4-v6：部件检查与修复
**新增功能**：
- 检测偏离主体的部件
- 武器归位逻辑雏形
- 头部区域判断（保护帽子）

**问题**：
- 扇面（Weapon_High002）因位置在头部区域，被误判为头饰
- 扇子双部件没有正确组装

**报错**：无，但视觉效果差

---

### v7：体积判断引入
**改进**：
- 引入体积判断：体积 > 0.5 的视为武器，体积 < 0.5 的视为头饰
- 扇面体积约 1.5，被正确识别为武器
- 凤凰头饰体积约 0.3，被正确保留在头部

**问题**：手位置仍然不够精确

---

### v8：Z-slice 宽度曲线分析 ✅ 稳定版本
**核心算法**：
```
1. 将模型沿 Z 轴（高度方向）切片，每片 2% 高度
2. 计算每片的宽度（X轴方向）
3. 在躯干区域（40%-60% 高度）找中位数宽度
4. 手位置 = 躯干左边缘 - 手臂突出量（12%-15% 躯干宽度）
5. Y轴偏移 -0.15 ~ +0.30（调整前后深度）
```

**v8 参数**：
- 手臂突出量：12% 躯干宽度
- Y轴偏移：-0.15（稍靠后）
- 躯干区域：40%-60% 高度

**结果**：
- 人物显示正常 ✅
- 帽子位置正确 ✅
- 扇子靠近右手，但还不够精确 ⚠️

---

### v9-v11：手位置精细化
**尝试的方法**：
1. 顶点聚类分析：筛选手腕附近顶点聚类
2. 分层外侧点追踪：每 2% 高度一层，追踪手臂路径
3. 调整聚类百分比、Z轴范围等参数

**问题**：
- 算法越来越复杂，但精度提升有限
- 容易把外袍边缘误判为手臂

---

### v12-v14：扇子双部件组装优化
**改进**：
- 识别扇柄和扇面，分别处理
- 扇柄握手点移动至手心
- 扇面与扇柄末端对齐
- 应用自然旋转角度（绕X轴 70°，绕Z轴 -45°）

**参数微调**：
- 手臂突出量从 10% → 12% → 15%
- Y轴偏移从 -0.1 → -0.15 → +0.30

---

### v15：自然姿态优化 ❌ 重大失败
**改动**：
- 重新计算模型尺寸（`_model_h`, `_z_min`, `_z_max`）
- 不再使用全局的 `bounds_center` 和 `bounds_size`
- 优化扇子姿态，更自然的握持角度

**结果**：**灾难性失败**
- 人物完全不在画面中 ❌
- 扇子和帽子都掉到地上 ❌
- 相机定位完全错乱

**根因分析**：
v15 在武器归位部分重新计算了模型尺寸，但这个计算只基于武器部件（而不是整个模型），导致尺寸完全错误，进而所有坐标计算都错乱。同时，修改了全局的 bounds 变量，破坏了相机定位逻辑。

---

### v8 回退版（当前版本）
**方案**：回退到 v8 稳定版本，仅微调参数
- 手臂突出量：15%
- Y轴偏移：+0.30（更靠前，避免被外袍遮挡）
- 体积判断：保留
- 模板修复：`.format()` → `.replace()`

**Bug 修复（2026-07-25）**：
- **问题**：完整渲染中扇子和帽子都在地上，人物偏右
- **根因**：`create_cel_material_v2()` 中 `ColorRampElement.position` 报错，导致脚本在材质创建阶段就崩溃退出，武器归位代码根本没执行
- **报错信息**：`TypeError: bpy_struct: item.attr = val: ColorRampElement.position expected a float type, not str`
- **原因分析**：`RAMP_STOPS` 从 JSON 读取后，position 值可能是字符串类型（取决于 JSON 反序列化）
- **修复方案**：在三处 `color_ramp.elements[0].position = pos` 前都加上 `pos = float(pos)` 强制转换
- **修复结果**：渲染成功，武器归位代码正常执行 ✅

---

## 🐛 报错与问题记录

### 问题1：模板格式化错误
**现象**：完整渲染中扇子完全没动
**根因**：`cel_shading.py` 脚本通过 `.format()` 注入参数，但脚本中包含字典的花括号 `{}` 被误解析
**修复**：将 `.format()` 改为 `.replace()`，用 `{params_file}` 这样的占位符替换

**相关代码**：
```python
# 错误写法
script_content = CEL_SHADING_SCRIPT_TEMPLATE.format(params_file=...)

# 正确写法
script_content = CEL_SHADING_SCRIPT_TEMPLATE.replace("{params_file}", str(params_file))
```

---

### 问题2：v15 人物消失
**现象**：渲染结果中人物不在画面中
**根因**：v15 在武器归位部分重新计算 `_model_h` 等变量，且计算依据错误（只算了武器部件），破坏了全局相机定位
**修复**：回退到 v8，使用全局的 `bounds_center` 和 `bounds_size`

**教训**：
- **永远不要在局部逻辑中修改全局的 bounds 变量**
- 武器归位逻辑应该只读全局 bounds，不应该重新计算
- 修改相机/定位相关的代码要极其谨慎

---

### 问题3：扇面被误判为头饰
**现象**：扇面（Weapon_High002）停在头部不动
**根因**：扇面初始位置在头部区域，被头部保护逻辑误判为头饰
**修复**：引入体积判断，体积 > 0.5 的视为武器

**体积数据**：
- 扇柄（Weapon_High）：约 0.37
- 扇面（Weapon_High002）：约 1.54
- 凤凰头饰：约 0.3

---

### 问题4：完整渲染 vs 测试脚本效果不一致 ⚠️ 当前最大问题
**现象**：
- `debug_fan_v8_test.py` 测试脚本渲染效果很好，扇子在右手边
- 完整渲染（通过 `engine.render_cel_animation()`）中，扇子和帽子都在地上

**可能原因**：
1. 完整渲染的脚本模板注入有问题，武器归位代码没被正确执行
2. 参数传递有差异
3. 模型预处理顺序不同

**待排查**：
- 查看完整渲染的完整 stdout（目前只返回最后 500 字符）
- 对比测试脚本和完整流程的每一步输出

---

## 💡 经验教训

### 1. 测试脚本与主流程保持同步
测试脚本必须完全复制主流程的逻辑，包括：
- 模型导入方式
- 缩放和居中流程
- 材质设置顺序
- 武器归位的触发时机

### 2. 不要修改全局变量
武器归位等局部逻辑**只能读取**全局 bounds，不能修改。
如果需要局部计算，用新的变量名（如 `_local_bounds`）。

### 3. 体积是区分武器和头饰的好指标
- 武器通常体积较大（扇子展开面积大）
- 头饰通常体积较小
- 阈值：0.5 是个不错的分界点

### 4. Z-slice 宽度曲线分析是定位手的有效方法
比顶点聚类更稳定可靠：
- 躯干宽度相对稳定
- 手臂突出量可以用躯干宽度的比例估算
- 中位数宽度比平均值更抗干扰

### 5. 完整日志很重要
调试时必须能看到完整的 stdout，而不只是最后几行。
可以修改 `engine.py` 中的 `stdout_tail` 长度，或者把完整日志写入文件。

### 6. 每次只改一个变量
调参时每次只改一个参数，便于定位问题。
同时改多个参数 = 不知道哪个起了作用。

---

# 🎯 当前状态（2026-07-25 进度同步后更新）

### 已完成
- ✅ v8 核心算法稳定（Z-slice 宽度曲线分析）
- ✅ 体积判断区分武器和头饰
- ✅ 扇子双部件识别（handle: Weapon_High, blade: Weapon_High002）
- ✅ 帽子（凤凰头饰）正确保留在头部
- ✅ 测试脚本中扇子位置正确
- ✅ ColorRamp float(pos) 修复已生效（cel_shading.py 第686/719/756行）
- ✅ `.format()` → `.replace()` 模板注入修复已生效（engine.py 第1072行）
- ✅ **完整渲染中武器归位代码确认被执行**（stdout.log 证实，见下方问题5）
- ✅ 灯光段 KeyError: 'size' 崩溃已修复（params 中已包含 size 字段）

### 待解决
- ❌ **扇子部件位移计算错误，扇面飞到 X≈-5.6（画面外）**（最高优先级，根因已定位）
- ❌ 扇柄旋转围绕物体原点而非握点，旋转后脱离手部
- ❌ 人物偏右，不在画面中心（根因已定位：身体几何中心在 X≈+1.2）
- ⚠️ engine.py stdout_tail 仍为 500 字符（建议改为 5000）

### 渲染输出位置
- 最新完整渲染：`C:\output_director\full_render_test2\`（11:05:53，1帧成功）
- 调试运行日志：`C:\output_director\full_render_debug3\stdout.log`（关键证据）
- 测试脚本渲染：`C:\output_director\test_v8_fix\`
- 生成的脚本副本：`C:\output_director\full_render_debug3\generated_script.py`

---

# 🚀 下一步计划（更新版）

### 第一步：修复扇子组装逻辑（最高优先级，根因已定位）

**根因**（由 `full_render_debug3/stdout.log` 确认）：
```
>>> [v8-WEAPON] Fan handle moved to right hand:
>>>   shift=(X+0.07, Y+0.57, Z+2.13)     ← 扇柄到达手位 ✅
>>> [v8-WEAPON] Fan handle rotated: X=70°, Z=-45°  ← 绕物体原点旋转，柄脱离手 ❌
>>> [v8-WEAPON] Fan blade aligned with handle tip:
>>>   shift=(X+-4.78, Y+0.43, Z+-2.4)    ← 扇面飞到 X≈-5.6 ❌❌❌
```

**问题分析**：
1. 扇柄先移动到手位，再旋转 → 旋转围绕物体原点（柄中心）而非握点，柄被甩离手部
2. 扇面对齐使用旋转后柄的 bbox `min_x` 作为"柄末端" → 旋转后 bbox 极度膨胀，min_x 远在实际末端之外
3. 结果：扇面落在 X≈-5.6（模型范围仅 X=-3.05~+3.05），即画面最左侧地面

**修复方案**（修改 `cel_shading.py` 第1296-1381行的模板代码）：
```python
# 正确顺序：先旋转 → 再以握点为轴定位 → 扇面通过 parent 跟随
# 1. 先在原地旋转扇柄
handle_obj.rotation_euler = Euler((radians(70), 0, radians(-45)), 'XYZ')
# 2. 计算旋转后握点（柄底部中心）的世界坐标
# 3. 移动扇柄使握点对准手位置
# 4. 扇面 parent 到扇柄，用 local 偏移对齐到柄顶端
blade_obj.parent = handle_obj
blade_obj.matrix_parent_inverse = handle_obj.matrix_world.inverted()
# 5. 在 handle 的 local 空间中设置 blade 的相对位置
```

### 第二步：修复人物居中
**根因**：原始模型身体在 X=0~3，武器在 X=-3~-0.9；居中后身体视觉中心在 X≈+1.2，但相机目标固定在 X=0
**方案**：相机 target 的 X 坐标使用"非武器部件"的 bbox 中心，而非全局 bounds 中心
```python
# 在相机段（第1692行起），只用非武器物体计算居中参考
cam_target_x = body_only_center[0]  # 而非固定 0
```

### 第三步：增强调试能力
1. `engine.py` 第1102行：`stdout[-500:]` → `stdout[-5000:]`
2. 武器归位结束后打印所有武器部件的最终世界坐标
3. 保留生成脚本副本到输出目录（已有先例：full_render_debug3/generated_script.py）

---

## 🔍 调试技巧

### 查看完整 Blender 输出
修改 `engine.py` 中的：
```python
"stdout_tail": stdout[-500:] if stdout else "",
```
改为：
```python
"stdout_tail": stdout[-5000:] if stdout else "",
```

### 快速测试
使用 `debug_fan_v8_test.py` 测试，比完整渲染快很多：
```bash
python debug_fan_v8_test.py
```

### 检查 Blend 文件
渲染后可以用 Blender 打开 `.blend` 文件手动检查：
- 物体位置是否正确
- 武器是否被正确识别
- 相机位置是否合适

---

## 📊 关键参数速查

| 参数 | v8 默认值 | 说明 |
|------|----------|------|
| 目标模型高度 | 6.0 | 缩放后的模型总高度 |
| 躯干 Z 范围 | 40%-60% | 手位置估算的参考区域 |
| 手臂突出量 | 12%-15% | 相对于躯干宽度的比例 |
| Y轴偏移 | -0.15 ~ +0.30 | 扇子前后位置调整 |
| 体积阈值 | 0.5 | 武器 vs 头饰的判断阈值 |
| 头部 Z 阈值 | 75% 高度以上 | 头饰的位置判断区域 |
| 扇子旋转 X | 70° | 扇子俯仰角度 |
| 扇子旋转 Z | -45° | 扇子左右角度 |

---

# 📝 备注

- 文档最后更新：2026-07-25（第二次同步）
- 对应代码版本：v8 回退优化版
- 下次接手请先阅读本文档，避免重复踩坑

---

## 🔄 2026-07-25 进度同步记录

### 同步结论

| 项目 | 状态 | 说明 |
|------|------|------|
| ColorRamp float 修复 | ✅ 已生效 | 三处 `pos = float(pos)` 已就位 |
| 模板注入 .replace() | ✅ 已生效 | engine.py:1072 |
| 武器代码在完整渲染中执行 | ✅ 已确认 | stdout.log 有完整 [v8-WEAPON] 输出 |
| 灯光 KeyError 崩溃 | ✅ 已修复 | params JSON 已含 size 字段 |
| 扇子最终位置正确 | ❌ 未达标 | 扇面飞到 X≈-5.6，扇柄旋转后脱手 |
| 人物居中 | ❌ 未达标 | 身体中心偏右 +1.2 |
| Blender 版本 | 5.1.0 Alpha | hash 4aeed42d06ef |

### 新发现问题5：扇子组装逻辑缺陷（根因定位）
**现象**：完整渲染中扇子部件散落在角色周围地面，未组装在手中
**证据文件**：`C:\output_director\full_render_debug3\stdout.log` 第70-75行
**根因**：
1. **旋转时序错误**：先移动后旋转，旋转围绕物体原点（非握点），导致柄脱离手位
2. **bbox 对齐失效**：旋转后 bbox 膨胀，`min_x` 不代表真实柄端位置，扇面被位移 X-4.78
3. **测试脚本"效果好"是假象**：test_v8 有相同逻辑缺陷，只是相机角度让结果看起来"靠近右手"

**实际运行数据**：
- 手位置估算：X=-0.85, Y=0.077, Z=-0.297（Z-slice 分析，47%高度处）
- 躯干参考宽度：3.195，left_x=-0.371
- 扇柄尺寸：X=2.129, Y=0.154, Z=1.141（vol=0.373）
- 扇面尺寸：X=1.152, Y=0.748, Z=1.786（vol=1.539）
- 模型8个部件名：Object001, 5133_ShuSheng_Weapon_High002, 5133_ShuSheng_Weapon_High, 5133_ShuSheng_OP_High, 5133_ShuSheng_Leg_High, 5133_ShuSheng_High, 5133_ShuSheng_Face_High, 5133_ShuSheng_Baosha_High

### 新发现问题6：debug 运行崩溃（已解决但需记录）
**现象**：`full_render_debug3` 运行在灯光段崩溃，未产出渲染帧
**报错**：`KeyError: 'size'` @ generated script line 1289
**原因**：当时的 params JSON 中 key_light 字典缺少 size 字段
**状态**：后续运行（11:05 full_render_test2）的 params 已包含 size，问题不再复现

### 需要复验的内容
1. 修复组装逻辑后，重新运行 `run_full_render2.py` 并检查 frame_0001.png
2. 验证扇柄握点是否精确贴合手部（容差 < 0.2 单位）
3. 验证相机居中后人物是否在画面中心
4. 多帧渲染（frame_end=60）确认动画过程中扇子不穿帮

---

## 🎨 2026-07-25 风格逆向分析完成记录

### 成果
基于参考视频（抖音3渲2示例，1920×1080/24fps/15.83s）完成像素级风格逆向，产出可直接使用的渲染预设：

- **新预设**: `style="douyin_3to2"` 已注入 `cel_shading.py` 的 `get_style_preset()`
- **风格定义**: 日式赛璐璐骨架(2级硬色阶, banding=1.0) + 厚涂光影(87.5%渐变比, 5层细腻过渡)
- **关键参数**: 冷蓝阴影色系 + 暖金高光互补分级 / 2.5px深灰蓝描边 / 大柔光高对比布光 / 近黑深蓝背景
- **验证结果**: 与目标风格帧对比 9/13 维度精确匹配，4项偏差均为内容差异(全身vs特写)
- **渲染输出**: `C:\output_director\style_verify_3to2\frames\frame_0001.png` (256 samples)

### 完整报告
`temp/3to2_analysis/风格逆向分析报告.md` — 含视频结构解析、六维度像素分析、参数设计依据、验证对比表、后续建议

### 与扇子归位任务的关系
风格预设已就绪，扇子归位修复完成后即可用 `style="douyin_3to2"` 重新渲染验证最终效果。

---

## ✅ 2026-07-25 三渲二动画正式制作完成

### 执行顺序（全部完成）
1. **分析范围修正** — 仅frame_0011~0033为目标风格基准，预设升级至v3(对标目标段brightness_std=82.7)
2. **扇子归位修复(v9)** — 先旋转→握点对准手→顶点精确对齐→parent组装，握点(-0.85,0.08,-0.29)精确对到手位
3. **相机居中修复(v9)** — target+环绕圆心改用非武器部件bbox中心(X=1.23)，人物偏移+0.9%
4. **单帧验证** — 扇子/居中/风格全部通过
5. **60帧动画渲染** — 360°相机环绕+浮动+手持微晃，4.2分钟完成
6. **视频合成** — FFmpeg H.264 CRF18 慢速预设

### 最终交付物
| 文件 | 规格 |
|------|------|
| `C:\output_director\shangguan_3to2_final\shangguan_3to2_animation.mp4` | 1920×1080, 24fps, 60帧, 2.5s, H.264 |
| `C:\output_director\shangguan_3to2_final\frames\frame_0001~0060.png` | PNG序列帧(256 samples) |
| `C:\output_director\shangguan_3to2_final\cel_shading_scene.blend` | 场景文件(可直接打开) |
| `run_final_animation_render.py` | 一键复渲脚本 |

### 技术状态
- 风格: `douyin_3to2` v3 参数冻结（仅对标frame_0011~0033）
- 扇子: v9组装逻辑稳定，60帧环绕中保持跟随
- 已知无害警告: FBX贴图路径缺失(嵌入纹理)、Glare节点index 2、Vignette节点不存在(Blender 5.1)

---

## 🔧 2026-07-27 扇面误判修复 & v13发冠归位禁用

### 问题描述
用户反馈 v15 版本人物消失，回退到 v8 版本后，又发现扇面（Weapon_High002）被误判为头饰，导致扇子只有扇柄在手上，扇面留在头部区域。

### 根因分析
1. **扇面误判为头饰**：旧的头饰判断逻辑中，位置在头部区域且形状垂直的物体被判定为头饰。扇面（vol=1.539）因位置在头部且形状偏垂直，被误判为头饰，不进行移动。
2. **v13发冠归位逻辑缺陷**：v13 发冠归位使用**身体mesh**（顶点最多的主体mesh）的上部顶点计算头顶位置，但身体上部是肩膀/胸部，不是真正的头顶，导致发冠被移动到错误位置。
3. **"帽子"认知偏差**：用户看到的"头上的帽子"实际上是**扇面**（被误判为头饰留在头部），不是真正的头饰。真正的 Object001 是胸前饰品，不是头饰。

### 修复方案

#### 修复1：优化武器/头饰判断逻辑（第1380~1395行附近）
**修改前**：
```python
_is_headgear = False
if _in_head_zone:
    if _is_vertical:
        _is_headgear = True
    elif _wpn_vol <= 2.0 and _wpn_sz > 0.5:
        _is_headgear = True
```

**修改后**：
```python
_is_headgear = False
if _in_head_zone:
    if _wpn_vol > 1.0:
        _is_headgear = False  # 体积>1.0的即使在头部也视为武器
    elif _is_vertical and _wpn_vol <= 0.6:
        _is_headgear = True
    elif _wpn_vol <= 0.5 and _wpn_sz > 0.5:
        _is_headgear = True
```

**关键改动**：
- 增加体积判断：体积 > 1.0 的物体即使在头部也视为武器（扇面体积约1.5，会被正确识别为武器）
- 垂直形状的头饰体积限制从无限制改为 ≤ 0.6
- 小体积头饰的体积限制从 ≤ 2.0 收紧为 ≤ 0.5

#### 修复2：禁用 v13 发冠归位逻辑（第748~752行附近）
**修改前**：
```python
if _crown_part and _body_parts:
```

**修改后**：
```python
_v13_crown_reposition = False
if _v13_crown_reposition and _crown_part and _body_parts:
```

**原因**：
- FBX 导入后各部件位置通常是正确的
- v13 自动归位使用身体mesh计算头顶，位置不准确
- 自动归位反而会把正确位置的物体移错

### 验证结果
- ✅ 扇面（Weapon_High002）被正确识别为武器（vol=1.539 > 1.0）
- ✅ 扇柄（Weapon_High）被正确识别为武器
- ✅ 扇子双部件正确组装在右手上
- ✅ 胸前饰品（Object001）保持在原始位置
- ✅ 人物居中显示正常

### 当前状态
| 项目 | 状态 |
|------|------|
| 扇子归位 | ✅ 扇柄+扇面组装完整，在右手上 |
| 发冠/头饰 | ⚠️  Object001 是胸前饰品，非头饰。模型无独立头饰部件 |
| 人物居中 | ✅ 正常 |
| 卡通渲染风格 | ✅ 正常 |

### 待确认事项
1. 用户是否接受"头上没有大帽子"的造型（因为那本来就是扇面，不是帽子）
2. 是否需要为角色添加额外的头饰（需要额外的模型资源）

---

## 🧠 2026-07-27 经验汲取与防御性改进

### 历次失败的根本原因分析（5 大共性模式）

从 v1 到 v15 的所有失败可以归纳为 5 个共性模式：

#### 模式1：局部逻辑污染全局状态
- **v15 失败**：武器归位部分重新计算 `_model_h`/`_z_min`/`_z_max`，破坏了相机定位
- **v13 失败**：发冠归位修改 `_crown_part.location`，但参考mesh选错了
- **教训**：局部逻辑**只读**全局变量，不写。需要局部计算用新变量名

#### 模式2：旋转时序错误
- **v9 失败**：先移动后旋转 → 旋转围绕物体原点而非握点，物体被甩离目标
- **v12 修复**：先旋转→再以握点为轴定位→扇面通过 parent 跟随
- **教训**：旋转必须先于移动，parent 关系建立后子物体位置由 parent 决定

#### 模式3：部件分类靠"猜"而非"验证"
- **v4-v7 失败**：名字含 "weapon" 就当武器 → 但头饰名字也可能含 "weapon"
- **v14 失败**：扇面在头部区域且形状垂直 → 被误判为头饰
- **v14 修复**：增加体积判断（vol>1.0 优先判为武器）
- **教训**：分类逻辑要有多维度交叉验证，不能只靠单一指标

#### 模式4：测试与生产环境不一致
- **多次失败**：测试脚本用独立流程，主流程用模板注入 → 参数格式、执行顺序不同
- **假象**：测试脚本"看起来对"是相机角度的假象，实际扇面已飞到画面外
- **教训**：测试脚本必须完全复制主流程逻辑，包括导入、缩放、材质、归位全流程

#### 模式5：缺少"安全网"
- **v15 失败**：扇面飞到 X≈-5.6（画面外），没有校验机制及时发现
- **v13 失败**：发冠移到胸部，没有事后校验
- **教训**：每次移动物体后必须有最终位置校验

### 防御性代码改进（已实施）

#### 改进1：v15-SAFETY 安全网（cel_shading.py 第1728~1760行）
```python
# 武器归位完成后校验所有武器部件是否在合理范围内
# 使用欧几里得距离判断，阈值=模型最大尺寸*1.5
# 越界则在日志中输出 WARNING
```
**作用**：如果扇子飞到画面外（如 v15 的 X≈-5.6），安全网会立即告警。

#### 改进2：v13 发冠归位默认禁用（cel_shading.py 第751行）
```python
_v13_crown_reposition = False  # 默认禁用
```
**作用**：避免自动归位把正确位置的物体移错。

#### 改进3：体积优先的武器/头饰判断（cel_shading.py 第1385行）
```python
if _wpn_vol > 1.0:
    _is_headgear = False  # 体积>1.0的即使在头部也视为武器
```
**作用**：确保扇面（vol=1.539）不会被误判为头饰。

### 验证结果
```
>>> [SAFETY] 5133_ShuSheng_Weapon_High002 OK (dist=2.12 < 9.15)
>>> [SAFETY] 5133_ShuSheng_Weapon_High OK (dist=1.04 < 9.15)
```
两个武器部件都在安全范围内 ✅

### 未来开发指南
1. **任何修改 cel_shading.py 的操作**，修改后必须运行 `test_crown_v2.py` 验证
2. **日志中搜索 SAFETY**，确认没有 WARNING
3. **不要重新计算全局 bounds 变量**，只读不写
4. **新增部件移动逻辑**，必须在移动后添加位置校验
5. **旋转操作**，必须先旋转再移动（rotate-first 策略）
